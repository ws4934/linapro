# Tasks

## 工作台与演示租户体验

- [x] 将多租户工作台头部租户切换器的固定宽度、右侧间距、单行截断、暗色样式和建筑图标收敛到插件本地实现，避免继续依赖外部样式扫描。
- [x] 扩展既有多租户工作台 `E2E` 页面对象与断言，验证顶部租户切换器位于全局搜索左侧、保持固定宽度且不贴边。
- [x] 补齐 `tenant-user` 演示账号、菜单权限、本租户数据权限与 5 个活跃租户关联，保证数据隔离演示和多租户选择链路完整可复现。

## 插件标识治理

- [x] 抽取插件 ID 基础安全校验组件，并将 manifest ID、动态 artifact manifest ID、源码插件注册 ID 与插件依赖 ID 统一接入该治理能力。
- [x] 按官方规范化映射重命名全部官方插件目录、`plugin.yaml`、Go module/import、源码注册常量、依赖声明、默认配置和 README 示例。
- [x] 同步更新官方插件自有 SQL、表名/索引命名、DAO/DO/Entity、菜单 key、权限字符串、`cron handlerRef`、运行时 `i18n`/`apidoc` namespace、动态资源路径和治理扫描基线。

## 动态插件公开路由治理

- [x] 将动态插件 canonical 数据面入口切换为 `/x/{pluginId}/...`，并移除旧 `/api/v1/extensions/{pluginId}/...` 分发入口。
- [x] 调整宿主动态插件路由绑定、OpenAPI 投影、插件资源列表、`apidoc` 路径识别和示例插件前后端调用，确保新路径成为唯一公开口径。
- [x] 更新动态插件 runtime、controller、pluginbridge、性能审计脚本和 `E2E`/单元测试，覆盖新路径可用且旧路径不再分发的行为。

## 演示保护插件安装与启动治理

- [x] 为源码插件 `BeforeInstall` 生命周期暴露可信的启动自动启用安装上下文，且普通插件管理安装与自动依赖预安装不得继承该上下文。
- [x] 为 `linapro-ops-demo-guard` 注册 `BeforeInstall` 前置校验，只允许 `plugin.autoEnable` 启动自动启用安装，并本地化安装拒绝原因。
- [x] 保持 `linapro-ops-demo-guard` 的只读写请求拦截、最小会话白名单和插件治理写操作拦截语义不变。

## 治理验证与审查

- [x] 运行 OpenSpec 校验、后端 Go 编译/测试门禁、前端类型检查、运行时 `i18n` 治理扫描、动态插件相关 `E2E` 与静态残留扫描，确认旧插件 ID 和旧扩展路由不再作为正向运行时契约。
- [x] 复核本次变更的 `i18n`、缓存一致性、数据权限和开发工具影响结论，确保未引入新的无作用域缓存失效、权限绕过或平台专属脚本依赖。
- [x] 完成 `/lina-review` 审查，重点确认插件 ID 一致性、动态路由治理链路、启动自动启用上下文可信边界和演示工作台体验回归。

