# Tasks

## 1. 国际化基础设施

- [x] 建立覆盖静态界面文案、宿主动/插件动态元数据和业务内容的三层国际化模型。
- [x] 实现宿主统一语言解析、请求级语言上下文注入和默认语言回退逻辑。
- [x] 建立基于`manifest/i18n/<locale>/`与`manifest/i18n/<locale>/apidoc/**/*.json`的资源组织约定。
- [x] 将翻译内容治理收敛为文件资源单一事实来源，并移除运行时翻译持久化表依赖。
- [x] 建立稳定翻译键规则与源码命名空间注册机制。
- [x] 将默认内置语言收敛为`zh-CN`与`en-US`，保留通过资源目录和默认配置扩展新语言的机制。

## 2. 运行时分发与性能优化

- [x] 重写`Translate`、`TranslateSourceText`、`TranslateOrKey`和`TranslateWithDefaultLocale`热路径，命中缓存时直接读取消息而不克隆整包数据。
- [x] 将运行时翻译缓存重构为按语言与扇区分层的结构，并为失效操作增加显式作用域。
- [x] 为运行时翻译包接口增加`ETag`、`If-None-Match`协商和版本递增机制。
- [x] 为翻译热路径、缓存失效和`ETag`协商补充基准测试与单元测试。
- [x] 为前端运行时翻译包增加持久化缓存、后台协商刷新和按失败语义拆分的加载流程。

## 3. 服务边界与共享组件

- [x] 将庞大的`i18n.Service`拆分为`LocaleResolver`、`Translator`、`BundleProvider`和`Maintainer`四类小接口。
- [x] 将菜单、字典、系统参数、任务、角色、插件和`apidoc`等模块的国际化依赖收敛为最小接口集合。
- [x] 抽取共享`ResourceLoader`，统一宿主、源码插件、动态插件和`apidoc`的资源加载流程。
- [x] 将动态插件`WASM`自定义`section`读取能力下沉到`pkg/pluginbridge`并替换重复实现。
- [x] 明确业务模块在自身边界内维护本地化投影规则，禁止`i18n`基础服务反向依赖业务实体。

## 4. 动态元数据与默认工作台本地化

- [x] 让菜单、字典、系统参数、系统信息、角色、定时任务和插件元数据按当前请求语言返回本地化结果。
- [x] 让公共前端配置、登录页、工作台、动态菜单、路由标题和宿主嵌入式插件页面随语言切换同步刷新。
- [x] 将默认管理工作台首次启动语言与浏览器语言联动，并将所有中文语言标签统一映射到`zh-CN`。
- [x] 固定默认管理工作台文档方向为`ltr`，同步更新`<html dir>`与`ConfigProvider`方向配置。
- [x] 清理默认工作台中的繁体中文静态语言包与三语默认支持描述，保留双语默认交付。
- [x] 修正英文环境下的高频表单、表格、弹窗和抽屉布局，补齐系统生成内容与内置记录的英文显示。

## 5. 消息治理与交付物治理

- [x] 建立基于`bizerr`的结构化业务错误模型，并统一响应中`message`、`errorCode`、`messageKey`与`messageParams`的输出契约。
- [x] 按消息分类治理用户消息、导入导出交付物、宿主治理投影、开发者诊断、运维日志和用户数据的本地化边界。
- [x] 清理后端、插件和前端中可返回给调用方的硬编码中文与中英混杂文案。
- [x] 将导入导出模板、表头、失败原因和插件桥接错误收敛为按请求语言解析的统一实现。
- [x] 为宿主服务、插件桥接和动态插件返回结果建立稳定错误码与本地化显示契约。

## 6. 宿主治理与交互改进

- [x] 将动态插件权限按钮挂载到所属插件菜单下，并统一插件资源接口权限校验语义。
- [x] 收敛默认工作台内容为真实导航入口和业务语义，修正角色显示、内置保护对象和执行确认交互。
- [x] 阻断`demo-control`启用场景下的插件治理写操作，同时保留查询能力。
- [x] 统一源码插件与动态插件的安装审查、授权快照和生命周期验证流程。

## 7. 项目治理与文档

- [x] 统一LinaPro项目定位为“面向可持续交付的`AI`原生全栈框架”，并明确`apps/lina-core`核心宿主边界。
- [x] 建立仓库级`README.md`与`README.zh-CN.md`镜像治理规则，并更新相关说明文档。
- [x] 为数据库`init`与`mock`命令增加显式确认门禁和首错即停执行语义。
- [x] 更新`OpenAPI`文案、系统信息页、脚本提示和相关治理说明，使其与统一定位和国际化约束保持一致。

## 8. 测试、扫描与审查

- [x] 为默认启用语言补充缺失翻译检查、单元测试、基准测试和`E2E`覆盖。
- [x] 移除对默认`zh-TW`资源的专项断言与验收要求，同时保留中文浏览器语言标签回退到`zh-CN`的验证。
- [x] 将前后端硬编码文案扫描和国际化校验整合到本地验证入口中。
- [x] 更新国际化相关`README`与治理规则，说明默认双语基线、`ETag`协商、资源目录扩展方式和扫描要求。
- [x] 执行相关验证与`lina-review`，确认国际化、缓存、接口、测试和治理规则满足最终设计。
