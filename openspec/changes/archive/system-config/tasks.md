# Tasks

## 1. 运行时配置治理

- [x] 注册 JWT 有效期、在线会话超时、上传大小、登录 IP 黑名单等宿主运行时参数，以及登录页和工作台启动所需的公共前端配置元数据。
- [x] 为受保护配置补齐默认值、格式说明、值校验、导入/更新保护、稳定键删除保护和公共白名单读取入口。
- [x] 将运行时配置接入认证、在线会话、文件上传和前端启动链路，让宿主行为直接消费配置服务的有效值。
- [x] 将 `sys.upload.maxSize` 的数据库种子值、配置模板默认值和后端静态兜底值统一为 20 MB，并同步更新上传校验、请求体限制、友好错误提示和相关测试。

## 2. 缓存、一致性与覆盖率门禁

- [x] 为受保护运行时参数实现进程内快照缓存、共享修订号同步和单机模式优化，减少热路径重复查询并保持多实例收敛。
- [x] 为配置管理补齐插件路径、公共前端辅助方法、运行时修订号控制器、快照缓存、默认值和异常分支测试，使 `apps/lina-core/internal/service/config` 包级覆盖率达到 80% 以上。
- [x] 记录并审查缓存一致性边界：启动快照仅限单次启动编排，请求级 release 复用仅限单次请求或列表投影，集群模式仍以数据库与现有修订机制为权威。

## 3. 系统 API 文档与插件治理

- [x] 以宿主管理的 OpenAPI 构建器替代 GoFrame 默认 `/api.json`，投影启用中的宿主接口、源码插件接口和动态插件接口，并排除内部或非业务路由。
- [x] 在源码插件路由注册时捕获插件归属、HTTP 方法、路径和 DTO `g.Meta` 文档元数据，保持 `plugin.yaml` 精简且不要求重复声明后端路由。
- [x] 保持源码插件中间件顺序和组合由插件自行控制，不把源码插件接线改造成动态插件式声明模型。
- [x] 完善插件详情弹窗、宿主服务范围展示、资源分组、空态表现和动态插件示例分页，并统一结构化日志开关、扩展命名空间和相关后端注释治理。

## 4. 启动链路 SQL 效率

- [x] 记录启动 SQL 与日志基线，并将交付默认配置中的 `database.default.debug` 统一为 `false`，保留显式 debug 诊断模式。
- [x] 设计并实现一次 HTTP 启动编排内共享的 `StartupContext`，复用 catalog、integration 和 job 启动快照以及阶段统计采集器。
- [x] 将自动启用、源码插件 HTTP 路由注册、运行时前端包预热、插件只读投影和 cron 启动同步全部接入共享启动上下文，避免重复构造等价快照。
- [x] 将插件清单同步改为差异驱动的 no-op fast path：无差异时不写库、不启事务、不做写后回读；有差异时同步更新当前启动快照。
- [x] 让内置定时任务使用声明派生的 projection snapshot 完成运行时注册，并保持持久化调度器启动扫描排除 `is_builtin=1` 任务。
- [x] 增加结构化启动摘要日志，以及默认无 SQL 明细、显式 debug、插件 no-op 同步和启动快照预算的 smoke/回归测试。

## 5. 登录后首页 SQL 效率

- [x] 优化 DB-only 在线会话校验，改为单次读取后判断有效性、租户隔离和超时状态，并仅在超过写入节流窗口时更新活跃时间。
- [x] 保持过期会话删除、强制下线、JWT revoke 和租户不匹配拒绝语义不变，同时减少每个鉴权请求对 `sys_online_session` 的往返次数。
- [x] 为插件 catalog 与 runtime 列表读取引入请求级或列表级 release 快照复用，减少同一作用域内对 `sys_plugin_release` 的按 ID 和按版本重复查询。
- [x] 为会话校验和插件 release 复用补齐自动化测试，覆盖有效、过期、租户不匹配、近期活跃、按 ID 复用、按版本复用和刷新后状态更新等场景。

## 6. 验证与审查

- [x] 运行配置管理、会话、插件 catalog/runtime、启动编排等相关 Go 测试与启动 smoke，验证运行时配置、启动效率和首页 SQL 优化未引入回归。
- [x] 运行 OpenSpec 校验、归档审查和 `git diff --check`，并记录 i18n、数据权限、缓存一致性和开发工具跨平台影响评估。
- [x] 复核归档文档语言、规范结构和能力边界，确保提案、设计、任务和增量规范在语义上保持一致。
