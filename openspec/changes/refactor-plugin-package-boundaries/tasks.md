## 1. 现状审计与边界确认

- [x] 1.1 审计`apps/lina-core/pkg/pluginservice`、`pkg/pluginhost`、`pkg/pluginbridge`、`pkg/sourceupgrade`、宿主插件运行时、官方源码插件、动态插件样例和`linactl`中的导入关系，形成迁移清单。
- [x] 1.2 确认本变更不修改 HTTP API、SQL、前端 UI、插件 manifest 字段或运行时用户可见文案，并记录`i18n`、数据权限、缓存一致性、开发工具跨平台和测试策略影响。
- [x] 1.3 确认`pkg/plugin/internal`只承载`pkg/plugin/...`公共组件自身实现，不放置源码插件、动态 guest 或宿主插件运行时需要直接 import 的契约。
- [x] 1.4 确认`pkg/plugin/capability/internal`保留为 capability 私有实现边界，`capabilityregistry`等仅服务能力目录的实现不迁入`pkg/plugin/internal`。

## 2. 建立目标插件公共命名空间

- [x] 2.1 新建`apps/lina-core/pkg/plugin/`目标结构，并迁移`pluginhost`到`pkg/plugin/pluginhost`，保持源码插件 route、hook、cron、lifecycle 和 provider factory 贡献语义不变。
- [x] 2.2 迁移`pluginbridge`到`pkg/plugin/pluginbridge`，保持 ABI、WASM envelope、host call、host service、codec、artifact 和 guest runtime 协议行为不变。
- [x] 2.3 将`pluginbridge`内部实现继续放在职责明确的`internal`或子组件边界内，避免 bridge 低层实现泄漏为业务能力 owner。

## 3. 将 pluginservice 迁移为 capability

- [x] 3.1 将`apps/lina-core/pkg/pluginservice`迁移到`apps/lina-core/pkg/plugin/capability`，并更新包注释、组件说明和根能力目录命名，使其表达插件消费宿主能力目录语义。
- [x] 3.2 迁移`contract`、`guest`、`bizctx`、`config`、`hostconfig`、`manifest`、`orgcap`、`pluginlifecycle`、`pluginstate`、`tenantcap`、`tenantfilter`和`internal/capabilityregistry`等子包。
- [x] 3.3 确保`capability/guest`、`capability/bizctx`、`capability/contract`等插件可导入契约不在`internal`目录下。
- [x] 3.4 保持动态插件 manifest 中`hostServices`字段和授权快照语义不变，仅将 guest client 和 host handler 映射到新的`capability`包路径。
- [x] 3.5 保持`capability/internal/capabilityregistry`为 capability 私有实现，静态检索确认`pluginhost`和`pluginbridge`不直接 import 该内部 registry。

## 4. 删除公共 sourceupgrade

- [x] 4.1 删除`apps/lina-core/pkg/sourceupgrade`公共 facade、contract 和测试。
- [x] 4.2 将源码插件升级状态和结果 DTO 归属到`internal/service/plugin`或`internal/service/plugin/internal/sourceupgrade`，由宿主插件服务接口对内部调用方暴露必要方法。
- [x] 4.3 更新`internal/service/plugin`和相关测试，确保源码插件升级治理仍通过内部 sourceupgrade 实现完成。

## 5. 更新调用方和工具链

- [x] 5.1 更新宿主插件运行时、WASM host service、data host、openapi、catalog、runtime、integration 和启动装配中的 import 路径。
- [x] 5.2 更新官方源码插件的`backend/plugin.go`、provider adapter、controller/service 测试和内部服务导入路径。
- [x] 5.3 更新动态插件样例、dynamic route declaration、guest host call demo 和 WASI 构建入口导入路径。
- [x] 5.4 更新`hack/tools/linactl`中 WASM 构建器、动态插件 smoke fixture、go.mod 复制逻辑和相关测试中的旧路径。

## 6. 规范和治理清理

- [x] 6.1 更新本变更命中的 OpenSpec 规范，确保目标路径统一为`pkg/plugin/pluginhost`、`pkg/plugin/pluginbridge`和`pkg/plugin/capability`。
- [x] 6.2 静态检索确认生产代码不再 import `lina-core/pkg/pluginservice`、`lina-core/pkg/pluginhost`、`lina-core/pkg/pluginbridge`或`lina-core/pkg/sourceupgrade`旧路径。
- [x] 6.3 静态检索确认没有源码插件、动态插件或宿主插件运行时代码 import `lina-core/pkg/plugin/internal/**`。
- [x] 6.4 静态检索确认`pkg/plugin/pluginhost`和`pkg/plugin/pluginbridge`没有 import `pkg/plugin/capability/internal/**`。

## Feedback

- [x] **FB-1**: 将`plugindb`一并迁移到`pkg/plugin`下合适的 capability data 组件中
- [x] **FB-2**: 审查并修正`apps/lina-core/pkg/plugin`下组件目录名、包名、主文件名和文件前缀不一致的问题
- [x] **FB-3**: 收窄`pluginbridge/guest`职责并将动态 guest 能力 client 语义迁入`capability/guest`
- [x] **FB-4**: 将`pkg/plugin/capability`根接口命名由`Directory`调整为`Services`
- [x] **FB-5**: 收敛`pluginbridge`、`pluginbridge/guest`和`capability/guest`重复协议别名，建立单一公开协议出口

## 7. 验证

- [x] 7.1 运行`cd apps/lina-core && go test ./pkg/plugin/... -count=1`。
- [x] 7.2 运行覆盖宿主插件运行时和启动装配的 Go 编译门禁，至少包括`cd apps/lina-core && go test ./internal/service/plugin/... ./internal/cmd -count=1`或更窄但能覆盖变更路径的组合。
- [x] 7.3 运行官方源码插件受影响包的 Go 编译门禁，覆盖 provider adapter、插件入口和内部 service/controller。
- [x] 7.4 运行动态插件样例普通 Go 测试和`GOOS=wasip1 GOARCH=wasm`编译 smoke，确认 guest SDK 新路径可用。
- [x] 7.5 运行`cd hack/tools/linactl && go test ./... -count=1`，确认 WASM 构建器和 smoke fixture 已同步新路径。
- [x] 7.6 运行`openspec validate refactor-plugin-package-boundaries --strict`和`git diff --check`。
- [x] 7.7 完成实现后执行`lina-review`，并按审查结论修复阻断问题。

## 8. 执行记录

- 2026-05-25 规则加载：已读取`openspec-apply-change`、`goframe-v2`和`karpathy-guidelines`技能说明；已按`AGENTS.md`读取`.agents/rules/openspec.md`、`.agents/rules/documentation.md`、`.agents/rules/architecture.md`、`.agents/rules/data-permission.md`、`.agents/rules/plugin.md`、`.agents/rules/backend-go.md`、`.agents/rules/dev-tooling.md`、`.agents/rules/testing.md`、`.agents/rules/cache-consistency.md`、`.agents/rules/i18n.md`和`.agents/rules/api-contract.md`。本轮只执行现状审计与边界确认，不修改 Go 生产代码、HTTP API、SQL、前端 UI、插件 manifest 或运行时文案。
- 2026-05-25 迁移清单：当前`apps/lina-core/pkg/plugin/`目标目录尚不存在；现有旧公共目录包括`apps/lina-core/pkg/pluginhost`、`apps/lina-core/pkg/pluginbridge`、`apps/lina-core/pkg/pluginservice`和`apps/lina-core/pkg/sourceupgrade`。静态检索范围为`apps/lina-core`、`apps/lina-plugins`和`hack/tools/linactl`的 Go 文件，旧路径命中文件数为`pluginservice` 152、`pluginhost` 88、`pluginbridge` 139、`sourceupgrade` 5。
- 2026-05-25 迁移范围确认：`pluginhost`需迁移到`pkg/plugin/pluginhost`并保留源码插件 route、hook、cron、lifecycle、provider factory 和 manifest view 语义；`pluginbridge`需迁移到`pkg/plugin/pluginbridge`并保留`contract`、`guest`、ABI、WASM artifact、codec、hostcall、hostservice 和 facade 行为；`pluginservice`需迁移到`pkg/plugin/capability`，覆盖`contract`、`guest`、`bizctx`、`config`、`hostconfig`、`manifest`、`orgcap`、`pluginlifecycle`、`pluginstate`、`tenantcap`、`tenantfilter`和`internal/capabilityregistry`；`sourceupgrade`公共 facade 需删除，现有`internal/service/plugin/internal/sourceupgrade`使用点需改为内部 DTO 和服务接口。
- 2026-05-25 调用方确认：宿主运行时命中`internal/cmd`、`internal/controller/plugin`、`internal/controller/user`、`internal/service/plugin/internal/{catalog,datahost,frontend,integration,openapi,runtime,sourceupgrade,testutil,wasm}`和`internal/service/pluginhostservices`等；官方源码插件命中`linapro-content-notice`、`linapro-demo-source`、`linapro-monitor-*`、`linapro-ops-demo-guard`、`linapro-org-core`和`linapro-tenant-core`；动态插件样例命中`linapro-demo-dynamic/main.go`、`backend/plugin.go`、`plugin_host_dispatcher.go`及其 controller/service；工具链命中`hack/tools/linactl/internal/wasmbuilder`和`hack/tools/linactl/main_test.go`。
- 2026-05-25 影响评估：`i18n`无资源影响，本轮和目标重构不新增、修改或删除运行时语言包、插件`manifest/i18n`、`apidoc i18n JSON`或用户可见文案；数据权限无新增数据操作面，后续迁移必须保持 org/tenant/data capability 的既有权限边界；缓存一致性无新增缓存键或失效策略，后续迁移必须复用现有插件状态、运行时配置、能力目录和 host service 共享实例；HTTP API 和 SQL 无影响；前端 UI 无影响；开发工具跨平台影响仅限`linactl` Go 工具导入路径和 smoke fixture，同步后通过`cd hack/tools/linactl && go test ./... -count=1`验证；测试策略以 Go 编译门禁、动态插件 WASI smoke、静态检索、OpenSpec strict 校验和`git diff --check`为主，纯内部包路径重构不新增 E2E。
- 2026-05-25 边界确认：`pkg/plugin/internal`只作为`pkg/plugin/...`公共组件之间共享实现的预留边界，源码插件、动态 guest SDK 和宿主插件运行时需要 import 的`guest`、`bizctx`、`contract`、`config`、`manifest`、`orgcap`、`tenantcap`等契约必须留在公开子包；宿主运行时内部实现必须保留在`internal/service/plugin/internal/...`等宿主可导入边界，不迁入`pkg/plugin/internal`。
- 2026-05-25 capability internal 确认：`capabilityregistry`当前只服务能力目录的 provider registry、懒加载、fallback、delegation 和冲突检测语义，迁移目标应为`pkg/plugin/capability/internal/capabilityregistry`。不得迁入`pkg/plugin/internal`，避免`pkg/plugin/pluginhost`和`pkg/plugin/pluginbridge`获得可导入 capability 私有 registry 的编译可见性。
- 2026-05-25 任务 2.1 执行记录：已使用`git mv`将`apps/lina-core/pkg/pluginhost`迁移到`apps/lina-core/pkg/plugin/pluginhost`，并同步更新宿主运行时、官方源码插件和相关测试中的`lina-core/pkg/pluginhost`导入为`lina-core/pkg/plugin/pluginhost`。本任务只调整源码插件贡献入口包路径，未修改 HTTP API、SQL、前端 UI、插件 manifest 字段或运行时用户可见文案；`i18n`、数据权限、缓存一致性和开发工具跨平台无新增影响。验证已执行`cd apps/lina-core && go test ./pkg/plugin/pluginhost -count=1`通过；静态检索确认 Go 源码中不再出现`lina-core/pkg/pluginhost`旧导入，且`lina-core/pkg/plugin/pluginhost/internal`导入仅存在于`pluginhost`自身包内。
- 2026-05-25 任务 2.2/2.3 执行记录：已使用`git mv`将`apps/lina-core/pkg/pluginbridge`迁移到`apps/lina-core/pkg/plugin/pluginbridge`，并同步更新 Go 源码中的`lina-core/pkg/pluginbridge`导入为`lina-core/pkg/plugin/pluginbridge`，同步调整`.github/workflows/reusable-plugin-command-smoke.yml`和`.github/workflows/reusable-make-command-smoke.yml`中隔离 smoke fixture 复制路径。本任务只调整动态插件 bridge 公共包路径、内部 import 和 CI smoke fixture 路径，不修改 ABI 常量、WASM envelope、host call、host service、codec、artifact、guest runtime 协议、HTTP API、SQL、前端 UI、插件 manifest 字段或运行时用户可见文案；`i18n`、数据权限和缓存一致性无新增影响。开发工具跨平台影响仅为`hack/tools/linactl`中 Go 构建器、smoke fixture 和 CI workflow 复制路径同步，未新增平台专属命令。验证已执行`cd apps/lina-core && go test ./pkg/plugin/pluginbridge/... -count=1`和`cd apps/lina-core && go test ./pkg/plugin/... -count=1`通过；静态检索确认`pkg/plugin/pluginbridge`没有 import `pkg/plugin/capability/internal/**`或旧`lina-core/pkg/pluginbridge`路径，bridge 内部实现仍保留在`contract`、`guest`和职责明确的`internal/artifact`、`internal/codec`、`internal/hostcall`、`internal/hostservice`、`internal/wire`边界内。
- 2026-05-25 任务 3.1-3.5 执行记录：已使用`git mv`将`apps/lina-core/pkg/pluginservice`迁移到`apps/lina-core/pkg/plugin/capability`，并将根包声明更新为`package capability`；迁移阶段曾将根能力集合命名为`Directory`、插件作用域工厂命名为`ScopedDirectoryFactory`、插件作用域获取入口命名为`DirectoryForPlugin`，最终命名已在 FB-4 调整为`capability.Services`、`ScopedServicesFactory`和`ServicesForPlugin`。已迁移`contract`、`guest`、`bizctx`、`config`、`hostconfig`、`manifest`、`orgcap`、`pluginlifecycle`、`pluginstate`、`tenantcap`、`tenantfilter`和`internal/capabilityregistry`等子包，并同步宿主运行时、源码插件、动态插件样例和测试中的导入路径到`pkg/plugin/capability/**`。本任务不修改 HTTP API、SQL、前端 UI、插件 manifest 字段、动态插件`hostServices`授权快照语义或运行时用户可见文案；`i18n`资源无影响，数据权限和缓存一致性保持既有 org/tenant/config/manifest 能力语义。验证已执行`cd apps/lina-core && go test ./pkg/plugin/capability/... -count=1`和`cd apps/lina-core && go test ./pkg/plugin/pluginhost -count=1`通过；静态检索确认 Go/go.mod/go.sum 中不再出现`lina-core/pkg/pluginservice`旧路径，`capability/guest`、`capability/bizctx`和`capability/contract`均位于公开子包，且`pkg/plugin/pluginhost`、`pkg/plugin/pluginbridge`、宿主插件运行时、官方源码插件和动态插件样例均未 import `lina-core/pkg/plugin/capability/internal/**`。
- 2026-05-25 任务 4.1-4.3 执行记录：已删除`apps/lina-core/pkg/sourceupgrade`公共 facade、`contract`和对应 facade 测试；源码插件升级状态和执行结果 DTO 已归属到`apps/lina-core/internal/service/plugin/internal/sourceupgrade`，并由`apps/lina-core/internal/service/plugin`通过`SourceUpgradeStatus`和`SourceUpgradeResult`别名对内部调用方暴露。`internal/service/plugin`不再 import `lina-core/pkg/sourceupgrade/contract`，宿主启动 readiness、插件服务显式升级和源码插件升级测试继续委托内部 sourceupgrade 组件完成。本任务不修改 HTTP API、SQL、前端 UI、插件 manifest 字段或运行时用户可见文案；`i18n`资源、数据权限和缓存一致性无新增影响；开发工具跨平台无影响。验证已执行`cd apps/lina-core && go test ./internal/service/plugin/internal/sourceupgrade -count=1`和`cd apps/lina-core && go test ./internal/service/plugin -run 'Test(SourcePlugin|UpgradeSourcePlugin|ListSourceUpgradeStatuses|ValidateSourcePlugin|PluginPlatform)' -count=1`通过；静态检索确认 Go 源码中不再出现`pkg/sourceupgrade`、`sourceupgradecontract`、`SourcePluginStatus`或`SourcePluginUpgradeResult`旧公共 contract 残留。
- 2026-05-25 任务 5.1 执行记录：已复核宿主插件运行时、WASM host service、data host、openapi、catalog、runtime、integration、启动装配和相关宿主控制器/API 文档加载路径中的导入，确认均已使用`lina-core/pkg/plugin/pluginhost`、`lina-core/pkg/plugin/pluginbridge`和`lina-core/pkg/plugin/capability`新路径。本任务未继续修改 Go 代码，只完成宿主运行时范围静态核查、编译门禁和任务记录；不修改 HTTP API、SQL、前端 UI、插件 manifest 字段或运行时用户可见文案，`i18n`资源、数据权限和缓存一致性无新增影响，开发工具跨平台无影响。验证已执行`rg -n "pkg/pluginservice|pkg/pluginhost|pkg/pluginbridge|pkg/sourceupgrade" apps/lina-core/internal/cmd apps/lina-core/internal/service/plugin apps/lina-core/internal/service/pluginhostservices apps/lina-core/internal/service/apidoc apps/lina-core/internal/controller/plugin apps/lina-core/pkg/plugin/capability/data --glob '*.go'`无命中；`rg -n "lina-core/pkg/plugin/internal|lina-core/pkg/plugin/capability/internal" apps/lina-core/internal/cmd apps/lina-core/internal/service/plugin apps/lina-core/internal/service/pluginhostservices apps/lina-core/internal/service/apidoc apps/lina-core/internal/controller/plugin apps/lina-core/pkg/plugin/capability/data --glob '*.go'`无命中；`cd apps/lina-core && go test ./internal/service/plugin/internal/catalog ./internal/service/plugin/internal/datahost ./internal/service/plugin/internal/integration ./internal/service/plugin/internal/openapi ./internal/service/plugin/internal/runtime ./internal/service/plugin/internal/wasm ./internal/service/pluginhostservices ./internal/cmd -count=1`通过。
- 2026-05-25 任务 5.2-5.4 执行记录：已复核并同步官方源码插件、动态插件样例和`hack/tools/linactl`中的旧插件公共包导入路径，确认插件入口、provider adapter、controller/service 测试、dynamic route declaration、guest host call demo、WASI 构建入口、WASM 构建器、动态插件 smoke fixture 和 go.mod 复制逻辑均使用`lina-core/pkg/plugin/pluginhost`、`lina-core/pkg/plugin/pluginbridge`和`lina-core/pkg/plugin/capability`新路径。为保证动态插件样例可从自身模块独立测试和通过`linactl wasm`构建，已在`apps/lina-plugins/linapro-demo-dynamic/go.mod`补充`lina-core v0.0.0`依赖与`replace lina-core => ../../lina-core`，并由`go mod tidy`补齐必要校验和。本任务不修改 HTTP API、SQL、前端 UI、插件 manifest 字段、动态插件`hostServices`授权快照语义或运行时用户可见文案；`i18n`资源、数据权限和缓存一致性无新增影响。开发工具跨平台影响仅为`linactl` Go 工具和动态 WASM 构建 fixture 继续使用跨平台 Go 实现，未新增平台专属脚本或命令。验证已执行`rg -n "lina-core/pkg/(pluginservice|pluginhost|pluginbridge|sourceupgrade)|pkg/pluginservice|pkg/pluginhost|pkg/pluginbridge|pkg/sourceupgrade" . --glob '*.go' --glob 'go.mod' --glob 'go.sum'`于`apps/lina-plugins`无命中；同等静态检索于`hack/tools/linactl`无旧路径命中；`cd apps/lina-plugins && GOWORK=off go test lina-plugin-linapro-content-notice/backend/... lina-plugin-linapro-demo-source/backend/... lina-plugin-linapro-monitor-loginlog/backend/... lina-plugin-linapro-monitor-online/backend/... -count=1`通过；`cd apps/lina-plugins && GOWORK=off go test lina-plugin-linapro-monitor-operlog/backend/... lina-plugin-linapro-monitor-server/backend/... lina-plugin-linapro-ops-demo-guard/backend/... lina-plugin-linapro-org-core/backend/... lina-plugin-linapro-tenant-core/backend/... -count=1`通过；`cd apps/lina-plugins/linapro-demo-dynamic && GOWORK=off go test ./... -count=1`通过；`go run ./hack/tools/linactl wasm p=linapro-demo-dynamic out=/tmp/linapro-wasm-smoke`通过并生成`/tmp/linapro-wasm-smoke/linapro-demo-dynamic.wasm`；`cd hack/tools/linactl && go test ./... -count=1`通过。直接在动态插件目录执行`GOWORK=off GOOS=wasip1 GOARCH=wasm go build .`因该样例按设计依赖`linactl wasm`临时生成`wasip1`dispatcher 而失败，不作为本任务完成门禁。
- 2026-05-25 任务 6.1 执行记录：已复核本变更命中的`plugin-config-service`、`plugin-host-service-extension`、`plugin-package-boundary-governance`、`plugin-upgrade-governance`和`pluginbridge-subcomponent-architecture`五个 OpenSpec 增量规范，确认目标路径已统一为`pkg/plugin/pluginhost`、`pkg/plugin/pluginbridge`和`pkg/plugin/capability`。旧`pkg/pluginservice`、`pkg/pluginhost`、`pkg/pluginbridge`和`pkg/sourceupgrade`只保留在历史背景、旧入口删除、静态检索失败条件和禁止导入约束中，未作为目标路径继续沉淀。本任务仅更新 OpenSpec 任务状态和执行记录，不修改 Go 生产代码、HTTP API、SQL、前端 UI、插件 manifest 字段或运行时用户可见文案；`i18n`、数据权限、缓存一致性和开发工具跨平台无新增影响。验证已执行`rg -n "apps/lina-core/pkg/pluginhost|apps/lina-core/pkg/pluginbridge|apps/lina-core/pkg/pluginservice|apps/lina-core/pkg/sourceupgrade|lina-core/pkg/pluginhost|lina-core/pkg/pluginbridge|lina-core/pkg/pluginservice|lina-core/pkg/sourceupgrade" openspec/changes/refactor-plugin-package-boundaries/specs`，命中均为旧入口删除或禁止导入约束；`rg -n "pkg/plugin/pluginhost|pkg/plugin/pluginbridge|pkg/plugin/capability" openspec/changes/refactor-plugin-package-boundaries/specs`确认新目标路径存在。
- 2026-05-25 任务 6.2-6.4 执行记录：已完成包边界治理静态检索并标记任务完成。`rg -n 'lina-core/pkg/(pluginservice|pluginhost|pluginbridge|sourceupgrade)(/|"|$)' apps/lina-core apps/lina-plugins hack/tools/linactl --glob '*.go' --glob 'go.mod' --glob 'go.sum'`无命中，确认生产 Go 代码、官方插件、动态插件样例和`linactl`中不再 import 旧公共路径；`rg -n 'lina-core/pkg/plugin/internal(/|"|$)' apps/lina-core/internal apps/lina-plugins hack/tools/linactl --glob '*.go' --glob 'go.mod' --glob 'go.sum'`无命中，确认源码插件、动态插件和宿主插件运行时没有直接 import `pkg/plugin/internal/**`；`rg -n 'lina-core/pkg/plugin/capability/internal(/|"|$)' apps/lina-core/pkg/plugin/pluginhost apps/lina-core/pkg/plugin/pluginbridge --glob '*.go' --glob 'go.mod' --glob 'go.sum'`无命中，确认`pluginhost`和`pluginbridge`没有直接 import capability 私有 registry。本任务只更新 OpenSpec 任务状态和执行记录，不修改 Go 生产代码、HTTP API、SQL、前端 UI、插件 manifest 字段或运行时用户可见文案；`i18n`资源、数据权限、缓存一致性和开发工具跨平台无新增影响。
- 2026-05-25 FB-1 根因分析：原设计将`plugindb`作为 Open Question 留待后续评估，导致顶层`apps/lina-core/pkg/plugindb`仍作为插件公共 data SDK 暴露。根据当前包边界目标，`plugindb`实际承担动态插件 data 能力的 guest-side ORM facade、typed plan 契约和宿主 data governance facade，属于插件消费宿主能力目录下的 data 子能力，应迁移到公开`pkg/plugin/capability/data`；不能迁入`pkg/plugin/capability/internal`，因为动态插件 guest、宿主 datahost 和工具 smoke fixture 都需要直接导入该契约，放入`internal`会破坏 Go 可见性。规则加载：已读取`lina-feedback`、`goframe-v2`、`karpathy-guidelines`技能说明；已读取`.agents/rules/openspec.md`、`.agents/rules/documentation.md`、`.agents/rules/architecture.md`、`.agents/rules/plugin.md`、`.agents/rules/backend-go.md`、`.agents/rules/data-permission.md`、`.agents/rules/testing.md`、`.agents/rules/cache-consistency.md`、`.agents/rules/dev-tooling.md`、`.agents/rules/i18n.md`、`.agents/rules/api-contract.md`和`.agents/rules/database.md`。影响分析：本反馈修改 Go 公共包路径、OpenSpec 文档、动态插件样例导入、宿主 datahost 导入、CI smoke fixture 和`linactl`测试期望；不新增 HTTP API、SQL、前端 UI、插件 manifest 字段、运行时用户可见文案、缓存键或失效策略；数据权限语义保持既有 data host service 授权、资源表和方法校验；开发工具跨平台影响限于 Go 工具测试与 workflow 复制路径同步；测试策略为 Go 编译门禁、动态插件 smoke、静态检索、OpenSpec strict 校验和`git diff --check`。
- 2026-05-25 FB-1 修复记录：已使用`git mv`将`apps/lina-core/pkg/plugindb`迁移到`apps/lina-core/pkg/plugin/capability/data`，保留包名`plugindb`和现有 guest-side ORM facade 语义；`internal/host`和`internal/plan`同步迁移到`capability/data/internal/{host,plan}`，仅由公开 data facade 使用。已同步宿主 datahost、动态插件样例、panic allowlist、CI smoke fixture 和`linactl`测试期望中的导入或复制路径；已更新`proposal.md`、`design.md`、`plugin-package-boundary-governance`和`plugin-host-service-extension`，明确旧`pkg/plugindb`删除、目标路径、不可放入`internal`的原因和 data host service 授权边界。验证已执行：`cd apps/lina-core && go test ./pkg/plugin/capability/data/... -count=1`通过；`cd apps/lina-core && go test ./pkg/plugin/capability/... -count=1`通过；`cd apps/lina-core && go test ./internal/service/plugin/internal/datahost ./internal/service/plugin/internal/wasm -count=1`通过；`cd apps/lina-core && go test ./pkg/plugin/... -count=1`通过；`cd apps/lina-core && go test ./internal/cmd -run TestPanicAllowlist -count=1`通过；`cd apps/lina-plugins/linapro-demo-dynamic && GOWORK=off go test ./... -count=1`通过；`go run ./hack/tools/linactl wasm p=linapro-demo-dynamic out=/tmp/linapro-wasm-smoke-plugindb-move`通过；`go test ./hack/tools/linactl -count=1`通过；`openspec validate refactor-plugin-package-boundaries --strict`通过；`git diff --check -- apps/lina-core/pkg/plugin/capability/data apps/lina-core/internal/service/plugin/internal/datahost apps/lina-core/internal/cmd/cmd_panic_allowlist_test.go apps/lina-plugins/linapro-demo-dynamic/backend/internal/service/dynamic/dynamic.go hack/tools/linactl/main_test.go .github/workflows/reusable-plugin-command-smoke.yml .github/workflows/reusable-make-command-smoke.yml openspec/changes/refactor-plugin-package-boundaries`通过；静态检索确认生产 Go、动态插件样例、CI workflow 和`linactl`无旧`lina-core/pkg/plugindb`、`apps/lina-core/pkg/plugindb`或`./pkg/plugindb`路径残留，且宿主/插件/工具无非法 import `lina-core/pkg/plugin/capability/data/internal/**`。
- 2026-05-25 FB-2 根因分析：`pkg/plugin`迁移后存在命名残留：`capability`根组件主文件仍命名为`services.go`，`capability/contract`只使用`doc.go`作为组件说明入口，`capability/data`目录仍使用旧`package plugindb`和`plugindb_*.go`文件前缀，`capability/guest`公开接口仍命名为`Services`。这些残留会让目录职责、包名、主文件名和组件名不一致，违反公共组件主文件作为组件契约入口和组件命名清晰的治理要求。影响分析：本反馈只修改 Go 包名、文件名、导入别名、注释、测试名和 OpenSpec 设计/规范记录；不新增 HTTP API、SQL、前端 UI、插件 manifest 字段、运行时用户可见文案、缓存键、缓存失效策略或数据操作面。`i18n`资源无影响；数据权限语义保持既有 data host service 授权、资源表和方法校验；缓存一致性无影响；开发工具跨平台无新增脚本或工具入口；测试策略采用 Go 编译门禁、WASI 编译 smoke、静态检索、OpenSpec strict 校验和`git diff --check`。已读取规则文件：`AGENTS.md`、`.agents/rules/openspec.md`、`.agents/rules/documentation.md`、`.agents/rules/architecture.md`、`.agents/rules/plugin.md`、`.agents/rules/backend-go.md`、`.agents/rules/data-permission.md`、`.agents/rules/testing.md`、`.agents/rules/cache-consistency.md`、`.agents/rules/dev-tooling.md`和`.agents/rules/i18n.md`。
- 2026-05-25 FB-2 修复记录：已将`pkg/plugin/capability/services.go`重命名为`capability.go`，将`pkg/plugin/capability/contract/doc.go`重命名为`contract.go`，将`pkg/plugin/capability/data`改为`package data`并把主文件和实现文件统一为`data.go`、`data_*.go`，调用方使用`plugindata`导入别名避免与局部变量冲突；已将`capability/guest.Services`收敛为`guest.Directory`，并同步默认实例命名为`defaultDirectory`。同时更新`datahost`测试名和注释、动态插件样例 data SDK 引用、`pluginbridge`中旧 data SDK 说明，以及 OpenSpec 中曾要求保留旧`plugindb`包名的设计文本。验证已执行：`cd apps/lina-core && go test ./pkg/plugin/... -count=1`通过；`cd apps/lina-core && go test ./internal/service/plugin/internal/datahost -count=1`通过；`cd apps/lina-plugins/linapro-demo-dynamic && GOWORK=off go test ./... -count=1`通过；`cd apps/lina-core && GOOS=wasip1 GOARCH=wasm go test -c ./pkg/plugin/capability/data -o /tmp/plugin_capability_data_wasip1.test`通过；`openspec validate refactor-plugin-package-boundaries --strict`通过；`git diff --check -- apps/lina-core/pkg/plugin apps/lina-core/internal/service/plugin/internal/datahost apps/lina-plugins/linapro-demo-dynamic/backend/internal/service/dynamic openspec/changes/refactor-plugin-package-boundaries`通过。静态检索确认`apps/lina-core/pkg/plugin`下各 Go 包声明与目录名一致，`capability/data`无`package plugindb`和`plugindb_*.go`残留，`capability/guest`无`Services`公开接口残留；`contract`和`data/internal/{host,plan}`下少量非目录名前缀文件属于按契约类型或内部职责拆分，不是旧组件名残留。
- 2026-05-25 FB-3 根因分析：当前`capability/guest`已经作为动态插件侧能力目录存在，但其`Directory`仍直接返回`pluginbridge/guest`中的`RuntimeHostService`、`StorageHostService`、`ConfigHostService`等能力接口，具体 WASI client 和非 WASI stub 也仍放在`pluginbridge/guest`。这会让`pluginbridge/guest`同时承担 guest runtime、route dispatcher、raw host call transport 和宿主能力 SDK 语义，和本变更中“`pluginbridge`只负责动态插件 ABI、WASM transport 和协议 facade，`capability`负责插件消费宿主能力目录”的目标不一致。合理切口是将动态 guest 能力 client 的接口、默认实例、WASI 实现和 non-WASI stub 迁入`pkg/plugin/capability/guest`，让其通过`pluginbridge/guest`的 raw `InvokeHostService` transport 发起调用；`pluginbridge`根包可保留历史函数入口并转发到`capability/guest`，避免 bridge 根 facade 在本轮结构迁移中失效。影响分析：本反馈修改 Go 公共包边界、OpenSpec 规范和单元测试；不新增 HTTP API、SQL、前端 UI、插件 manifest 字段、运行时用户可见文案、缓存键或失效策略。`i18n`资源无影响；数据权限语义保持既有 dynamic `hostServices`授权、data host service 资源表和方法范围校验；缓存一致性无新增影响；开发工具跨平台影响限于后续`linactl wasm`和 Go 测试继续验证 WASM 构建；测试策略采用`go test ./pkg/plugin/...`、WASI 编译 smoke、动态插件样例测试、静态检索、`openspec validate`和`git diff --check`。已读取规则文件：`AGENTS.md`、`.agents/rules/openspec.md`、`.agents/rules/documentation.md`、`.agents/rules/architecture.md`、`.agents/rules/plugin.md`、`.agents/rules/backend-go.md`、`.agents/rules/data-permission.md`、`.agents/rules/testing.md`、`.agents/rules/cache-consistency.md`、`.agents/rules/dev-tooling.md`和`.agents/rules/i18n.md`。
- 2026-05-25 FB-3 修复记录：已将动态 guest 侧`RuntimeHostService`、`StorageHostService`、`HTTPHostService`、`DataHostService`、`CacheHostService`、`LockHostService`、`ConfigHostService`、`NotifyHostService`、`CronHostService`、`HostConfigHostService`和`ManifestHostService`等能力 client 接口、默认实例、WASI 实现和非 WASI stub 迁入`apps/lina-core/pkg/plugin/capability/guest`；`capability/guest`通过`guest_transport.go`复用`pluginbridge/guest`的 raw `InvokeHostService`，实现文件显式引用`pluginbridge/guest`协议 DTO、常量和 codec，避免把 bridge 导出常量或函数重新别名为 capability 包内导出符号。`apps/lina-core/pkg/plugin/pluginbridge/guest`保留 guest runtime、controller route dispatcher、request/response helper、协议 alias 和 raw host call transport，不再定义能力 client 接口或 stub。`pluginbridge`根包的`Runtime()`、`Data()`、`Cron()`等历史 facade 入口继续存在，但只转发到`capability/guest`。已同步`capability/data` WASI 执行路径、动态插件样例、单元测试和 OpenSpec 设计/增量规范，明确新代码应从`pkg/plugin/capability/guest`使用能力 client，`pluginbridge/guest`不得重新拥有宿主能力 SDK 语义。本反馈不修改 HTTP API、SQL、前端 UI、插件 manifest 字段、运行时用户可见文案、缓存键或失效策略；`i18n`资源无影响；数据权限仍由既有 dynamic `hostServices`授权快照、data host service 资源表和方法范围校验承担；开发工具跨平台影响通过`linactl` Go 工具测试和动态 WASM 构建 smoke 覆盖。
- 2026-05-25 FB-3 验证记录：已执行`cd apps/lina-core && go test ./pkg/plugin/pluginbridge/... ./pkg/plugin/capability/guest ./pkg/plugin/capability/data -count=1`通过；`cd apps/lina-core && GOOS=wasip1 GOARCH=wasm go test -c ./pkg/plugin/capability/guest -o /tmp/plugin_capability_guest_wasip1.test`通过；`cd apps/lina-core && GOOS=wasip1 GOARCH=wasm go test -c ./pkg/plugin/capability/data -o /tmp/plugin_capability_data_wasip1.test`通过；`cd apps/lina-core && go test ./pkg/plugin/... -count=1`通过；`cd apps/lina-core && go test ./internal/service/plugin/... ./internal/cmd -count=1`通过；`cd apps/lina-plugins/linapro-demo-dynamic && GOWORK=off go test ./... -count=1`通过；`go run ./hack/tools/linactl wasm p=linapro-demo-dynamic out=/tmp/linapro-wasm-smoke-fb3`通过并生成`/tmp/linapro-wasm-smoke-fb3/linapro-demo-dynamic.wasm`；`cd apps/lina-plugins && GOWORK=off go test lina-plugin-linapro-content-notice/backend/... lina-plugin-linapro-demo-source/backend/... lina-plugin-linapro-monitor-loginlog/backend/... lina-plugin-linapro-monitor-online/backend/... lina-plugin-linapro-monitor-operlog/backend/... lina-plugin-linapro-monitor-server/backend/... lina-plugin-linapro-ops-demo-guard/backend/... lina-plugin-linapro-org-core/backend/... lina-plugin-linapro-tenant-core/backend/... -count=1`通过；`cd hack/tools/linactl && go test ./... -count=1`通过；`openspec validate refactor-plugin-package-boundaries --strict`通过；`git diff --check -- apps/lina-core/pkg/plugin openspec/changes/refactor-plugin-package-boundaries apps/lina-plugins/linapro-demo-dynamic`通过。静态检索确认`apps/lina-core/pkg/plugin/pluginbridge/guest`无`RuntimeHostService`、`StorageHostService`、`DataHostService`等能力 client 类型残留，生产代码无`pluginbridge.Runtime()`、`pluginbridge.Data()`等根 facade 能力消费路径，`pluginbridge/guest`导入仅保留在`pluginbridge`根 facade、`capability/guest` raw transport adapter 和 panic allowlist 中。
- 2026-05-25 `lina-review`反馈级审查记录：审查范围来自`git status --short`、`git ls-files --others --exclude-standard`和 FB-3 变更文件，实际聚焦`apps/lina-core/pkg/plugin/capability/guest`、`apps/lina-core/pkg/plugin/pluginbridge/guest`、`pluginbridge`根 facade、`capability/data` WASI 执行路径、动态插件样例服务和本变更 OpenSpec 文档；工作区存在大量与 FB-3 无关的既有脏文件，审查未回滚也未评价这些无关改动。已读取`lina-review`技能、`AGENTS.md`、`.agents/rules/openspec.md`、`.agents/rules/documentation.md`、`.agents/rules/architecture.md`、`.agents/rules/plugin.md`、`.agents/rules/backend-go.md`、`.agents/rules/data-permission.md`、`.agents/rules/testing.md`、`.agents/rules/cache-consistency.md`、`.agents/rules/dev-tooling.md`和`.agents/rules/i18n.md`。审查发现`capability/guest/guest_transport.go`曾将 bridge 导出常量和 codec 函数重新定义为包内别名，违反后端规则中“禁止为已导入包的导出常量或变量创建包内别名”的要求；已修复为能力 client 实现文件显式使用`bridgeguest.*`协议符号，`guest_transport.go`只保留必要类型契约别名、capability 自有不可用错误和私有 transport wrapper。审查后验证已执行：`cd apps/lina-core && go test ./pkg/plugin/capability/guest ./pkg/plugin/pluginbridge/guest ./pkg/plugin/pluginbridge -count=1`通过；`cd apps/lina-core && GOOS=wasip1 GOARCH=wasm go test -c ./pkg/plugin/capability/guest -o /tmp/plugin_capability_guest_wasip1.test`通过；`cd apps/lina-core && go test ./pkg/plugin/... -count=1`通过；`cd apps/lina-core && GOOS=wasip1 GOARCH=wasm go test -c ./pkg/plugin/capability/data -o /tmp/plugin_capability_data_wasip1.test`通过；`cd apps/lina-plugins/linapro-demo-dynamic && GOWORK=off go test ./... -count=1`通过；`go run ./hack/tools/linactl wasm p=linapro-demo-dynamic out=/tmp/linapro-wasm-smoke-fb3-review`通过并生成 WASM；`cd hack/tools/linactl && go test ./... -count=1`通过；`openspec validate refactor-plugin-package-boundaries --strict`通过；`git diff --check -- apps/lina-core/pkg/plugin openspec/changes/refactor-plugin-package-boundaries apps/lina-plugins/linapro-demo-dynamic`通过；静态检索确认`capability/guest`无 bridge 常量或 codec 函数别名残留，`pluginbridge/guest`无能力 client 类型残留，生产代码无`pluginbridge.Runtime()`等根 facade 能力消费路径。审查结论：未发现 FB-3 阻塞问题；无 HTTP API、SQL、前端 UI、运行时文案、插件 manifest、缓存一致性或数据权限语义变更。
- 2026-05-25 FB-4 根因分析：`pkg/plugin/capability`迁移后根接口被命名为`Directory`，是为了强调能力目录语义；但在当前包名已经是`capability`的前提下，完整限定名`capability.Services`更直接表达“宿主基础能力集合”，也与接口中各方法返回`APIDocService`、`ConfigService`、`orgcap.Service`、`tenantcap.Service`等服务集合结构一致。`Directory`还容易与菜单目录、插件资源目录和`capability/guest.Directory`动态 guest client 目录语义混淆。已将根接口`capability.Directory`重命名为`capability.Services`，将`ScopedDirectoryFactory`重命名为`ScopedServicesFactory`，将`DirectoryForPlugin`重命名为`ServicesForPlugin`；`capability/guest.Directory`不在本次范围内，继续保留为动态插件 guest 侧 client directory。
- 2026-05-25 FB-4 影响分析：本反馈修改 Go 公共能力契约命名、源码插件/动态插件宿主适配调用点、测试断言、注释和 OpenSpec 文档；不改变能力方法集合、授权边界、DTO、HTTP API、SQL、前端 UI、插件 manifest 字段、运行时用户可见文案、缓存键或失效策略。`i18n`资源无影响；数据权限无新增数据操作面，`TenantFilter`仍只在源码插件专属`pluginhost.SourceCapabilities`中暴露；缓存一致性无新增实现影响，能力集合继续复用启动期共享实例；开发工具跨平台无新增脚本或工具入口；测试策略采用 Go 编译门禁、静态残留检索、OpenSpec 严格校验和格式检查。已读取规则文件：`AGENTS.md`、`.agents/rules/openspec.md`、`.agents/rules/documentation.md`、`.agents/rules/architecture.md`、`.agents/rules/plugin.md`、`.agents/rules/backend-go.md`和`.agents/rules/testing.md`。
- 2026-05-25 FB-4 验证记录：已运行`cd apps/lina-core && go test ./pkg/plugin/capability -count=1`通过；已运行`cd apps/lina-core && go test ./pkg/plugin/pluginhost -count=1`通过；已运行`openspec validate refactor-plugin-package-boundaries --strict`通过；已运行本次命名反馈相关文件的`git diff --check`通过；静态检索确认`capability.Directory`、`DirectoryForPlugin`和`ScopedDirectoryFactory`在生产 Go、官方插件和`linactl`中无残留，剩余`Directory`仅为`capability/guest.Directory`动态 guest SDK 接口和菜单目录业务枚举。补充运行`cd apps/lina-core && go test ./internal/service/pluginhostservices ./internal/service/plugin/internal/integration ./internal/service/plugin/internal/wasm -count=1`、`cd apps/lina-core && go test ./internal/service/plugin ./internal/cmd -count=1`时失败，阻断点为当前工作区既有未完成窄接口重构：`tenantcap.ScopeService.Enabled`、`orgcap.RuntimeService.Enabled`、`orgcap.ScopeService.Enabled`方法调用与对应接口定义不一致，以及`pkg/plugin/capability/tenantcap/tenantcap_host_impl.go`调用不存在的`capabilityregistry.Manager.ActiveProvider`；这些不在 FB-4 命名修改范围内。
- 2026-05-26 FB-5 根因分析：`pluginbridge/protocol`已经作为 bridge envelope、ABI 常量、host call、host service DTO 和 codec 的公开协议 owner，但`pluginbridge/guest/guest_types_aliases.go`仍把 request/response envelope、route snapshot、host call envelope、opcode、status 和 response/codec helper 重新导出为`guest`包符号；此前`capability/guest`中也存在同类协议别名。结果是动态插件样例、`linactl`生成 dispatcher 和 guest runtime 测试可以绕过`protocol`直接依赖`pluginbridge/guest`协议别名，形成多个公开协议出口，和“协议 DTO/常量/codec 由`pluginbridge/protocol`单一持有”的目标不一致。规则加载：已读取`lina-feedback`、`goframe-v2`、`karpathy-guidelines`技能说明；已按`AGENTS.md`读取`.agents/rules/openspec.md`、`.agents/rules/documentation.md`、`.agents/rules/architecture.md`、`.agents/rules/plugin.md`、`.agents/rules/backend-go.md`、`.agents/rules/data-permission.md`、`.agents/rules/testing.md`、`.agents/rules/cache-consistency.md`、`.agents/rules/dev-tooling.md`、`.agents/rules/i18n.md`和`.agents/rules/api-contract.md`。
- 2026-05-26 FB-5 修复记录：已删除`apps/lina-core/pkg/plugin/pluginbridge/guest/guest_types_aliases.go`，并将`pluginbridge/guest`的 runtime、route dispatcher、binding、context、helper、host-call transport 和测试改为在签名和实现中显式使用`protocol.*`；`pluginbridge/guest`继续只提供 guest runtime、controller dispatcher、request/response helper 和 raw host-call transport，不再导出协议 DTO、常量或 codec 别名。已同步`apps/lina-plugins/linapro-demo-dynamic`中的 host-build dispatcher、controller helper 和 error classifier，使 envelope 类型与 response builder 直接来自`protocol`；已同步`hack/tools/linactl/internal/wasmbuilder`生成的 WASM dispatcher，使生成代码使用`protocol.BridgeRequestEnvelopeV1`和`protocol.BridgeResponseEnvelopeV1`，同时去除构建器内部`RuntimeArtifactMetadata`和`LifecycleContract`的协议 type alias。已更新治理扫描`TestRepositoryPluginCapabilityBoundaries`，阻止`capability/guest`和`pluginbridge/guest`重新创建`protocol`导出别名，并阻止`pluginbridge`根包恢复协议 facade。规范审计中发现动态插件样例`backend/api/dynamic/v1/dynamic_lifecycle.go`仍保留`BeforeInstallReq`等 lifecycle 命名 DTO 别名；该文件用于让`linactl`从方法签名推导不同 lifecycle `requestType`，属于插件 API 层回调命名契约，不是公共 SDK 协议二次出口，本轮保留并记录原因。
- 2026-05-26 FB-5 影响分析：本反馈修改 Go 公共包导出面、动态插件样例、`linactl` Go 工具生成模板、治理测试和 OpenSpec 文档；不新增或修改 HTTP API 路由、DTO JSON 结构、SQL、前端 UI、插件 manifest 字段、运行时用户可见文案、缓存键、缓存失效策略或数据操作面。`i18n`无运行时文案、API 文档源文本、插件清单或语言包资源影响；数据权限无新增数据读写或可见性边界变化，既有 host service 授权和 data host service 边界保持不变；缓存一致性无新增缓存或失效路径；开发工具跨平台影响限于`linactl` Go 工具生成模板，未新增平台专属脚本；测试策略为 Go 编译门禁、WASI 编译 smoke、治理扫描、静态检索、OpenSpec strict 校验和格式检查，纯内部协议出口收敛不新增 E2E。
- 2026-05-26 FB-5 验证记录：已执行`cd apps/lina-core && go test ./pkg/plugin/capability -run TestRepositoryPluginCapabilityBoundaries -count=1`通过；`cd apps/lina-core && go test ./pkg/plugin/capability/guest ./pkg/plugin/capability/data ./pkg/plugin/pluginbridge/guest ./pkg/plugin/pluginbridge/protocol -count=1`通过；`cd apps/lina-plugins/linapro-demo-dynamic && GOWORK=off go test ./... -count=1`通过；`cd hack/tools/linactl && go test ./internal/wasmbuilder -count=1`通过；`cd apps/lina-core && GOOS=wasip1 GOARCH=wasm go test -c ./pkg/plugin/capability/guest -o /tmp/plugin_capability_guest_wasip1.test`通过；`cd apps/lina-core && GOOS=wasip1 GOARCH=wasm go test -c ./pkg/plugin/capability/data -o /tmp/plugin_capability_data_wasip1.test`通过；`openspec validate refactor-plugin-package-boundaries --strict`通过；`git diff --check -- apps/lina-core/pkg/plugin apps/lina-plugins/linapro-demo-dynamic hack/tools/linactl openspec/changes/refactor-plugin-package-boundaries`通过。静态检索确认`apps/lina-core/pkg/plugin/pluginbridge/guest/guest_types_aliases.go`已删除，`bridgeguest.BridgeRequestEnvelopeV1`、`bridgeguest.BridgeResponseEnvelopeV1`、`bridgeguest.NewBadRequestResponse`等协议别名消费无残留，`apps/lina-core/pkg/plugin`、`apps/lina-plugins`和`hack/tools/linactl`中除动态样例 lifecycle 回调命名 DTO 外无`type ... = protocol.*`同类重复别名。
- 2026-05-26 FB-5 `lina-review`反馈级审查记录：审查范围聚焦`apps/lina-core/pkg/plugin/pluginbridge/guest`、`apps/lina-core/pkg/plugin/capability/capability_boundary_governance_test.go`、`apps/lina-plugins/linapro-demo-dynamic`、`hack/tools/linactl/internal/wasmbuilder`和本变更 OpenSpec 文档。已读取`lina-review`、`lina-feedback`、`goframe-v2`、`karpathy-guidelines`、`AGENTS.md`、`.agents/rules/openspec.md`、`.agents/rules/documentation.md`、`.agents/rules/architecture.md`、`.agents/rules/plugin.md`、`.agents/rules/backend-go.md`、`.agents/rules/data-permission.md`、`.agents/rules/testing.md`、`.agents/rules/cache-consistency.md`、`.agents/rules/dev-tooling.md`、`.agents/rules/i18n.md`和`.agents/rules/api-contract.md`。审查确认`pluginbridge/protocol`为单一公开协议出口，`pluginbridge`根包未恢复 facade，`pluginbridge/guest`和`capability/guest`未重新导出协议 DTO、常量或 codec 别名，`linactl`生成 dispatcher 使用`protocol` envelope 且继续只用`bridgeguest` runtime/helper；动态样例 lifecycle 命名 DTO 别名已作为 requestType 推导契约记录豁免。审查未发现阻塞问题；无 HTTP API、SQL、前端 UI、运行时文案、插件 manifest、缓存一致性或数据权限语义变更。验证证据同 FB-5 验证记录，另执行`openspec status --change refactor-plugin-package-boundaries --json`确认变更产物状态为完成。
