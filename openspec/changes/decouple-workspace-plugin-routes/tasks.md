## 1. 配置与契约准备

- [x] 1.1 将默认管理工作台入口路径确定为 `/admin`，并在配置设计中明确工作台入口可按部署边界配置为 `/` 或其他非保留路径。
- [x] 1.2 在后端配置结构、默认配置、配置模板和配置元数据中新增工作台入口路径配置及校验规则。
- [x] 1.3 更新 public frontend 配置投影或等价前端启动配置，使前端可以读取管理工作台入口路径；同时明确工作台 `basePath` 以启动期配置为权威，不作为运行期热修改配置。
- [x] 1.4 明确本变更不新增插件 HTTP 路由 manifest 声明，不新增 `public_routes`、`portal_routes` 或工作台 API 路由分组字段；同时在 `plugin.yaml` 增加可选 `public_assets` 严格 schema 声明边界，用于源码插件和动态插件通过 `/x-assets/{plugin-id}/{version}/...` 托管可公开静态资源，且不保留 `/plugin-assets` 兼容入口。
- [x] 1.5 明确插件 API base 契约：宿主控制面 API 使用 `/api/v1`，源码插件和动态插件 API 统一使用 `/x/{plugin-id}/api/v1`；`/x` 表示统一插件 API 命名空间，不再表示动态插件专属数据面，且 `/x/**` 不允许承载插件非 API 公开路由。
- [x] 1.6 记录 i18n 影响判断；若新增用户可见配置项、接口文档或页面文案，则同步运行时 i18n 与 apidoc i18n 资源。
- [x] 1.7 记录缓存一致性影响判断；确认本变更不新增业务缓存，且工作台 fallback 与源码插件路由注册不依赖跨节点可变状态；public asset URL 以插件版本为缓存边界，同一版本资源内容必须不可变。

## 2. 后端路由边界

- [x] 2.1 调整后端前端静态资源处理器，使默认管理工作台静态资源与 SPA fallback 仅匹配配置后的工作台入口路径。
- [x] 2.2 保护宿主已注册控制面 API 路由、统一插件 API 命名空间 `/x`、统一插件资产命名空间 `/x-assets` 和工作台入口路径；源码插件 API 必须注册到 `/x/{plugin-id}/api/v1`，源码插件公开页面、门户、静态资源或自管 fallback 仍可注册其他非保留路径但不得注册到 `/x`。
- [x] 2.3 保持源码插件 HTTP registrar 代码注册模型，不要求插件在 `plugin.yaml` 中声明 HTTP 路由或路由分组语义；新增 `APIPrefix()` 或等价方法返回 `/x/{plugin-id}/api/v1`。
- [x] 2.4 增加后端测试覆盖：工作台 basePath 可访问、非工作台根路径不返回管理台 SPA、源码插件根路径可注册并命中插件 handler。
- [x] 2.5 增加后端测试覆盖：源码插件与宿主保留路径或其他插件发生 GoFrame 路由冲突时启动或注册失败。
- [x] 2.6 增加插件 public asset resolver 和校验测试，覆盖源码插件 embedded FS 与动态插件 artifact 只暴露 `plugin.yaml public_assets` 声明的资源，动态插件既有 frontend assets 迁移到 `/x-assets/{plugin-id}/{version}/...` 声明式模型后才能访问，非法 schema 阻止插件校验/安装/启用，插件禁用或租户不可用时返回 404，声明路径不得通过 `../`、绝对路径、URL 或符号链接逃逸插件自身资源边界，旧 `/plugin-assets` 入口不可访问。
- [x] 2.7 调整动态插件 route contract 示例、OpenAPI 投影和路由分发测试，使动态插件 API 公开路径使用 `/x/{plugin-id}/api/v1/...`，并确认动态插件 `/x` 分发不会吞掉源码插件 `/x/{source-plugin-id}/api/v1/...` 具体路由。
- [x] 2.8 若修改 Go 生产代码，运行覆盖变更包的 `go test`，并对涉及启动路由绑定的变更运行 `cd apps/lina-core && go test ./internal/cmd -count=1` 或更窄等价测试。

## 3. 前端工作台入口

- [x] 3.1 调整管理工作台 Vue Router base、登录跳转、刷新路径和默认首页解析，使其使用配置后的工作台入口路径。
- [x] 3.2 调整 public frontend 配置同步逻辑，确保在根路径或非根路径工作台入口下仍能正确请求 `/api/v1/config/public/frontend`。
- [x] 3.3 增加或调整插件前端 API base helper，区分宿主 `/api/v1` 和统一插件 API `/x/{plugin-id}/api/v1`。
- [x] 3.4 调整开发代理和构建配置，使本地开发以后端公开地址作为统一入口，并由后端在工作台入口路径下代理或转发到 Vite；同时不吞掉后端源码插件根路由或 `/x/*` 统一插件 API 请求。
- [x] 3.5 更新前端单元测试，覆盖工作台入口路径规范化、登录重定向、插件 API base helper 和刷新行为。
- [x] 3.6 运行相关前端单元测试和类型检查，确认工作台入口路径变更不破坏现有管理台页面。

## 4. 插件与菜单边界

- [x] 4.1 确认现有源码插件 `plugin.yaml` 的 `menus` 仍作为工作台菜单与权限事实来源，不新增 HTTP 路由声明字段。
- [x] 4.2 增加或调整源码插件测试夹具，验证插件可通过代码注册公开 HTTP 路由并自行返回内容，同时可通过 `APIPrefix()` 注册 `/x/{plugin-id}/api/v1/...` API。
- [x] 4.3 验证插件 HTTP 路由不会自动生成菜单、权限节点、OpenAPI 文档或插件资源引用。
- [x] 4.4 验证工作台菜单声明不会让主框架推断同路径 HTTP handler 存在。
- [x] 4.5 为源码插件和动态插件补充 `plugin.yaml public_assets` 声明示例或夹具，验证声明式托管不影响插件自行注册 HTTP 静态资源路由。
- [x] 4.6 迁移官方源码插件和动态插件示例的 API 路径文档、前端调用与测试夹具，源码插件和动态插件示例均使用 `/x/{plugin-id}/api/v1/...`。
- [x] 4.7 迁移动态插件 hosted frontend 菜单，继续使用 `system/plugin/dynamic-page` 或等价工作台承载组件，并通过菜单 query 或等价参数引用 `/x-assets/{plugin-id}/{version}/...`；不得把 `/x-assets/.../index.html` 直接作为管理工作台菜单路由替代动态页组件。

## 5. E2E 与文档

- [x] 5.1 按 lina-e2e 规范新增或更新 E2E，用配置后的工作台入口访问登录页和管理台页面。
- [x] 5.2 新增或更新 E2E，验证源码插件公开根路由或非保留公开路径不被管理台 SPA fallback 吞掉，并验证源码插件统一插件 API 路径 `/x/{plugin-id}/api/v1/...` 可用。
- [x] 5.3 新增或更新 E2E，验证动态插件 API 路径 `/x/{plugin-id}/api/v1/...` 可用，旧式 `/x/{plugin-id}/{resource}` 示例和断言已迁移。
- [x] 5.4 更新 README 或开发文档中的默认管理工作台访问路径；若新增目录说明文档，同步英文 `README.md` 与中文 `README.zh-CN.md`。
- [x] 5.5 更新插件开发文档，说明源码插件 HTTP 路由是插件实现细节，源码插件和动态插件 API 均必须使用 `/x/{plugin-id}/api/v1/...`，`/x` 下不得注册非 API 路由，工作台权限入口仍通过 `plugin.yaml` 菜单声明，并说明 `plugin.yaml public_assets` 声明只用于 `/x-assets/{plugin-id}/{version}/...` 托管可公开静态资源，动态插件既有 frontend assets 访问路径也需迁移到该声明式模型并继续由动态页组件承载，且不提供 `/plugin-assets` 兼容路径。

## 6. 验证与审查

- [x] 6.1 运行 `openspec validate decouple-workspace-plugin-routes --strict`。
- [x] 6.2 运行后端路由绑定、配置校验、插件注册、`APIPrefix()`、源码插件 `/x/{plugin-id}/api/v1` 和动态插件 `/x/{plugin-id}/api/v1` 相关 Go 测试。
- [x] 6.3 运行前端工作台入口、插件 API base helper 相关单元测试和必要的 E2E 回归。
- [x] 6.4 运行静态检查，确认未新增将源码插件 HTTP 路由写入 `plugin.yaml` 的治理字段或文档要求，确认 `public_assets` 声明文档以插件自身资源边界和路径安全校验为准，确认文档不再使用旧源码插件 API 前缀，确认动态插件示例不再使用旧式 `/x/{plugin-id}/{resource}` API 路径，确认 `/x` 下非 API 路由未被作为允许示例，并确认 public asset 文档不再把 `/plugin-assets` 作为可用入口。
- [x] 6.5 执行 `/lina-review` 审查，重点检查主框架边界、插件 API base、工作台菜单治理、i18n 判断、缓存一致性判断、Go 编译门禁和 E2E 覆盖。

## Feedback

- [x] **FB-1**: 需求澄清为全新项目且不考虑根路径管理工作台兼容性，更新提案、设计、增量规范和任务清单，移除 `/` 兼容与回滚表述。
- [x] **FB-2**: 需求澄清为源码插件和动态插件均可声明 public assets，由宿主通过声明式资产入口托管可公开静态资源；保留源码插件 HTTP 路由代码注册事实来源，并明确 SQL、manifest、i18n、apidoc 与后端资源不得被公开。
- [x] **FB-3**: 需求澄清为源码插件公开页面、门户、静态资源和自管 fallback 可继续自由注册非保留 HTTP 路由；public assets 声明放在 `plugin.yaml public_assets`，并明确动态插件既有 frontend assets 访问路径迁移到该声明式模型。
- [x] **FB-4**: 需求澄清为源码插件 API 前缀需要从建议性描述升级为规范性契约；源码插件仍保留非 API 公开 HTTP 路由的自由注册能力。
- [x] **FB-5**: 需求澄清为不考虑兼容性，源码插件和动态插件 API 统一使用 `/x/{plugin-id}/api/v1` 插件 API 命名空间；`/x` 不再作为动态插件专属含义，源码插件非 API 公开 HTTP 路由仍保留自由注册能力。
- [x] **FB-6**: 需求澄清为不考虑 `/plugin-assets` 兼容入口，插件 public assets 统一改为 `/x-assets/{plugin-id}/{version}/...`，保留插件版本作为静态资源缓存和升级隔离边界。
- [x] **FB-7**: 根据需求澄清完善官网能力边界文档，说明 `/admin` 工作台入口、统一插件 API 命名空间 `/x/{plugin-id}/api/v1`、`public_assets` 与 `/x-assets/{plugin-id}/{version}/...` 的边界，并同步英文镜像。
- [x] **FB-8**: 需求澄清为开发模式以后端公开地址作为统一入口并由后端代理 Vite；工作台 `basePath` 为启动期权威配置；`/x/**` 只承载插件 API；源码插件 OpenAPI 只投影插件 API；`public_assets` 使用严格 schema、默认匿名可读但受插件启用与租户可用性约束；动态插件页面继续通过 `system/plugin/dynamic-page` 引用 `/x-assets` 资源。
- [x] **FB-9**: 需求澄清为 `plugin.yaml` 静态资源声明字段必须使用 `public_assets`，替代 camelCase 旧写法，以保持配置项命名规则一致。
- [x] **FB-10**: 根据需求澄清改进官网能力边界文档中“门户路由与管理路由的区分”示例代码，明确公开门户入口使用非保留路径，插件 API 使用 `/x/{plugin-id}/api/v1`，工作台入口仍由 `plugin.yaml` 的 `menus` 管理，并同步英文镜像。
- [x] **FB-11**: 将 `public_assets` 目录默认文件从硬编码 `index.html` 调整为每个声明项的可选 `index` 配置；未配置时仍默认 `index.html`，并校验 `index` 为安全相对文件名。
- [x] **FB-12**: 将插件宿主路由、托管资产和动态页菜单的重复硬编码契约收敛到 `pkg/pluginhost` 主文件维护，避免 controller、cmd 和插件前端服务重复定义同一宿主插件边界。
- [x] **FB-13**: 需求澄清为 `public_assets` 完全信任插件作者对插件内资源的显式发布声明；宿主只做路径安全、声明存在性、声明目录读取和插件资源边界校验，不再维护治理目录黑名单或前端目录白名单。
- [x] **FB-14**: 参考 `linapro-content-notice` 源码插件的路由注册编码风格，统一其他官方源码插件的路由注册代码结构。
- [x] **FB-15**: 修复已有根通配路由 `/*` 存在时工作台 `/admin` 静态资源 fallback 被抢占并返回 404 的单元测试失败。
- [x] **FB-16**: 允许独立管理后台域名部署将 `workspace.basePath` 配置为根路径 `/`，同时保留宿主 API、插件 API 与插件资产保留命名空间不被工作台 fallback 吞掉。
- [x] **FB-17**: 修复 `make test.go` 在 `lina-core/internal/service/role` 编译失败，原因是测试替身未实现新增的工作台入口配置读取接口。
- [x] **FB-18**: 修复管理后台 LinaPro logo 在配置后的工作台入口下无法展示的问题。
- [x] **FB-19**: 修复管理后台接口文档页面在配置后的工作台入口下打开为空白页面的问题。
- [x] **FB-20**: 修复语言切换后顶部面包屑导航未按当前语言环境重新本地化的问题。
- [x] **FB-21**: 修复 `TestProductionPanicsMatchAllowlist` 中 `linapro-ops-demo-guard` 源码插件注册失败退出 allowlist 计数滞后的单元测试失败。
- [x] **FB-22**: 修复 nightly `plugin-full / extension-plugin` 与 `plugin-full / plugins-5-of-5` E2E 分片失败。
- [x] **FB-23**: 修复 nightly `host-only` 与 `plugin-full / plugins-2-of-5`、`plugins-3-of-5`、`plugins-4-of-5` E2E 分片失败。

- [x] **FB-24**: 将动态 WASM 插件运行时分发从样例插件手写大文件改为 `linactl wasm` 构建期生成 `wasip1` 零反射 dispatcher，移除 `linapro-demo-dynamic` 临时手写分发器。
- [x] **FB-25**: 修复 Main CI 中 `host-only` 与 `plugin-full` 前端单测失败，原因是测试环境缺省 `VITE_GLOB_API_URL` 时插件 API helper 顶层初始化读取 `undefined.trim()`。
- [x] **FB-26**: 修复 nightly `plugin-full / plugins-5-of-5` E2E 中 `linapro-tenant-core` 卸载前置条件弹窗用例依赖测试顺序导致租户创建接口失败。
- [x] **FB-27**: 修复 nightly `plugin-full / plugins-1-of-5` E2E 中 `linapro-content-notice` 仍调用宿主旧通知接口路径、`linapro-demo-dynamic` 仍断言旧 `/plugin-assets` 资产路径的问题。
- [x] **FB-28**: 将源码插件示例和动态插件示例安装后的工作台菜单归入扩展中心目录，并排在插件管理菜单后面。
- [x] **FB-29**: 修复 nightly `plugin-full / plugins-1-of-5` 与 `plugins-2-of-5` E2E 中示例插件菜单已迁移到扩展中心后，旧测试仍按顶层菜单定位导致插件页面不可见的问题。

## Verification Notes

- `cd apps/lina-core && go test ./internal/service/plugin -count=1` 当前受本地数据库缺少 `plugin_linapro_tenant_core_user_membership` 表阻断；已运行变更相关定向测试和子包测试覆盖插件路由、public assets、动态插件运行时升级回滚与启动路由绑定。
- `pnpm -C hack/tests test:validate` 当前受多个既有官方源码插件四位 TC 文件名阻断；本次新增/调整的 `linapro-demo-source` 插件 E2E 已按 `TC001`/`TC002` 命名并串行运行通过。
- FB-11 已验证：`cd apps/lina-core && go test ./internal/service/plugin/internal/catalog ./internal/service/plugin/internal/frontend ./internal/service/plugin/internal/runtime -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`、`cd hack/tools/linactl && go test ./internal/wasmbuilder -count=1`、`openspec validate decouple-workspace-plugin-routes --strict`、`git diff --check`、`git -C apps/lina-plugins diff --check`、`rg -n "publicAssets" openspec apps hack -g '!node_modules' -g '!dist' -g '!*.map'` 均通过或无命中。
- FB-12 为项目治理类反馈，不新增用户可见文案、接口语义、缓存或运行时行为；i18n 与缓存资源无需变更。已验证：`cd apps/lina-core && go test ./pkg/pluginhost ./internal/controller/user ./internal/controller/menu ./internal/service/plugin/internal/frontend ./internal/service/plugin/internal/runtime ./internal/service/plugin/internal/openapi ./internal/service/apidoc ./internal/cmd -count=1`、`openspec validate decouple-workspace-plugin-routes --strict`、`git diff --check -- apps/lina-core/pkg/pluginhost/pluginhost.go apps/lina-core/internal/controller/user/user_v1_get_info.go apps/lina-core/internal/controller/user/user_v1_get_info_test.go apps/lina-core/internal/controller/menu/menu_v1_all.go apps/lina-core/internal/service/plugin/internal/frontend/contract.go apps/lina-core/internal/service/plugin/internal/frontend/contract_test.go apps/lina-core/internal/service/plugin/internal/frontend/frontend_runtime.go apps/lina-core/internal/cmd/cmd_http_frontend.go apps/lina-core/pkg/pluginhost/pluginhost_route_registrar.go apps/lina-core/internal/service/plugin/internal/runtime/route.go apps/lina-core/internal/service/plugin/internal/runtime/route_dispatch_test.go apps/lina-core/internal/service/plugin/internal/runtime/route_test.go apps/lina-core/internal/service/plugin/internal/openapi/openapi.go apps/lina-core/internal/service/plugin/internal/openapi/openapi_paths.go apps/lina-core/internal/service/apidoc/apidoc_i18n.go openspec/changes/decouple-workspace-plugin-routes/tasks.md`。
- FB-13 不新增用户可见运行时文案、接口字段、数据访问接口或业务缓存；i18n、数据权限和缓存资源无需变更。已验证：`cd apps/lina-core && go test ./pkg/pluginfs ./internal/service/plugin/internal/catalog ./internal/service/plugin/internal/frontend -count=1`、`openspec validate decouple-workspace-plugin-routes --strict`、`git diff --check -- apps/lina-core/pkg/pluginfs/pluginfs.go apps/lina-core/internal/service/plugin/internal/catalog/manifest_validate.go apps/lina-core/internal/service/plugin/internal/catalog/catalog_test.go apps/lina-core/internal/service/plugin/internal/frontend/frontend_runtime.go apps/lina-core/internal/service/plugin/internal/frontend/public_asset_test.go openspec/changes/decouple-workspace-plugin-routes/proposal.md openspec/changes/decouple-workspace-plugin-routes/design.md openspec/changes/decouple-workspace-plugin-routes/specs/plugin-ui-integration/spec.md openspec/changes/decouple-workspace-plugin-routes/tasks.md apps/lina-plugins/README.md apps/lina-plugins/README.zh-CN.md`。
- FB-14 为源码插件路由注册代码风格治理，未新增用户可见文案、接口字段、数据操作接口、业务缓存、开发工具或脚本；i18n、数据权限和缓存资源无需变更。已验证：使用临时 `go.work` 包含 `apps/lina-core` 与相关源码插件后运行 `GOWORK="$tmpdir/go.work" go test lina-plugin-linapro-content-notice/backend lina-plugin-linapro-demo-source/backend lina-plugin-linapro-monitor-loginlog/backend lina-plugin-linapro-monitor-online/backend lina-plugin-linapro-monitor-operlog/backend lina-plugin-linapro-monitor-server/backend lina-plugin-linapro-org-core/backend lina-plugin-linapro-tenant-core/backend -run '^$' -count=1`、`cd apps/lina-plugins && GOWORK=off go test ./... -run '^$' -count=1`、`cd apps/lina-core && go test ./pkg/pluginhost -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`、`openspec validate decouple-workspace-plugin-routes --strict`。
- FB-14 `/lina-review`：审查范围限定为官方源码插件 `backend/plugin.go` 路由注册结构与本任务记录；未发现阻塞问题。
- FB-15 为后端工作台与插件公开资产静态路由绑定修复，不新增用户可见文案、接口字段、数据操作接口、业务缓存、开发工具或脚本；i18n、数据权限和缓存资源无需变更。已验证：`cd apps/lina-core && go test ./internal/cmd -run '^(TestFrontendAssetFallbackIsScopedToWorkspaceBasePath|TestFrontendAssetFallbackClaimsHostedPluginAssetNamespace)$' -count=1 -v`、`cd apps/lina-core && go test ./internal/cmd -count=1`、`openspec validate decouple-workspace-plugin-routes --strict`、`git diff --check -- apps/lina-core/internal/cmd/cmd_http_frontend.go apps/lina-core/internal/cmd/cmd_http_test.go openspec/changes/decouple-workspace-plugin-routes/tasks.md`。
- FB-16 为工作台入口路径配置与 fallback 行为修复；新增/更新了后端配置校验、后端静态资源 fallback、前端运行时 basePath 规范化、E2E workspace path helper、README 与 OpenSpec 文档，并补充根路径工作台不覆盖 `/api/**`、`/x/**`、`/x-assets/**`、后置绑定 `/api.json` 仍可访问，以及已有 `/` 路由会与根路径工作台显式冲突的回归测试。用户可见接口文档文本涉及 `FrontendWorkspaceRes.basePath`，已同步源资源 `manifest/i18n/zh-CN/apidoc/core-api-publicconfig.json`；`internal/packed/manifest/**` 为忽略的生成镜像，不纳入本次提交范围。不新增数据操作接口、业务缓存、开发工具或脚本，数据权限与缓存资源无需变更。工作台 fallback 单测使用自包含内存前端资源，不依赖未跟踪的 `internal/packed/public/index.html` 生成产物；插件资产路由注册由 `/x-assets//*any` 规范化为 `/x-assets/*any`；SPA fallback 改为直接读取 `index.html`，避免 `http.FileServer` 在根路径工作台刷新场景触发 `index.html` 重定向。已验证：`cd apps/lina-core && go test ./internal/service/config -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`、`pnpm -C apps/lina-vben exec vitest run apps/web-antd/src/runtime/public-frontend.test.ts --dom`、`openspec validate decouple-workspace-plugin-routes --strict`、`git diff --check`、残留文本扫描确认非根/拒绝根路径表述已清理。
- FB-17 为后端测试替身接口同步修复，不修改生产代码、用户可见文案、REST API、数据操作接口、业务缓存、开发工具或脚本；i18n、数据权限和缓存资源无需变更。已验证：`cd apps/lina-core && go test -race ./internal/service/role -count=1`。
- FB-17 `/lina-review`：审查范围为 `role_access_cache_test.go` 测试替身接口同步和本任务记录；未发现阻塞问题。该修复仅补齐测试 fake 的 `WorkspaceConfigReader` 方法，未修改生产代码、API、i18n、缓存或数据权限边界。
- FB-18/FB-19 为工作台入口路径下的前端静态资源与接口文档 iframe 加载修复。前端运行时新增 `resolveWorkspaceAssetURL`，将 logo、默认头像、工作台卡片图标和接口文档 iframe 的工作台静态资源统一解析到配置后的 `workspace.basePath` 下，同时保留 `/api/**`、`/api.json`、`/x/**`、`/x-assets/**` 等宿主/插件保留命名空间不被改写。Stoplight HTML 改用相对 CSS/JS 路径，并以当前 origin 加载 `/api.json`；Vite 开发态按当前 `VITE_BASE` 直接返回 Stoplight HTML，避免 iframe 文档请求因 `Sec-Fetch-Dest: iframe` 被 Vite SPA fallback 改写成空白/404 页面。该修复不新增或修改用户可见文案、翻译键、REST API、后端数据操作接口、业务缓存或开发工具脚本，i18n、数据权限和缓存资源无需变更。已验证：`cd apps/lina-core && go test ./internal/cmd -count=1`、`pnpm -C apps/lina-vben exec vitest run apps/web-antd/src/runtime/public-frontend.test.ts --dom`、`pnpm -C apps/lina-vben --filter @lina/web-antd typecheck`、`make dev`、带 `Sec-Fetch-Dest: iframe` 请求头的 `/admin/stoplight/apidocs.html` HTTP 探针、`pnpm -C hack/tests exec playwright test hack/tests/e2e/auth/TC007-brand-logo-assets.ts hack/tests/e2e/about/TC001-api-docs-page.ts --workers=1`。
- FB-18/FB-19 `/lina-review`：审查范围为前端公共配置资源路径解析、接口文档 iframe、Stoplight 静态 HTML、Vite 开发态 Stoplight HTML bypass、工作台卡片图标、相关 E2E/单元测试和本任务记录；未发现阻塞问题。实现未修改 Go 生产代码、REST API、数据库 schema、后端数据权限、运行时缓存、manifest i18n、apidoc i18n 或长期维护开发脚本；Go 编译门禁已通过 `cd apps/lina-core && go test ./internal/cmd -count=1` 覆盖本次 Go 测试变更和路由绑定回归。
- FB-20 为前端共享布局 i18n 行为修复，面包屑标题现在显式依赖当前 `vue-i18n` locale，并优先使用后端菜单路由提供的 `meta.i18nKey` 重新本地化，避免语言切换后继续显示旧语言的 `meta.title` 缓存值。已新增 `hack/tests/e2e/i18n/TC015-breadcrumb-language-switch.ts` 覆盖中文、英文、中文三段切换；不新增或修改翻译资源、REST API、后端数据操作接口、业务缓存、开发工具或脚本，数据权限与缓存资源无需变更。已验证：`E2E_BROWSER_CHANNEL=chrome pnpm -C hack/tests exec playwright test e2e/i18n/TC015-breadcrumb-language-switch.ts --config playwright.config.ts --project=chromium --workers=1`、`pnpm -C apps/lina-vben -F @lina/web-antd typecheck`、`openspec validate decouple-workspace-plugin-routes --strict`、`git diff --check -- apps/lina-vben/packages/effects/layouts/src/widgets/breadcrumb.vue hack/tests/pages/MainLayout.ts hack/tests/e2e/i18n/TC015-breadcrumb-language-switch.ts openspec/changes/decouple-workspace-plugin-routes/tasks.md`。`pnpm -C hack/tests test:validate` 当前仍受既有源码插件四位 TC 文件名阻断；`pnpm -C hack/tests exec tsc -p tsconfig.json --noEmit --pretty false` 当前仍受既有 `@host-tests/pages/MultiTenantPage` 缺失阻断，均非本次新增测试引入。
- FB-24 为动态 WASM 插件运行期分发治理与构建工具修复：删除 `linapro-demo-dynamic` 样例插件中的临时手写大文件 dispatcher，将 `HandleRequest` 改为 `linactl wasm` 在 `wasip1` 构建期间临时生成 `zz_generated_wasm_dispatcher.go`，生成代码按 `RequestType` 与 `InternalPath` 零反射分发到 typed controller，并通过 `sync.Once` 在首次请求时惰性构造 controller，避免 WASM 包初始化阶段执行业务构造逻辑；生成文件在 `go build` 后删除，普通 Go 测试保留 `!wasip1` fallback dispatcher。该修复不新增用户可见文案、菜单、路由展示文本、REST API 字段或翻译资源，i18n 资源无需变更；不新增宿主数据操作接口或扩大数据权限面，数据权限无需变更；不新增运行时缓存或跨实例状态，缓存一致性无需变更；涉及开发工具 `linactl wasm`，已通过 Go 包测试、真实 WASM 构建和临时生成文件清理验证。已验证：`cd apps/lina-core && go test ./pkg/pluginbridge -count=1`、`go test ./hack/tools/linactl/internal/wasmbuilder -count=1`、`GOWORK=/Users/john/Workspace/github/linaproai/linapro/temp/go.work.plugins go test ./backend ./backend/internal/controller/dynamic -count=1`（工作目录为 `apps/lina-plugins/linapro-demo-dynamic`）、`make wasm p=linapro-demo-dynamic out=../../temp/output`、`test ! -e apps/lina-plugins/linapro-demo-dynamic/backend/zz_generated_wasm_dispatcher.go`、`openspec validate decouple-workspace-plugin-routes --strict`、`git diff --check`、`git -C apps/lina-plugins diff --check`。
- FB-24 `/lina-review`：审查范围为 `pluginbridge` 公开 guest helper、`linactl` WASM dispatcher 生成器、动态样例插件 `wasip1` 入口/fallback、生命周期回调上下文恢复、生成器单元测试和本任务记录；未发现阻塞问题。确认生成文件不提交到源码树、运行时 WASM 路径不依赖反射 dispatcher、不在包级变量初始化阶段构造业务 controller，Go 编译门禁、真实 WASM 构建、OpenSpec 和静态 diff 校验均已通过。
- FB-20 `/lina-review`：审查范围为共享面包屑组件、宿主 MainLayout E2E POM、新增 i18n 面包屑语言切换用例和本任务记录；未发现阻塞问题。实现优先使用 `meta.i18nKey` 且在缺少翻译键时回退到 `title/name`，与菜单和标签页的本地化容错一致。变更不涉及 Go 生产代码、REST API、数据库 schema、后端数据权限、运行时缓存、manifest i18n、apidoc i18n 或开发工具脚本；Go 编译门禁不适用。
- FB-21 为后端 panic 治理测试基线修复；`linapro-ops-demo-guard` 源码插件 `init` 现在包含 `BeforeInstall`、HTTP route callback 和 source plugin registration 三个静态注册失败退出点，均位于顶层源码插件静态注册入口并显式处理 error 后 fail-fast。该修复只同步 `TestProductionPanicsMatchAllowlist` allowlist 计数，不新增运行时行为、用户可见文案、REST API、数据操作接口、业务缓存、i18n 资源、开发工具或脚本，数据权限、缓存一致性和 E2E 均不适用。已验证：`cd apps/lina-core && go test ./internal/cmd -run TestProductionPanicsMatchAllowlist -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`、`openspec validate decouple-workspace-plugin-routes --strict`、`git diff --check -- apps/lina-core/internal/cmd/cmd_panic_allowlist_test.go openspec/changes/decouple-workspace-plugin-routes/tasks.md`。
- FB-21 `/lina-review`：审查范围为 `apps/lina-core/internal/cmd/cmd_panic_allowlist_test.go` 和本任务记录；未发现阻塞问题。确认新增计数对应既有 `plugin-registration` 允许类别，保留原因仍准确描述源码插件顶层静态注册入口在错误返回后选择中止启动；未修改生产 Go 代码。
- FB-22 修复了两个 nightly plugin-full 分片失败根因：`linapro-ops-demo-guard` 全局写保护改用插件治理权威启用状态，避免 stale process-local route snapshot 在插件已禁用/卸载后继续拦截写请求；动态插件 host-service E2E 去除 `/x//api/v1` 空插件 ID 路径；`linapro-tenant-core` 的 E2E helper、插件前端页面客户端和宿主租户客户端改为通过 `/x/linapro-tenant-core/api/v1/...` 调用插件自有 `platform/tenants`、`auth/select-tenant`、`auth/switch-tenant`、`auth/login-tenants`、`tenant/plugins` 接口，宿主登录、菜单、用户、角色、插件治理控制面仍保留 `/api/v1`。本次不新增用户可见文案、翻译键、接口字段或 apidoc 文本，i18n 资源无需变更；不新增数据操作语义，tenant-core 路径迁移复用既有插件端点权限、租户上下文和数据权限边界；缓存影响限定为新增权威插件状态读取路径，普通菜单/路由高频判断继续使用既有 snapshot，demo guard 这类全局中间件控制使用持久化治理状态以避免单机/集群环境 stale local snapshot 造成写保护误拦截，不新增缓存键或跨实例失效机制。已验证：`cd apps/lina-core && go test ./pkg/pluginservice/pluginstate ./internal/service/pluginhostservices ./internal/service/plugin ./internal/cmd -count=1`、临时 `go.work` 下 `GOWORK="$tmpdir/go.work" go test ./backend/internal/service/middleware -count=1`、`pnpm -C hack/tests exec tsc -p tsconfig.json --noEmit --pretty false --incremental false`、`pnpm -C apps/lina-vben --filter @lina/web-antd typecheck`、`E2E_BASE_URL=http://127.0.0.1:5666 E2E_API_BASE_URL=http://127.0.0.1:9120/api/v1/ E2E_PUBLIC_BASE_URL=http://127.0.0.1:9120 E2E_PARALLEL_WORKERS=1 pnpm -C hack/tests test:module -- plugins -- --shard=5/5`（56 passed）、`E2E_BASE_URL=http://127.0.0.1:5666 E2E_API_BASE_URL=http://127.0.0.1:9120/api/v1/ E2E_PUBLIC_BASE_URL=http://127.0.0.1:9120 E2E_PARALLEL_WORKERS=1 pnpm -C hack/tests test:module -- extension:plugin`（29 passed, 1 skipped）。
- FB-23 修复了 nightly `host-only` 与 `plugin-full / plugins-2-of-5`、`plugins-3-of-5`、`plugins-4-of-5` 分片失败根因：`linapro-demo-dynamic` 动态插件运行时不再在 WASM 请求路径使用 reflection guest controller dispatcher，而是使用显式 requestType/internalPath 分发器，避免动态插件在 CI 运行时触发 `backend.init`/反射分发崩溃；动态插件 E2E 强制重建当前 WASM 产物并删除旧 `runtime/*.wasm` 残留，避免本地或 CI 缓存拿到旧崩溃产物；旧 `/x/{plugin-id}/{resource}` 动态插件示例断言同步迁移到 `/x/{plugin-id}/api/v1/{resource}`。本次不新增用户可见文案、翻译键、接口字段或 apidoc 文本，i18n 资源无需变更；不新增数据操作语义，动态插件 CRUD、附件下载、host service 和菜单权限仍复用既有插件端点权限、租户上下文和数据权限边界；不新增业务缓存键、跨实例失效或缓存一致性机制，缓存影响仅限 E2E 产物治理通过强制重建和旧产物删除避免 stale WASM artifact。已验证：临时 `go.work` 下 `GOWORK="$tmpdir/go.work" go test ./... -count=1`（`apps/lina-plugins/linapro-demo-dynamic`）、`make wasm p=linapro-demo-dynamic out=../../temp/output`、`cd apps/lina-core && go test ./internal/service/plugin/internal/runtime -count=1`、`cd apps/lina-core && go test ./internal/cmd -count=1`、`pnpm -C hack/tests exec tsc -p tsconfig.json --noEmit --pretty false --incremental false`、`pnpm -C apps/lina-vben --filter @lina/web-antd typecheck`、`E2E_BASE_URL=http://127.0.0.1:5666 E2E_API_BASE_URL=http://127.0.0.1:9120/api/v1/ E2E_PUBLIC_BASE_URL=http://127.0.0.1:9120 E2E_PARALLEL_WORKERS=1 pnpm -C hack/tests exec playwright test ../apps/lina-plugins/linapro-demo-dynamic/hack/tests/e2e/runtime/TC004-menu-dynamic-permission-tree.ts --config playwright.config.ts --project=chromium --workers=1`（3 passed）、`E2E_BASE_URL=http://127.0.0.1:5666 E2E_API_BASE_URL=http://127.0.0.1:9120/api/v1/ E2E_PUBLIC_BASE_URL=http://127.0.0.1:9120 E2E_PARALLEL_WORKERS=1 pnpm -C hack/tests test:module -- plugins -- --shard=2/5`（55 passed）、`E2E_BASE_URL=http://127.0.0.1:5666 E2E_API_BASE_URL=http://127.0.0.1:9120/api/v1/ E2E_PUBLIC_BASE_URL=http://127.0.0.1:9120 E2E_PARALLEL_WORKERS=1 pnpm -C hack/tests test:module -- plugins -- --shard=3/5`（49 passed, 7 skipped）、`E2E_BASE_URL=http://127.0.0.1:5666 E2E_API_BASE_URL=http://127.0.0.1:9120/api/v1/ E2E_PUBLIC_BASE_URL=http://127.0.0.1:9120 E2E_PARALLEL_WORKERS=1 pnpm -C hack/tests test:module -- plugins -- --shard=4/5`（55 passed）、按 CI host-only 流程执行 `make init confirm=init rebuild=true`、`make mock confirm=mock`、`make dev plugins=0` 后运行 `E2E_BASE_URL=http://127.0.0.1:5666 E2E_API_BASE_URL=http://127.0.0.1:9120/api/v1/ E2E_PUBLIC_BASE_URL=http://127.0.0.1:9120 E2E_PARALLEL_WORKERS=1 pnpm -C hack/tests test:host`（parallel: 35 passed; serial: 247 passed, 1 skipped）。
- FB-25 为前端单测环境兼容性修复：`apps/web-antd/src/api/request.ts` 现在集中规范化 `useAppConfig()` 返回的 API base，缺省或空值回退到默认宿主 API base `/api/v1`，避免 `pluginApiPath` 顶层初始化在未注入 `VITE_GLOB_API_URL` 的 Vitest 环境中读取 `undefined.trim()`；新增 `apps/web-antd/src/api/request.test.ts` 覆盖缺省 API base 和绝对 API base 下的插件 API URL 生成。该修复不新增用户可见文案、菜单、路由展示文本、REST API 字段、manifest i18n 或 apidoc 文本，i18n 资源无需变更；不新增或修改后端数据操作接口、权限边界、业务缓存、开发工具或脚本，数据权限、缓存一致性和 Go 编译门禁均不适用。已验证：`pnpm -C apps/lina-vben exec vitest run apps/web-antd/src/api/request.test.ts apps/web-antd/src/components/tree/src/helper.test.ts --dom`、`cd apps/lina-vben && pnpm test:unit`（44 files, 359 tests）、`pnpm -C apps/lina-vben --filter @lina/web-antd typecheck`、`openspec validate decouple-workspace-plugin-routes --strict`、`git diff --check -- apps/lina-vben/apps/web-antd/src/api/request.ts apps/lina-vben/apps/web-antd/src/api/request.test.ts openspec/changes/decouple-workspace-plugin-routes/tasks.md`。
- FB-25 `/lina-review`：审查范围为 `apps/lina-vben/apps/web-antd/src/api/request.ts`、新增 `apps/lina-vben/apps/web-antd/src/api/request.test.ts` 和本任务记录；未发现阻塞问题。确认修复点与两个失败 CI job 的 `pnpm test:unit` 命令一致，新增测试可复现缺省 `VITE_GLOB_API_URL` 场景并验证插件 API base helper 仍按 `/x/{plugin-id}/api/v1/...` 生成路径；未修改 Go 生产代码、REST API、数据库 schema、后端数据权限、运行时缓存、运行时 i18n 资源或开发工具脚本。
- FB-26 为 E2E 测试独立性修复：`linapro-tenant-core` 卸载前置条件弹窗用例现在先显式调用 `ensureMultiTenantPluginEnabled` 再创建租户，并将租户创建纳入 `try/finally`，避免同分片其他插件生命周期用例改动插件状态后导致 `/x/linapro-tenant-core/api/v1/platform/tenants` 准备请求失败。该修复只调整测试准备与清理逻辑，不新增用户可见文案、翻译键、REST API、后端生产代码、数据权限、业务缓存、开发工具或脚本；i18n、缓存一致性和 Go 编译门禁均不适用。已验证：`pnpm -C hack/tests exec tsc -p tsconfig.json --noEmit --pretty false --incremental false`、`make init confirm=init rebuild=true`、`make mock confirm=mock`、`make dev plugins=1`、`E2E_BASE_URL=http://127.0.0.1:5666 E2E_API_BASE_URL=http://127.0.0.1:9120/api/v1/ E2E_PUBLIC_BASE_URL=http://127.0.0.1:9120 E2E_PARALLEL_WORKERS=1 pnpm -C hack/tests exec playwright test ../apps/lina-plugins/linapro-tenant-core/hack/tests/e2e/lifecycle-precondition/TC004-plugin-uninstall-force-dialog.ts --config playwright.config.ts --project=chromium --workers=1`（1 passed）、`E2E_BASE_URL=http://127.0.0.1:5666 E2E_API_BASE_URL=http://127.0.0.1:9120/api/v1/ E2E_PUBLIC_BASE_URL=http://127.0.0.1:9120 E2E_PARALLEL_WORKERS=1 pnpm -C hack/tests test:module -- plugins -- --shard=5/5`（56 passed）。
- FB-26 `/lina-review`：审查范围为 `linapro-tenant-core/hack/tests/e2e/lifecycle-precondition/TC004-plugin-uninstall-force-dialog.ts`、`openspec/changes/decouple-workspace-plugin-routes/tasks.md` 和子模块工作区状态；未发现阻塞问题。确认目标 E2E 不再依赖同分片前序用例保留的插件安装/启用状态，单独运行与 CI 同款 `plugins -- --shard=5/5` 均通过；本次未修改 Go 生产代码、REST API、数据库 schema、后端数据权限、运行时缓存、运行时 i18n 资源或开发工具脚本。
- FB-27 为 E2E 测试契约同步修复：`linapro-content-notice` 消息列表、通知发布和通知详情用例将宿主消息接口继续保留在 `/api/v1/user/message/**`，并把插件自有通知 CRUD 请求迁移到 `/x/linapro-content-notice/api/v1/notice/**`；`linapro-demo-dynamic` 运行时生命周期与菜单权限树用例将动态托管资产断言迁移到 `/x-assets/{plugin-id}/{version}/...`，并让临时生成的动态 WASM manifest 声明 `public_assets` 且将测试资源放入 `frontend/pages/`，匹配生产插件资产发布契约。该修复只调整 E2E 测试与测试生成的临时 WASM fixture，不新增用户可见文案、翻译键、REST API、后端生产代码、数据权限、业务缓存、开发工具或脚本；i18n、缓存一致性、数据权限和 Go 编译门禁均不适用。已验证：`pnpm -C hack/tests exec tsc -p tsconfig.json --noEmit --pretty false --incremental false`、`make init confirm=init rebuild=true`、`make mock confirm=mock`、`make dev plugins=1`、`E2E_BASE_URL=http://127.0.0.1:5666 E2E_API_BASE_URL=http://127.0.0.1:9120/api/v1/ E2E_PUBLIC_BASE_URL=http://127.0.0.1:9120 E2E_PARALLEL_WORKERS=1 pnpm -C hack/tests exec playwright test ../apps/lina-plugins/linapro-demo-dynamic/hack/tests/e2e/runtime/TC001-runtime-wasm-lifecycle.ts --config playwright.config.ts --project=chromium --workers=1`（10 passed）、`E2E_BASE_URL=http://127.0.0.1:5666 E2E_API_BASE_URL=http://127.0.0.1:9120/api/v1/ E2E_PUBLIC_BASE_URL=http://127.0.0.1:9120 E2E_PARALLEL_WORKERS=1 pnpm -C hack/tests test:module -- plugins -- --shard=1/5`（59 passed）。
- FB-27 `/lina-review`：审查范围为 `linapro-content-notice` 的 3 个通知/消息 E2E、`linapro-demo-dynamic` 的运行时生命周期与菜单权限树 E2E、以及本任务记录；未发现阻塞问题。确认宿主消息 API 与插件通知 API 边界清晰，动态插件测试 fixture 已同步 `public_assets` 发布契约，旧 `/plugin-assets` 仅保留为禁止回退断言；本次未修改生产 Go 代码、REST API、数据库 schema、后端数据权限、运行时缓存、运行时 i18n 资源或开发工具脚本，Go 编译门禁不适用。
- FB-28 为官方示例插件菜单归属修复：`linapro-demo-source` 和 `linapro-demo-dynamic` 的主工作台菜单均通过 `plugin.yaml menus[].parent_key: extension` 挂载到扩展中心，排序分别改为 2 和 3，使其排在宿主 `extension:plugin:list` 插件管理菜单之后；动态插件菜单树 E2E 强制按当前 manifest 重新安装源码示例和动态示例，覆盖扩展中心下的相对顺序和动态路由权限按钮父子关系。该修复新增用户可见菜单层级变化，但复用既有菜单名称与插件自身 `manifest/i18n` 资源，不新增翻译键、REST API、后端生产代码、数据操作接口、权限边界、业务缓存或开发工具脚本；数据权限、缓存一致性和 Go 生产编译门禁均不适用。已验证：`cd apps/lina-core && go test ./internal/service/plugin/internal/catalog -run TestOfficialDemoPluginMenusMountUnderExtension -count=1`、`openspec validate decouple-workspace-plugin-routes --strict`、`pnpm -C hack/tests exec tsc -p tsconfig.json --noEmit --pretty false --incremental false`、`make dev skip_wasm=true`、`E2E_BASE_URL=http://127.0.0.1:5666 E2E_API_BASE_URL=http://127.0.0.1:9120/api/v1/ E2E_PUBLIC_BASE_URL=http://127.0.0.1:9120 E2E_PARALLEL_WORKERS=1 pnpm -C hack/tests exec playwright test ../apps/lina-plugins/linapro-demo-dynamic/hack/tests/e2e/runtime/TC004-menu-dynamic-permission-tree.ts --config playwright.config.ts --project=chromium --workers=1 --reporter=list`（3 passed）。
- FB-28 `/lina-review`：审查范围为 `linapro-demo-source/plugin.yaml`、`linapro-demo-dynamic/plugin.yaml`、动态插件菜单权限树 E2E、`catalog_test.go` 中的示例菜单 manifest 合约测试，以及本次 OpenSpec 记录；未发现阻塞问题。确认两个显式启用 i18n 的示例插件复用既有菜单标题资源，菜单归属和排序由 manifest 声明驱动并被 Go 合约测试与 E2E 同时覆盖；本次未修改生产 Go 代码、REST API、数据库 schema、后端数据权限、运行时缓存或开发工具脚本，Go 生产编译门禁不适用。
- FB-29 为示例插件菜单迁移后的 E2E 反馈修复。根因是 FB-28 将源码/动态示例插件菜单移入扩展中心后，旧测试仍按顶层菜单或旧直达路径定位；同时当前工作台侧栏使用 Vben 菜单结构而非 Ant Design `.ant-menu-submenu-title`，共享 POM 未能展开 `Extensions`，导致插件菜单留在隐藏子菜单中；源码插件动态页注册键为叶子路由 `linapro-demo-source-sidebar-entry`，而新工作台路径为 `/extension/linapro-demo-source-sidebar-entry`，前端动态页只按完整路径查表时会显示 `Plugin page not found`。修复内容：`dynamic-page.vue` 按完整路径和叶子路径两级候选查找插件页；`PluginPage` 与 `MainLayout` 的侧栏 helper 同时支持 Ant Design 和 Vben 菜单结构、自动展开扩展中心并返回可见菜单 locator；相关源码/动态插件 E2E 改为全局安装示例插件、使用扩展中心路径或共享菜单 helper，并在动态插件语言切换测试中允许同一插件菜单的中英文候选标签，页面正文仍严格断言目标语言。该修复不新增运行时用户可见文案、翻译键、REST API、后端生产代码、数据库 schema、数据操作语义、业务缓存、开发工具或脚本；`i18n` 影响仅限测试定位容忍菜单投影刷新时序，插件自身 `manifest/i18n` 资源未变更；数据权限和缓存一致性无影响；插件影响限定为示例插件 E2E 准备流程和动态页前端适配层。已按规则读取 `AGENTS.md`、`.agents/rules/openspec.md`、`.agents/rules/testing.md`、`.agents/rules/frontend-ui.md`、`.agents/rules/i18n.md`、`.agents/rules/plugin.md`、`.agents/rules/data-permission.md`、`.agents/rules/cache-consistency.md`、`.agents/rules/architecture.md`、`.agents/rules/documentation.md`。已验证：`pnpm -C hack/tests exec tsc -p tsconfig.json --noEmit --pretty false --incremental false`、`pnpm -C apps/lina-vben --filter @lina/web-antd typecheck`、`E2E_BASE_URL=http://127.0.0.1:5666 E2E_API_BASE_URL=http://127.0.0.1:9120/api/v1/ E2E_PUBLIC_BASE_URL=http://127.0.0.1:9120 E2E_PARALLEL_WORKERS=1 pnpm -C hack/tests exec playwright test ../apps/lina-plugins/linapro-demo-dynamic/hack/tests/e2e/runtime/TC002-runtime-i18n-switch.ts --config playwright.config.ts --project=chromium --workers=1 --reporter=list`（3 passed）、`E2E_BASE_URL=http://127.0.0.1:5666 E2E_API_BASE_URL=http://127.0.0.1:9120/api/v1/ E2E_PUBLIC_BASE_URL=http://127.0.0.1:9120 E2E_PARALLEL_WORKERS=1 pnpm -C hack/tests exec playwright test ../apps/lina-plugins/linapro-demo-source/hack/tests/e2e/host-integration/TC007-backend-hardcoded-chinese-regression.ts --config playwright.config.ts --project=chromium --workers=1 --reporter=list`（2 passed）、浏览器脚本验证展开 `Extensions` 后点击 `Source Plugin Demo` 可进入 `/admin/extension/linapro-demo-source-sidebar-entry` 并显示 `Demo Records`、`git diff --check`、`git -C apps/lina-plugins diff --check`。`TC003-english-runtime-page-audit.ts` 与 `TC001-source-plugin-lifecycle.ts --grep "TC-1c"` 在本地受 Postgres.app 权限弹窗阻断：`FATAL: Postgres.app failed to verify "trust" authentication`；CI 使用 Docker Postgres，GitHub job 日志中的 Postgres 服务正常启动，该阻断为本机环境问题。
- FB-29 `/lina-review`：审查范围为 `dynamic-page.vue` 插件页路由候选匹配、宿主共享 `PluginPage`/`MainLayout` 侧栏菜单 POM、示例插件 E2E 安装模式和扩展中心路径同步、以及本任务记录；未发现阻塞问题。确认修复没有修改 `lina-core` 核心领域契约、REST API、数据库 schema、后端数据权限、运行时缓存或插件 i18n 资源；功能行为回归已由动态插件 i18n E2E、源码插件英文页面直达 E2E、前端类型检查、测试类型检查、OpenSpec 严格校验和静态 diff 检查覆盖，本地 psql 阻断已记录为环境限制。
