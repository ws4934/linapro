## 1. 公共能力接口拆分

- [x] 1.1 拆分`pkg/plugin/capability/orgcap`普通消费接口、provider-facing接口、宿主内部`ScopeService`和`AssignmentService`，删除普通消费面中的`*gdb.Model`、写入和数据范围注入方法。
- [x] 1.2 拆分`pkg/plugin/capability/tenantcap`普通消费接口、provider-facing接口、`RequestResolver`、`ScopeService`、`UserMembershipService`、`PluginProvisioningService`和`StartupConsistencyService`，删除普通消费面中的`*gdb.Model`、`*ghttp.Request`、写入、自动开通和启动一致性方法。
- [x] 1.3 调整`pkg/plugin/capability.Directory`，确保`Org()`、`Tenant()`和其他目录方法只返回普通插件消费接口或受治理只读能力。
- [x] 1.4 删除`contract.ProviderEnv.Services any`，为`orgcap`、`tenantcap`等 provider factory 定义强类型窄环境，并补齐接口注释。

## 2. 宿主运行时装配迁移

- [x] 2.1 调整`internal/service/pluginhostservices`，由启动期显式构造`capability.Directory`并为 provider env 注入所需窄能力，禁止创建孤立服务实例。
- [x] 2.2 将宿主`datascope`、`user`、`role`、`notify`、`middleware`、`auth`、`session`、`menu`和`plugin`等调用方改为构造函数显式注入实际需要的组织/租户窄接口。
- [x] 2.3 更新宿主测试替身，使测试只实现被测包依赖的窄接口，并删除为了满足旧宽接口保留的无关空方法。
- [x] 2.4 验证组织和租户能力禁用时宿主仍按既有降级策略返回零值、隐藏可选能力或拒绝越权请求，不产生 500。

## 3. 官方插件和 provider 迁移

- [x] 3.1 更新`linapro-org-core` provider adapter，改用`orgcap.ProviderEnv`强类型环境，保留业务逻辑在`backend/internal/service/`和`backend/internal/provider/`下。
- [x] 3.2 更新`linapro-tenant-core` provider adapter，改用`tenantcap.ProviderEnv`强类型环境，删除对完整`capability.Directory`的运行时类型断言。
- [x] 3.3 扫描并迁移其他官方源码插件对`orgcap`、`tenantcap`、`pluginhost.HostServices`、`HostServices()`和`HostServicesForPlugin`的使用。
- [x] 3.4 使用临时或既有 Go workspace 编译官方源码插件后端，确认插件无法依赖宿主内部 service、DAO、DO 或 Entity。

## 4. pluginhost 边界收敛

- [x] 4.1 删除`pluginhost.HostServices`别名、`ScopedHostServicesFactory`、`HostServicesForPlugin`和 registrar/payload 中的`HostServices()`方法。
- [x] 4.2 将源码插件 registrar、hook payload、cron registrar、HTTP registrar 和 lifecycle 输入统一改为`Services()`并返回源码插件运行期`pluginhost.Services`。
- [x] 4.3 更新`internal/service/plugin/internal/integration`源码插件注册、hook、cron 和 route 流程，使插件能力目录 scoped 绑定由`capability.ServicesForPlugin`或等价公共能力目录机制负责。

## 5. pluginbridge 和动态 guest 收敛

- [x] 5.1 删除`pluginbridge`根包业务能力 client 入口，包括`Runtime()`、`Storage()`、`HTTP()`、`Data()`、`Cache()`、`Lock()`、`Config()`、`Notify()`、`Cron()`、`HostConfig()`、`Manifest()`以及对应业务 client 类型别名。
- [x] 5.2 确认动态插件业务能力 client 统一位于`pkg/plugin/capability/guest`，并补齐动态插件样例、测试和文档引用。
- [x] 5.3 保留`pluginbridge`协议和 transport 必需入口，包括 ABI、envelope、codec、host call、host service wire 校验、WASM section 和动态路由 dispatcher。
- [x] 5.4 更新`hack/tools/linactl/internal/wasmbuilder`和动态插件 WASI 构建流程，确保生成代码使用新的`capability/guest`入口。

## 6. 数据权限、缓存和性能治理

- [x] 6.1 确认宿主内部`ScopeService`类接口在数据库查询阶段注入组织/租户数据权限，不先查全量再内存过滤。
- [x] 6.2 确认普通 capability 和动态 data service 只返回 DTO、值对象、批量投影或结构化数据响应，不暴露`*gdb.Model`、DAO、DO、Entity 或 raw SQL。
- [x] 6.3 验证用户列表、角色授权、通知发送、插件 datahost 等高频路径使用批量投影或数据库侧分页过滤，避免逐项 provider 查询或前端瀑布式补查。
- [x] 6.4 确认 provider 可用性仍以插件 enabled snapshot 和 runtime revision 为权威，不新增第二套 active 状态或孤立本地缓存。

## 7. 治理扫描和验证

- [x] 7.1 新增或更新跨平台治理扫描，检查`pkg/plugin/**`不得导入`internal/service/plugin/**`，插件代码不得导入宿主`internal/**`，生产代码不得引用旧`HostServices`、`ProviderEnv.Services`或`pluginbridge`业务能力入口。
- [x] 7.2 运行`cd apps/lina-core && go test ./pkg/plugin/... -count=1`，覆盖公共插件契约和动态 guest SDK。
- [x] 7.3 运行`cd apps/lina-core && go test ./internal/service/pluginhostservices ./internal/service/plugin/... -count=1`，覆盖宿主插件适配和运行时。
- [x] 7.4 运行覆盖启动装配的宿主编译门禁，默认使用`cd apps/lina-core && go test ./internal/cmd -count=1`。
- [x] 7.5 运行官方源码插件后端编译 smoke 和动态插件 WASI 构建 smoke。
- [x] 7.6 如修改`linactl`或构建器，运行`cd hack/tools/linactl && go test ./... -count=1`并记录跨平台影响；若未修改开发工具，记录无开发工具影响。
- [x] 7.7 运行`openspec validate harden-plugin-capability-boundaries --strict`和相关文件格式检查。

## 8. 影响分析记录

- [x] 8.1 在任务执行记录中记录`i18n`影响；预计无运行时用户可见文案、API 文档源文本、语言包或插件清单变更，若实现阶段出现文案变更则补充对应资源和校验。
- [x] 8.2 在任务执行记录中记录数据权限影响；本变更目标是收窄公共暴露面，宿主内部数据权限仍需数据库侧过滤和写前可见性校验。
- [x] 8.3 在任务执行记录中记录缓存一致性影响；确认不新增缓存源、失效通道或跨实例协调分支，且所有缓存敏感服务复用启动期共享实例。
- [x] 8.4 在任务执行记录中记录测试策略；纯内部重构不新增 E2E，除非实现阶段改变用户可观察行为或端到端工作流。

## Apply 执行记录

- 1.1 实现摘要：已在`pkg/plugin/capability/orgcap`中拆出普通`Service`、宿主内部`ScopeService`、宿主内部`AssignmentService`和组合`RuntimeService`；`capability.Directory.Org()`仍只返回普通`orgcap.Service`，普通消费面不再包含`*gdb.Model`、`ApplyUserDeptScope`、`BuildUserDeptScopeExists`、`ReplaceUserAssignments`或`CleanupUserAssignments`。
- 1.1 宿主迁移：`internal/service/datascope`改为依赖`orgcap.ScopeService`，`internal/service/user`改为对组织读能力和组织写入能力显式拆分注入；启动装配继续复用同一个运行期`orgCapSvc`实例，不新增孤立服务图。
- 1.1 测试替身：`datascope`测试替身已缩到只实现 scope 窄接口；`user`、`role`和`file`测试 helper 按需从普通组织能力中提取`ScopeService`或`AssignmentService`，避免被测包为旧宽接口补无关方法。
- 1.1 规则加载：已读取`openspec`、`documentation`、`architecture`、`plugin`、`api-contract`、`backend-go`、`data-permission`、`cache-consistency`、`testing`和`i18n`规则，并使用`goframe-v2`后端开发技能。
- 1.1 影响判断：`i18n`资源无影响；未新增 HTTP API、前端页面、SQL、插件清单或运行时用户可见文案；数据权限能力继续由宿主内部`ScopeService`在数据库查询阶段注入；缓存一致性无新增缓存源、失效通道或跨实例协调分支；开发工具跨平台无影响；测试策略为后端编译门禁和定向单元测试，不新增 E2E。
- 1.1 验证方式：已运行`cd apps/lina-core && go test ./pkg/plugin/capability/orgcap ./internal/service/datascope ./internal/service/user -count=1`通过；已运行`cd apps/lina-core && go test ./internal/cmd ./internal/service/role ./internal/service/file ./internal/service/auth ./internal/controller/user ./internal/service/pluginhostservices ./internal/service/plugin/internal/datahost ./internal/service/plugin/internal/integration ./internal/service/plugin/internal/wasm ./pkg/plugin/... -count=1`通过。
- 1.2 实现摘要：已在`pkg/plugin/capability/tenantcap`中拆出普通`Service`、provider-facing`Provider`/`UserMembershipProvider`/`PluginProvisioningProvider`、宿主内部`RequestResolver`、`ScopeService`、`UserMembershipService`、`PluginProvisioningService`、`StartupConsistencyService`、`FallbackService`和组合`RuntimeService`；普通`Service`不再包含`*gdb.Model`、`*ghttp.Request`、用户租户关系写入、自动开通或启动一致性方法。
- 1.2 宿主迁移：`session`改为依赖`tenantcap.ScopeService`完成数据库侧会话租户过滤；`notify`改为依赖`ScopeService`完成通知收件人数据库侧租户过滤；`user`和`role`改为依赖运行期`RuntimeService`承载宿主内部 membership 写入和校验；`middleware`进一步收窄为包内`tenantMiddlewareService`，只保留请求解析和平台绕过所需方法；`plugin`启动一致性和自动开通继续使用启动期共享的`RuntimeService`实例。
- 1.2 测试替身：`notify`、`role`、`middleware`、`plugin`和`pluginhostservices`相关测试替身已按被测包依赖收窄或改为返回`tenantcap.RuntimeService`；修复 provider-facing membership 接口和宿主内部`ScopeService`混用导致的 provider 类型断言降级问题。
- 1.2 规则加载：已读取`openspec`、`documentation`、`architecture`、`plugin`、`backend-go`、`data-permission`、`cache-consistency`、`testing`和`i18n`规则，并使用`goframe-v2`后端开发技能；本段未修改 HTTP API、DTO、SQL、前端、开发工具或插件资源。
- 1.2 影响判断：`i18n`资源无影响；未新增 HTTP API、前端页面、SQL、插件清单或运行时用户可见文案；数据权限能力继续由宿主内部`ScopeService`/`UserMembershipService`在数据库查询阶段或写前校验阶段执行，普通插件消费面不暴露底层查询注入或写入接口；缓存一致性无新增缓存源、失效通道或跨实例协调分支，租户 provider 可用性继续复用插件 enabled snapshot 和 runtime revision；开发工具跨平台无影响；测试策略为后端编译门禁和定向单元测试，不新增 E2E。
- 1.2 验证方式：已运行`cd apps/lina-core && go test ./pkg/plugin/capability/tenantcap ./internal/service/session ./internal/service/notify ./internal/service/user ./internal/service/role ./internal/service/middleware ./internal/service/plugin ./internal/service/pluginhostservices -count=1`通过；已静态检索确认`tenantcap.Service`普通消费面不含`*gdb.Model`、`*ghttp.Request`、写入、自动开通或启动一致性方法。
- 1.3 实现摘要：已从`pkg/plugin/capability.Directory`移除`TenantFilter()`，普通能力目录仅保留`Org() orgcap.Service`、`Tenant() tenantcap.Service`和其他只读或受治理消费接口；源码插件当前需要的插件自有表租户过滤保留在`pluginhost.HostServices`源码插件运行期目录中，不再作为普通 capability 目录方法暴露。
- 1.3 宿主迁移：`internal/service/pluginhostservices`继续由启动期共享服务实例构造目录，并同时实现普通`capability.Directory`和源码插件`pluginhost.HostServices`；源码插件注册、hook 和 cron 路径改为通过`pluginhost.HostServicesForPlugin`获取插件 scoped 目录，动态 WASM framework host service 仍只接收普通`capability.Directory`；`linapro-org-core` provider 暂从`env.Services`断言源码插件运行期目录`pluginhost.HostServices`以继续获取受治理的`TenantFilter()`，不恢复普通目录方法。
- 1.3 测试覆盖：新增`pkg/plugin/capability`反射测试，阻止`capability.Directory`重新暴露`TenantFilter()`；更新源码插件集成测试替身的 scoped 目录方法，使测试仍按源码插件运行期目录验证。
- 1.3 规则加载：已读取`openspec`、`documentation`、`architecture`、`plugin`、`backend-go`、`data-permission`、`cache-consistency`、`testing`和`i18n`规则，并使用`goframe-v2`后端开发技能；本段未修改 HTTP API、DTO、SQL、前端、开发工具或插件资源。
- 1.3 影响判断：`i18n`资源无影响；未新增运行时用户可见文案、API 文档源文本、语言包或插件清单字段；数据权限影响为收窄普通目录暴露面，`TenantFilterService.Apply`仍仅留在源码插件 host service 目录用于插件自有表数据库侧过滤；缓存一致性无新增缓存源、失效通道或跨实例协调分支，目录继续复用启动期共享实例；开发工具跨平台无影响；测试策略为后端编译门禁、定向单元测试和静态检索，不新增 E2E。
- 1.3 验证方式：已运行`cd apps/lina-core && go test ./pkg/plugin/capability ./pkg/plugin/pluginhost ./internal/service/pluginhostservices ./internal/service/plugin/internal/integration -count=1`通过；已使用临时 Go workspace 运行`cd apps/lina-plugins/linapro-org-core && GOWORK=<temp>/go.work go test ./backend -count=1`通过；已静态检索确认`capability.Directory`定义不再包含`TenantFilter()`或`*gdb.Model`/`*ghttp.Request`方法签名。
- 1.4 实现摘要：已删除`contract.ProviderEnv`及其中的`Services any`宽字段，将内部 capability registry 改为泛型 typed env；`orgcap.ProviderFactory`改为接收`orgcap.ProviderEnv{PluginID,TenantFilter}`，`tenantcap.ProviderFactory`改为接收`tenantcap.ProviderEnv{PluginID,BizCtx,PluginLifecycle}`，并补齐 provider env 和 runtime env 方法注释。
- 1.4 宿主迁移：`internal/service/plugin`不再向 provider 暴露完整 host service directory，改为实现`OrgProviderEnv`和`TenantProviderEnv`并从 plugin-scoped source host services 中只抽取 provider adapter 需要的窄依赖；`pluginBackedOrganizationCapabilityState`同步依赖新的组织 provider runtime 窄方法。
- 1.4 官方插件迁移：`linapro-org-core` provider 直接使用`orgcap.ProviderEnv.TenantFilter`构造 adapter；`linapro-tenant-core` provider 直接使用`tenantcap.ProviderEnv.BizCtx`和`PluginLifecycle`构造 adapter，删除对完整`capability.Directory`或`pluginhost.HostServices`的运行时类型断言。
- 1.4 测试替身：宿主 tenant/org provider 测试 runtime 改为只实现`TenantProviderEnv`或`OrgProviderEnv`，测试 provider factory 签名改为对应 capability 的强类型 env；同时修正 session 测试替身继续依赖`tenantcap.ScopeService`窄接口。
- 1.4 规则加载：已读取`openspec`、`documentation`、`architecture`、`plugin`、`backend-go`、`data-permission`、`cache-consistency`、`testing`和`i18n`规则，并使用`goframe-v2`后端开发技能；本段未修改 HTTP API、DTO、SQL、前端、开发工具、插件清单或运行时文案。
- 1.4 影响判断：`i18n`资源无影响；数据权限影响为 provider 构造环境不再暴露完整目录或普通插件消费面，组织 provider 仍仅通过受治理`TenantFilterService`对插件自有表做数据库侧租户过滤；缓存一致性无新增缓存源、失效通道或跨实例协调分支，provider 可用性继续由插件 enabled snapshot 和 runtime revision 路径控制；开发工具跨平台无影响；测试策略为后端编译门禁、官方源码插件 smoke 和旧入口静态检索，不新增 E2E。
- 1.4 验证方式：已运行`cd apps/lina-core && go test ./pkg/plugin/capability/internal/capabilityregistry ./pkg/plugin/capability/orgcap ./pkg/plugin/capability/tenantcap -count=1`通过；已运行`cd apps/lina-core && go test ./internal/service/plugin ./internal/service/role ./internal/service/auth ./internal/service/notify ./internal/service/menu ./internal/service/user ./internal/service/plugin/internal/wasm -count=1`通过；已使用临时 Go workspace 分别运行`linapro-org-core`和`linapro-tenant-core`的`go test ./backend -count=1`通过；已静态检索确认 Go 生产和测试代码中不再存在`contract.ProviderEnv`、`ProviderEnv.Services`、`Services any`或旧`ProviderServices()`残留。
- 2.1 实现摘要：`internal/service/pluginhostservices.New`已改为返回普通`capability.Directory`，启动期显式注入共享`orgCapSvc`、`tenantSvc`、`pluginSvc`、`bizCtxSvc`、`scopeSvc`、`i18nSvc`、`notifySvc`、`sessionStore`和`kvCacheSvc`等运行期实例；目录本身同时满足普通`capability.Directory`和当前源码插件过渡期`pluginhost.HostServices`，但宿主启动、`internal/service/plugin`和`internal/service/plugin/internal/integration`均只持有`capability.Directory`。
- 2.1 provider env：`internal/service/plugin.OrgProviderEnv`和`TenantProviderEnv`改为先通过`capability.ServicesForPlugin`绑定插件身份，再只抽取`TenantFilter`、`BizCtx`和`PluginLifecycle`等 provider adapter 需要的窄能力；不再通过旧`pluginhost.HostServicesForPlugin` helper 获取完整 host service 目录。
- 2.1 源码插件边界：`internal/service/plugin/internal/integration`新增源码插件回调边界 helper，仅在 HTTP registrar、Cron registrar、hook payload 和 managed cron collector 需要源码插件专属`TenantFilter`时，把 scoped `capability.Directory`受控断言为`pluginhost.HostServices`；后续 4.x 任务将继续删除旧`HostServices`/`HostServices()`公共语义。
- 2.1 测试替身：插件集成测试替身改为实现`capability.capability.ScopedServicesFactory`并由`ForPlugin`返回 scoped `capability.Directory`，避免测试装配继续依赖旧`ScopedHostServicesFactory`。
- 2.1 规则加载：已读取`openspec`、`documentation`、`architecture`、`plugin`、`api-contract`、`backend-go`、`data-permission`、`cache-consistency`、`testing`和`i18n`规则，并使用`goframe-v2`与`karpathy-guidelines`技能；本段未修改 SQL、前端、开发工具或插件资源。
- 2.1 影响判断：`i18n`资源无影响；未新增运行时用户可见文案、API 文档源文本、语言包或插件清单字段；数据权限影响为 provider 构造仅从 scoped 目录抽取受治理`TenantFilter`，普通目录仍不暴露`*gdb.Model`方法；缓存一致性无新增缓存源、失效通道或跨实例协调分支，cache、plugin state、tenant、org、i18n 等能力继续复用启动期共享实例；开发工具跨平台无影响；测试策略为后端编译门禁、定向单元测试、启动装配编译和 OpenSpec 严格校验，不新增 E2E。
- 2.1 验证方式：已运行`cd apps/lina-core && go test ./internal/service/pluginhostservices ./internal/service/plugin/internal/integration ./internal/service/plugin -run 'TestDirectory|TestCache|TestBuild|TestSet|Test.*HostServices|Test.*ProviderEnv|Test.*Framework' -count=1`通过；已运行`cd apps/lina-core && go test ./internal/service/pluginhostservices ./internal/service/plugin/internal/integration ./internal/service/plugin -count=1`通过；已运行`cd apps/lina-core && go test ./internal/cmd -count=1`通过；已运行`openspec validate harden-plugin-capability-boundaries --strict`通过；已运行本段修改文件的`git diff --check`通过。
- 2.2 实现摘要：已将宿主调用方继续从普通或组合型组织/租户能力收敛到实际使用的窄接口；`auth`新增`authOrgProjectionService`和`authTenantAccessService`，只保留登录与 token 流程所需的组织投影和租户成员校验；`menu`只接收`platformMenuTenantService`；`role`只接收`roleTenantGovernanceService`，不再在 service 结构中保存组织普通能力；`user`将租户依赖拆为`ScopeService`、`UserMembershipService`和只读`userTenantAccessService`，组织写入通过显式`AssignmentService`处理；`plugin`将旧`SetTenantCapability(RuntimeService)`拆为`SetTenantStartupCapability`、`SetTenantProvisioningCapability`和`SetTenantPlatformGovernanceCapability`。
- 2.2 宿主装配：`internal/cmd`启动期仍只构造一个共享`tenantSvc`和一个共享`orgCapSvc`实例，再按消费方需要注入对应窄接口或 setter，未新增孤立服务实例；`datascope`、`notify`、`session`和`middleware`已保持前序任务中的窄接口注入形态。
- 2.2 测试同步：为适配生产构造签名，同步调整`role`、`user`、`plugin`和`cmd`的测试装配 helper；本段只修正当前任务触及的构造边界，不提前完成 2.3 的全量测试替身瘦身。
- 2.2 规则加载：已读取`openspec`、`documentation`、`architecture`、`plugin`、`backend-go`、`data-permission`、`cache-consistency`、`testing`和`i18n`规则，并使用`goframe-v2`与`karpathy-guidelines`技能；本段未修改 HTTP API、DTO、SQL、前端、开发工具、插件清单或运行时文案。
- 2.2 影响判断：`i18n`资源无影响；未新增运行时用户可见文案、API 文档源文本、语言包或插件清单字段；数据权限影响为宿主内部继续通过`ScopeService`在数据库查询阶段注入组织/租户过滤，写前可见性和成员关系校验通过显式 membership/assignment 窄接口完成；缓存一致性无新增缓存源、失效通道或跨实例协调分支，tenant/org/plugin 等缓存敏感能力继续复用启动期共享实例；开发工具跨平台无影响；测试策略为后端编译门禁、静态检索、OpenSpec 严格校验和格式检查，不新增 E2E。
- 2.2 验证方式：已运行`cd apps/lina-core && go test ./internal/service/auth ./internal/service/menu ./internal/service/role ./internal/service/user ./internal/service/plugin ./internal/cmd -count=1`通过；已运行`cd apps/lina-core && go test ./internal/service/datascope ./internal/service/session ./internal/service/notify ./internal/service/middleware ./internal/service/auth ./internal/service/menu ./internal/service/role ./internal/service/user ./internal/service/plugin ./internal/cmd -count=1`通过；已运行`cd apps/lina-core && go test ./internal/controller/i18n -count=1`通过；已静态检索确认目标生产 service 中不再存在旧`SetTenantCapability`、`tenantcapsvc.RuntimeService`宽字段或宽构造签名残留，保留的`httpRuntime.tenantSvc`为启动期共享实例持有；已运行本段修改文件的`git diff --check`通过。
- 2.3 实现摘要：已将`middleware`租户中间件测试替身收窄到`tenantMiddlewareService`，删除为旧宽租户接口保留的查询 scope、membership、fallback 和 startup 空方法；已将`plugin`启动一致性与自动开通测试替身分别收窄到`pluginTenantStartupCapability`和`tenantcap.PluginProvisioningService`；已将`notify`租户 fan-out 测试改为直接实现`tenantcap.ScopeService`窄接口；已将`role`租户边界和菜单可分配性测试改为直接实现`roleTenantGovernanceService`，不再通过 provider 宽接口或完整`tenantcap.RuntimeService`构造测试依赖。
- 2.3 宿主测试同步：`role_assignable_menu_test`同步使用新的窄测试 helper，并保留平台上下文判定语义；本段只调整宿主测试替身和测试装配，不修改生产运行时接口、HTTP API、SQL、前端页面、插件清单或源码插件 registrar。
- 2.3 规则加载：已读取`openspec`、`documentation`、`architecture`、`plugin`、`backend-go`、`data-permission`、`cache-consistency`、`testing`和`i18n`规则，并使用`goframe-v2`与`karpathy-guidelines`技能；本段未命中前端、SQL、API 契约或开发工具变更。
- 2.3 影响判断：`i18n`资源无影响；未新增运行时用户可见文案、API 文档源文本、语言包或插件清单字段；数据权限影响为测试替身继续验证数据库侧租户/成员过滤和写前成员校验，不新增数据操作面；缓存一致性无新增缓存源、失效通道或跨实例协调分支；开发工具跨平台无影响；测试策略为后端定向包编译测试、静态检索、OpenSpec 严格校验和格式检查，不新增 E2E。
- 2.3 验证方式：已运行`cd apps/lina-core && go test ./internal/service/middleware ./internal/service/plugin ./internal/service/notify ./internal/service/role -count=1`通过；已静态检索确认本段调整的测试文件不再包含旧宽接口空方法、`tenantcap.RuntimeService`、旧`pkg/tenantcap`、旧`pkg/pluginservice/tenantcap`、`SetTenantCapability`、`RegisterProvider`或`RefreshProviderForPlugin`残留；已运行`openspec validate harden-plugin-capability-boundaries --strict`通过；已运行本段修改文件的`git diff --check`通过。
- 2.4 实现摘要：新增`pkg/plugin/capability/orgcap`和`pkg/plugin/capability/tenantcap`的禁用降级单元测试，直接验证 provider runtime 缺失或 provider 不可用时普通消费面返回空 map、空 slice、零值状态或平台默认值，宿主内部 scope、assignment、membership、startup consistency 和 tenant plugin provisioning 路径保持 no-op 或可控返回，不产生错误。
- 2.4 降级语义：组织能力禁用时，部门、岗位和用户组织投影返回零值，组织数据范围 scope 保持传入 model 不变并以 permissive fallback 报告可继续执行，组织关系写入清理为 no-op；租户能力禁用时，租户 scope 不注入过滤、HTTP 解析回落平台租户、成员/切换/启动一致性/自动开通为 no-op 或空投影；租户能力启用且请求跨租户时通过稳定`TENANT_FORBIDDEN`业务错误拒绝，平台 bypass 仍可访问。
- 2.4 宿主验证：定向回归覆盖`datascope`、`middleware`、`session`、`notify`、`user`、`role`、`plugin`和`pluginhostservices`等已改为窄接口注入的调用方，确认组织和租户能力禁用时继续按既有降级策略执行，不新增 500 分支或孤立服务实例。
- 2.4 规则加载：已读取`openspec`、`documentation`、`architecture`、`plugin`、`backend-go`、`data-permission`、`cache-consistency`、`testing`和`i18n`规则，并使用`goframe-v2`与`karpathy-guidelines`技能；本段未修改 HTTP API、DTO、SQL、前端、开发工具、插件清单或运行时文案。
- 2.4 影响判断：`i18n`资源无影响；未新增运行时用户可见文案、API 文档源文本、语言包或插件清单字段；数据权限影响为验证禁用状态不会放大普通插件能力暴露面，宿主内部查询过滤仍由启用时的`ScopeService`路径负责，禁用时按既有平台/零值降级；缓存一致性无新增缓存源、失效通道、跨实例协调分支或第二套 provider active 状态；开发工具跨平台无影响；测试策略为新增后端单元测试、宿主定向回归、启动装配编译门禁、OpenSpec 严格校验和格式检查，不新增 E2E。
- 2.4 验证方式：已运行`cd apps/lina-core && go test ./pkg/plugin/capability/orgcap ./pkg/plugin/capability/tenantcap -count=1`通过；已运行`cd apps/lina-core && go test ./internal/service/datascope ./internal/service/middleware ./internal/service/session ./internal/service/notify ./internal/service/user ./internal/service/role ./internal/service/plugin ./internal/service/pluginhostservices -count=1`通过；已运行`cd apps/lina-core && go test ./internal/cmd -count=1`通过；已运行`openspec validate harden-plugin-capability-boundaries --strict`通过；已运行本段新增测试文件和`tasks.md`的`git diff --check`通过。
- 3.1 实现摘要：`linapro-org-core`的 provider factory 已使用`func(context.Context, orgcap.ProviderEnv) (orgcap.Provider, error)`强类型签名，并直接通过`env.TenantFilter`构造`backend/internal/provider/orgcapadapter`；新增`backend/plugin_provider_test.go`验证强类型环境可构造 provider，且缺少`TenantFilter`时返回错误，不回退到旧`Services any`或完整能力目录断言。
- 3.1 边界确认：业务逻辑继续保留在`backend/internal/service/dept`、`backend/internal/service/post`和`backend/internal/provider/orgcapadapter`；provider adapter 只依赖插件自身`internal/dao`、`internal/model`、`internal/service`和宿主公共`pkg/plugin/capability`契约，未导入宿主`lina-core/internal/**`；路由注册中仍存在`registrar.HostServices()`调用，属于后续 4.x `pluginhost`边界收敛任务范围，本段未提前迁移。
- 3.1 规则加载：已读取`openspec`、`documentation`、`architecture`、`plugin`、`backend-go`、`data-permission`、`cache-consistency`、`testing`和`i18n`规则，并使用`goframe-v2`与`karpathy-guidelines`技能；本段未修改 HTTP API、DTO、SQL、前端、开发工具、插件清单或运行时文案。
- 3.1 影响判断：`i18n`资源无影响；数据权限影响为组织 provider 继续通过受治理`TenantFilterService`对插件自有表执行租户过滤，不向普通 capability 消费面暴露`*gdb.Model`或写入能力；缓存一致性无新增缓存源、失效通道、跨实例协调分支或 provider active 状态；开发工具跨平台无影响；测试策略为源码插件后端单元测试、静态检索、OpenSpec 严格校验和格式检查，不新增 E2E。
- 3.1 验证方式：已运行`cd apps/lina-plugins/linapro-org-core && GOWORK=/Users/john/Workspace/github/linaproai/linapro/temp/go.work.plugins go test ./backend ./backend/internal/provider/orgcapadapter -count=1`通过；已静态检索确认`apps/lina-plugins/linapro-org-core/backend`中不存在`env.Services`、`ProviderEnv.Services`、`contract.ProviderEnv`、`Services any`、provider 侧`capability.Directory`或`pluginhost.HostServices`残留；已运行本段新增测试文件的`git diff --check`通过。
- 3.2 实现摘要：`linapro-tenant-core`的 provider factory 已使用`func(context.Context, tenantcap.ProviderEnv) (tenantcap.Provider, error)`强类型签名，并直接通过`env.BizCtx`和`env.PluginLifecycle`构造`backend/internal/provider/tenantadapter`；新增`backend/plugin_provider_test.go`验证强类型环境可构造 provider，且缺少`BizCtx`时返回错误，不回退到旧`Services any`或完整能力目录断言。
- 3.2 边界确认：业务逻辑继续保留在`backend/internal/service/`和`backend/internal/provider/tenantadapter`下；provider adapter 只依赖插件自身`internal`服务和宿主公共`pkg/plugin/capability`契约，未导入宿主`lina-core/internal/**`；路由注册中仍存在`registrar.HostServices()`调用，属于后续 4.x `pluginhost`边界收敛任务范围，本段未提前迁移。
- 3.2 规则加载：已读取`openspec`、`documentation`、`architecture`、`plugin`、`backend-go`、`data-permission`、`cache-consistency`、`testing`和`i18n`规则，并使用`goframe-v2`与`karpathy-guidelines`技能；本段未修改 HTTP API、DTO、SQL、前端、开发工具、插件清单或运行时文案。
- 3.2 影响判断：`i18n`资源无影响；数据权限影响为租户 provider 继续通过 DTO、投影和宿主内部窄接口承载租户治理，不向普通 capability 消费面暴露`*gdb.Model`、`*ghttp.Request`或宿主写入面；缓存一致性无新增缓存源、失效通道、跨实例协调分支或 provider active 状态；开发工具跨平台无影响；测试策略为源码插件后端单元测试、静态检索、OpenSpec 严格校验和格式检查，不新增 E2E。
- 3.2 验证方式：已运行`cd apps/lina-plugins/linapro-tenant-core && GOWORK=/Users/john/Workspace/github/linaproai/linapro/temp/go.work.plugins go test ./backend ./backend/internal/provider/tenantadapter -count=1`通过；已静态检索确认`apps/lina-plugins/linapro-tenant-core/backend`中不存在`env.Services`、`ProviderEnv.Services`、`contract.ProviderEnv`、`Services any`、provider 侧`capability.Directory`或`pluginhost.HostServices`残留；已运行本段新增测试文件和`tasks.md`的`git diff --check`通过。
- 3.3 实现摘要：已在`pluginhost`公共注册器和 hook payload 上新增`Services()`访问器与`pluginhost.Services`命名接口，官方源码插件改为通过`registrar.Services()`或`payload.Services()`获取宿主能力目录；`linapro-content-notice`、`linapro-demo-source`、`linapro-monitor-loginlog`、`linapro-monitor-online`、`linapro-monitor-operlog`、`linapro-monitor-server`、`linapro-ops-demo-guard`、`linapro-org-core`和`linapro-tenant-core`的源码插件入口均不再调用`HostServices()`。
- 3.3 测试替身：`linapro-monitor-server`的 cron cleanup helper 已收窄为显式接收`pluginhost.Services`和 primary-node 布尔值，单元测试替身不再实现旧`HostServices()`方法，也不再引用`pluginhost.HostServices`类型名。
- 3.3 过渡边界：本段仅迁移官方源码插件调用方；`pluginhost.HostServices`别名、registrar/payload 旧`HostServices()`方法和宿主内部过渡实现仍保留，属于 4.1、4.2 和 4.3 的删除范围，未提前扩大到公共契约大删除。
- 3.3 规则加载：已读取`openspec`、`documentation`、`architecture`、`plugin`、`backend-go`、`data-permission`、`cache-consistency`、`testing`和`i18n`规则，并使用`goframe-v2`与`karpathy-guidelines`技能；本段未修改 HTTP API、SQL、前端、开发工具、插件清单、语言包或运行时用户可见文案。
- 3.3 影响判断：`i18n`资源无影响；数据权限影响为源码插件继续只通过受治理`TenantFilterService`对插件自有表执行数据库侧租户过滤，不向普通`capability.Directory`暴露`*gdb.Model`；缓存一致性无新增缓存源、失效通道、跨实例协调分支或孤立服务实例，插件能力目录继续由宿主运行期共享实例注入；开发工具跨平台无影响；测试策略为后端编译门禁、官方源码插件 smoke、静态检索、OpenSpec 严格校验和格式检查，不新增 E2E。
- 3.3 验证方式：已运行`cd apps/lina-core && go test ./pkg/plugin/pluginhost ./internal/service/plugin/internal/integration -count=1`通过；已使用`GOWORK=/Users/john/Workspace/github/linaproai/linapro/temp/go.work.plugins`分别运行`linapro-content-notice`、`linapro-demo-source`、`linapro-monitor-loginlog`、`linapro-monitor-online`、`linapro-monitor-operlog`、`linapro-monitor-server`、`linapro-ops-demo-guard`、`linapro-org-core`和`linapro-tenant-core`的`go test ./backend -count=1`通过；已静态检索确认`apps/lina-plugins/**/*.go`中不存在`HostServices()`、`pluginhost.HostServices`或`HostServicesForPlugin`残留。
- 3.4 实现摘要：已使用既有`temp/go.work.plugins`覆盖 9 个官方源码插件后端编译 smoke，范围为`linapro-content-notice`、`linapro-demo-source`、`linapro-monitor-loginlog`、`linapro-monitor-online`、`linapro-monitor-operlog`、`linapro-monitor-server`、`linapro-ops-demo-guard`、`linapro-org-core`和`linapro-tenant-core`；`linapro-demo-dynamic`为动态插件，按任务拆分留到后续 WASI 构建 smoke 覆盖。
- 3.4 边界确认：已静态检索`apps/lina-plugins/**/*.go`确认不存在对宿主`lina-core/internal/**`的直接导入；已对每个官方源码插件执行`go list -deps ./backend/...`并过滤`lina-core/internal/service`、`lina-core/internal/dao`和`lina-core/internal/model`，未发现依赖图残留，源码插件后端只能通过宿主公共`pkg/plugin/capability`、`pkg/plugin/pluginhost`等稳定契约访问宿主能力。
- 3.4 规则加载：已读取`openspec`、`documentation`、`architecture`、`plugin`、`backend-go`、`data-permission`、`cache-consistency`、`testing`、`i18n`和`dev-tooling`规则，并使用`goframe-v2`技能；本段为编译与治理验证任务，未修改 Go 生产代码、HTTP API、SQL、前端、开发工具、插件清单、语言包或运行时用户可见文案。
- 3.4 影响判断：`i18n`资源无影响；数据权限无新增数据操作面，验证结论是源码插件未绕过宿主公共契约直接依赖宿主内部 service、DAO、DO 或 Entity；缓存一致性无新增缓存源、失效通道、跨实例协调分支或第二套 provider active 状态；开发工具跨平台无实现变更，本段仅复用 Go workspace 和 Go 工具链运行 smoke；测试策略为源码插件后端编译门禁、依赖图静态检索、OpenSpec 严格校验和格式检查，不新增 E2E。
- 3.4 验证方式：已分别运行`GOWORK=/Users/john/Workspace/github/linaproai/linapro/temp/go.work.plugins go test ./backend/... -count=1`覆盖 9 个官方源码插件后端且全部通过；已运行`rg -n '"lina-core/internal/(service|dao|model)(/|")' apps/lina-plugins --glob '*.go'`和`rg -n '"lina-core/internal/' apps/lina-plugins --glob '*.go'`未发现匹配；已对 9 个官方源码插件分别运行`GOWORK=/Users/john/Workspace/github/linaproai/linapro/temp/go.work.plugins go list -deps ./backend/... | rg '^lina-core/internal/(service|dao|model)(/|$)'`未发现匹配。
- 4.1 实现摘要：已删除`pkg/plugin/pluginhost`中的`HostServices`类型别名、`ScopedHostServicesFactory`、`HostServicesForPlugin` helper 和`pluginhost_scoped_services.go`；HTTP registrar、Cron registrar、hook payload 和 managed cron collector 均只保留`Services()`并返回`pluginhost.Services`，不再发布旧`HostServices()`访问器。
- 4.1 宿主迁移：`internal/service/pluginhostservices`和`internal/service/plugin/internal/integration`改为使用`pluginhost.Services`与`capability.ServicesForPlugin`完成源码插件 scoped 能力绑定；测试 fixture 与 auth runtime 参数测试同步改用`Services()`和`pluginhost.Services`，未修改动态插件 manifest 的`hostServices`授权字段语义。
- 4.1 规则加载：已读取`openspec`、`documentation`、`architecture`、`plugin`、`backend-go`、`data-permission`、`cache-consistency`、`testing`、`i18n`和`dev-tooling`规则，并使用`goframe-v2`技能；本段未修改 HTTP API、SQL、前端、开发工具、插件清单、语言包或运行时用户可见文案。
- 4.1 影响判断：`i18n`资源无影响；数据权限无新增数据操作面，源码插件仍只能通过受治理`TenantFilterService`访问插件自有表过滤能力，普通`capability.Directory`不暴露数据库模型；缓存一致性无新增缓存源、失效通道、跨实例协调分支或孤立服务实例，能力目录继续复用启动期共享实例；开发工具跨平台无影响；测试策略为后端定向编译门禁、旧入口静态检索、OpenSpec 严格校验和格式检查，不新增 E2E。
- 4.1 验证方式：已运行`cd apps/lina-core && go test ./pkg/plugin/pluginhost ./internal/service/pluginhostservices ./internal/service/plugin/internal/integration ./internal/service/auth -count=1`通过；已运行`rg -n "pluginhost\\.HostServices|HostServicesForPlugin|ScopedHostServicesFactory|\\.HostServices\\(" apps/lina-core apps/lina-plugins --glob '*.go'`无匹配；已运行本段修改文件的`git diff --check`通过。
- 4.2 实现摘要：`pluginhost.HTTPRegistrar`、`CronRegistrar`和`HookPayload`公开消费面统一只暴露`Services()`；hook payload 构造器改为`NewHookPayloadWithServices`；宿主插件 facade 和 integration 装配 setter 改为`SetCapabilities`，内部字段改为`capabilities`；官方源码插件入口局部变量同步从`hostServices`收敛为`capabilities`。
- 4.2 lifecycle 判断：源码插件 lifecycle input 当前只承载操作、租户、版本、安装模式和清理策略快照，不暴露能力目录或旧`HostServices`访问器，因此本段无 lifecycle 能力目录迁移面；保持 lifecycle 输入不注入 capability directory，避免为生命周期回调扩大依赖面。
- 4.2 规则加载：已读取`openspec`、`documentation`、`architecture`、`plugin`、`backend-go`、`data-permission`、`cache-consistency`、`testing`、`i18n`和`dev-tooling`规则，并使用`goframe-v2`技能；本段未修改 HTTP API、SQL、前端、开发工具、插件清单、语言包或运行时用户可见文案。
- 4.2 影响判断：`i18n`资源无影响；数据权限无新增数据操作面，源码插件能力目录仍只通过`pluginhost.Services`暴露受治理源码插件能力，普通目录不暴露`TenantFilter()`；缓存一致性无新增缓存源、失效通道、跨实例协调分支或孤立服务实例，启动期共享 capability directory 继续注入 plugin facade、integration 和 WASM framework host service；开发工具跨平台无影响；测试策略为后端定向编译门禁、官方源码插件后端 smoke、旧命名静态检索、OpenSpec 严格校验和格式检查，不新增 E2E。
- 4.2 验证方式：已运行`cd apps/lina-core && go test ./pkg/plugin/pluginhost ./internal/service/pluginhostservices ./internal/service/plugin/internal/integration ./internal/service/plugin ./internal/service/auth ./internal/cmd -count=1`通过；已使用`GOWORK=/Users/john/Workspace/github/linaproai/linapro/temp/go.work.plugins`分别运行 9 个官方源码插件`go test ./backend -count=1`通过；已运行`rg -n "SetHostServices|pluginhost\\.HostServices|HostServicesForPlugin|ScopedHostServicesFactory|\\.HostServices\\(|NewHookPayloadWithServices|hostServices := .*Capabilities|hostServices = .*Capabilities" apps/lina-core apps/lina-plugins --glob '*.go'`无匹配；已运行本段修改文件的`git diff --check`通过。
- 4.3 实现摘要：复核`internal/service/plugin/internal/integration`源码插件 route、cron、hook 和 managed cron 流程，确认四条路径均通过`sourceServicesForPlugin`调用`capability.ServicesForPlugin`后再传给`HTTPRegistrar`、`CronRegistrar`、`HookPayload`或`managedCronCollector`；新增`extensions_capability_scope_test.go`，用可记录的`capability.ScopedServicesFactory`覆盖 route、cron、hook 和 managed cron 四条公开流程，验证 source plugin 回调只能收到按当前`pluginID`绑定后的`pluginhost.Services`。
- 4.3 规则加载：已读取`openspec`、`documentation`、`architecture`、`plugin`、`backend-go`、`data-permission`、`cache-consistency`、`testing`、`i18n`和`dev-tooling`规则，并使用`goframe-v2`与`karpathy-guidelines`技能；本段未修改生产 Go 逻辑、HTTP API、SQL、前端、开发工具、插件清单、语言包或运行时用户可见文案。
- 4.3 影响判断：`i18n`资源无影响；数据权限无新增数据操作面，源码插件 scoped 能力仍只暴露`pluginhost.Services`，普通`capability.Directory`不恢复数据库模型或宿主内部治理对象；缓存一致性无新增缓存源、失效通道、跨实例协调分支或第二套 provider active 状态，能力目录继续由启动期共享实例注入；开发工具跨平台无影响；测试策略为新增后端回归测试、定向包编译门禁、旧入口静态检索、OpenSpec 严格校验和格式检查，不新增 E2E。
- 4.3 验证方式：已运行`cd apps/lina-core && go test ./internal/service/plugin/internal/integration -run TestSourcePluginCallbacksUsePluginScopedServices -count=1`通过；已运行`cd apps/lina-core && go test ./internal/service/plugin/internal/integration ./internal/service/pluginhostservices ./internal/service/plugin -count=1`通过；已运行`rg -n "pluginhost\\.HostServices|HostServicesForPlugin|ScopedHostServicesFactory|\\.HostServices\\(|NewHookPayloadWithServices|SetHostServices" apps/lina-core/internal/service/plugin/internal/integration apps/lina-core/pkg/plugin/pluginhost apps/lina-core/internal/service/pluginhostservices --glob '*.go'`无匹配；已静态检索确认`NewHTTPRegistrar`、`NewCronRegistrar`、`NewHookPayloadWithServices`和`managedCronCollector`调用点均经`sourceServicesForPlugin`获取 scoped 能力。
- 5.1 实现摘要：已删除`pkg/plugin/pluginbridge`根包中的非 WASI 和 WASI guest 业务能力 facade 文件，根包不再转发`capability/guest`的`Runtime()`、`Storage()`、`HTTP()`、`Data()`、`Cache()`、`Lock()`、`Config()`、`Notify()`、`Cron()`、`HostConfig()`、`Manifest()`等业务 client，也不再发布对应 guest 业务 client 类型别名；同步删除只验证旧根包 guest facade 的测试。
- 5.1 边界保留：`pluginbridge`根包仍保留 ABI、bridge envelope、codec、WASM section、host call、host service wire DTO/常量/编解码/校验和动态路由 dispatcher 等协议与 transport 入口；动态插件业务能力 client 继续由`pkg/plugin/capability/guest`发布，未移动或删除 guest SDK 本体。
- 5.1 规则加载：已读取`openspec`、`documentation`、`architecture`、`plugin`、`backend-go`、`data-permission`、`cache-consistency`、`testing`、`i18n`和`dev-tooling`规则，并使用`goframe-v2`与`karpathy-guidelines`技能；本段未修改 HTTP API、SQL、前端、开发工具、插件清单、语言包或运行时用户可见文案。
- 5.1 影响判断：`i18n`资源无影响；数据权限无新增数据操作面，变更仅删除误导性的根包业务 client facade，不改变受治理 host service 授权与数据访问协议；缓存一致性无新增缓存源、失效通道、跨实例协调分支或本地 fallback；开发工具跨平台无实现变更；测试策略为后端定向编译门禁、旧入口静态检索、OpenSpec 严格校验和格式检查，不新增 E2E。
- 5.1 验证方式：已运行`cd apps/lina-core && go test ./pkg/plugin/pluginbridge ./pkg/plugin/capability/guest -count=1`通过；已运行`cd apps/lina-core && go test ./pkg/plugin/... -run 'TestFacade|TestGuest|TestHostService|TestHostCall|TestBridge|TestWasm|TestRuntime' -count=1`通过；已运行`rg -n "pluginbridge\\.(Runtime|Storage|HTTP|Data|Cache|Lock|Config|Notify|Cron|HostConfig|Manifest|HostLog|HostState|HostDB)\\(" apps/lina-core apps/lina-plugins hack/tools/linactl --glob '*.go'`无匹配；已运行`rg -n "\\b(Runtime|Storage|HTTP|Data|Cache|Lock|Config|Notify|Cron|HostConfig|Manifest)\\s*=\\s*capabilityguest\\.|\\b(RuntimeHostService|StorageHostService|HTTPHostService|DataHostService|CacheHostService|LockHostService|ConfigHostService|NotifyHostService|CronHostService|HostConfigHostService|ManifestHostService|HostDBQueryResult|DataListResult|DataGetResult|DataMutationResult|DataTransactionInput|DataTransactionResult)\\s*=\\s*capabilityguest\\." apps/lina-core/pkg/plugin/pluginbridge --glob '*.go'`无匹配。
- 5.2 实现摘要：已确认动态插件业务 host-service client 的包级入口和目录入口位于`pkg/plugin/capability/guest`；补齐`guest`侧动态业务常量和 cron 合约值的导出面，使动态插件业务 service 不再从`pluginbridge`根包引用`HostService*`业务 DTO、`CronContract`或运行时日志级别；`linapro-demo-dynamic`业务 service 已改为通过`capability/guest`类型和常量表达 runtime、storage、network、cron 等业务能力依赖。
- 5.2 文档与样例：已更新`linapro-demo-dynamic`中英文说明和`apps/lina-plugins`中英文工作区说明，明确动态插件业务 client 从`lina-core/pkg/plugin/capability/guest`导入，`pluginbridge`根包仅用于协议 envelope、路由分发、生命周期契约、cron 契约和响应 helper；桥接文件仍保留对`pluginbridge`协议层入口的使用。
- 5.2 测试覆盖：已新增`TestGuestBusinessConstantsMatchBridgeProtocol`，验证`capability/guest`业务常量与底层 bridge 协议值保持 wire-compatible；已有 guest directory 测试继续验证业务能力 client 由`capability/guest`发布。
- 5.2 规则加载：已读取`openspec`、`documentation`、Markdown 格式、`architecture`、`plugin`、`backend-go`、`data-permission`、`cache-consistency`、`testing`、`i18n`和`dev-tooling`规则，并使用`goframe-v2`与`karpathy-guidelines`技能；本段未修改 HTTP API、SQL、前端、插件清单、语言包或构建工具实现。
- 5.2 影响判断：`i18n`资源无影响，文档说明同步维护中英文镜像且未新增运行时用户可见文案；数据权限无新增数据操作面，动态 data 访问仍通过受治理`capability/data`和 host service 授权快照；缓存一致性无新增缓存源、失效通道、跨实例协调分支或孤立服务实例；开发工具跨平台无实现变更；测试策略为 guest SDK 单元测试、动态样例后端编译 smoke、旧入口静态检索、OpenSpec 严格校验和格式检查，不新增 E2E。
- 5.2 验证方式：已运行`cd apps/lina-core && go test ./pkg/plugin/capability/guest -count=1`通过；已运行`cd apps/lina-plugins/linapro-demo-dynamic && GOWORK=/Users/john/Workspace/github/linaproai/linapro/temp/go.work.plugins go test ./backend/... -count=1`通过；已运行`cd apps/lina-core && go test ./pkg/plugin/... -run 'TestDefaultDirectoryReturnsCapabilityClients|TestGuestCapabilityContractsUseInterfaces|TestGuestBusinessConstantsMatchBridgeProtocol|TestFacadeHostServicePayloadMatchesSubcomponent|TestFacadeRequestEnvelopeMatchesCodec' -count=1`通过；已运行`rg -n "pluginbridge\\.(Runtime|Storage|HTTP|Data|Cache|Lock|Config|Notify|Cron|HostConfig|Manifest|Org|Tenant)\\(" apps/lina-core apps/lina-plugins hack/tools/linactl --glob '*.go'`无匹配；已运行`rg -n "pluginbridge\\.HostService|pluginbridge\\.CronContract|pluginbridge\\.LogLevel|pluginbridge\\.DefaultCron|pluginbridge\\.CronScope|pluginbridge\\.CronConcurrency" apps/lina-plugins/linapro-demo-dynamic/backend/internal apps/lina-plugins/linapro-demo-dynamic/backend/plugin.go apps/lina-plugins/linapro-demo-dynamic/main.go --glob '*.go'`无匹配；已运行本段修改文件的`git diff --check`通过；已运行`openspec validate harden-plugin-capability-boundaries --strict`通过。
- 5.3 实现摘要：已确认`pkg/plugin/pluginbridge`根包继续保留 ABI、bridge envelope、codec、host call、host service wire DTO/常量/编解码/授权校验、WASM custom section 和动态路由 dispatcher 等协议与 transport 必需入口；将根包注释从“兼容”表述收敛为“root protocol facade”，避免误导为旧业务 client 兼容层。
- 5.3 测试覆盖：新增`TestRootFacadeKeepsProtocolBoundary`，通过 AST 检查阻止`pluginbridge`根包导入`pkg/plugin/capability/guest`、重新发布`Runtime()`/`Storage()`/`Data()`等业务 client 函数，或恢复`RuntimeHostService`等`capability/guest`业务 client 类型别名；既有 facade 测试继续覆盖 request envelope、WASM section 和 host-service payload 与底层协议子组件保持等价。
- 5.3 规则加载：已读取`openspec`、`documentation`、`architecture`、`plugin`、`backend-go`、`data-permission`、`cache-consistency`、`testing`、`i18n`和`dev-tooling`规则，并使用`goframe-v2`与`karpathy-guidelines`技能；本段未修改 HTTP API、SQL、前端、插件清单、语言包或开发工具实现。
- 5.3 影响判断：`i18n`资源无影响；数据权限无新增数据操作面，保留的是受治理 host service wire 协议入口和授权校验，不新增 raw SQL、DAO、DO、Entity 或数据库模型暴露；缓存一致性无新增缓存源、失效通道、跨实例协调分支或孤立服务实例；开发工具跨平台无实现变更；测试策略为后端单元测试、协议入口静态检索、OpenSpec 严格校验和格式检查，不新增 E2E。
- 5.3 验证方式：已运行`cd apps/lina-core && go test ./pkg/plugin/pluginbridge -count=1`通过；已运行`rg -n "\"lina-core/pkg/plugin/capability/guest\"|func (Runtime|Storage|HTTP|Data|Cache|Lock|Config|Notify|Cron|HostConfig|Manifest|Org|Tenant)\\s*\\(|\\b(Runtime|Storage|HTTP|Data|Cache|Lock|Config|Notify|Cron|HostConfig|Manifest|Org|Tenant)\\s*=\\s*capabilityguest\\.|\\b(RuntimeHostService|StorageHostService|HTTPHostService|DataHostService|CacheHostService|LockHostService|ConfigHostService|NotifyHostService|CronHostService|HostConfigHostService|ManifestHostService|OrgService|TenantService)\\s*=\\s*capabilityguest\\." apps/lina-core/pkg/plugin/pluginbridge --glob '*.go' --glob '!*_test.go'`无匹配；已运行`rg -n "pluginbridge\\.(Runtime|Storage|HTTP|Data|Cache|Lock|Config|Notify|Cron|HostConfig|Manifest|Org|Tenant)\\(" apps/lina-core apps/lina-plugins hack/tools/linactl --glob '*.go' --glob '!*_test.go'`无匹配。
- 5.4 实现摘要：`hack/tools/linactl/internal/wasmbuilder`生成的动态 WASM dispatcher 已使用`lina-core/pkg/plugin/pluginbridge`协议 facade，不再引用旧`lina-core/pkg/pluginbridge`路径；官方动态样例的业务 service 继续通过`lina-core/pkg/plugin/capability/guest`使用 runtime、storage、network、cron 等动态业务 host-service client，桥接入口只保留协议 envelope、生命周期和 route dispatcher 语义。
- 5.4 生成代码边界：已扩展`TestPluginDemoDynamicGeneratedDispatcherIsZeroReflection`，断言生成的`zz_generated_wasm_dispatcher.go`必须导入新的`pkg/plugin/pluginbridge`协议包，不得导入`pkg/plugin/capability/guest`或`capabilityguest`，也不得调用`pluginbridge.Runtime()`、`Storage()`、`Data()`、`Config()`等旧根包业务 client；动态业务能力入口只允许出现在插件业务 service 中。
- 5.4 规则加载：已读取`openspec`、`documentation`、`architecture`、`plugin`、`backend-go`、`dev-tooling`、`testing`、`data-permission`、`cache-consistency`和`i18n`规则，并使用`goframe-v2`、`karpathy-guidelines`和`lina-review`技能；本段未修改 HTTP API、SQL、前端、插件清单、语言包或运行时用户可见文案。
- 5.4 影响判断：`i18n`资源无影响；数据权限无新增数据操作面，动态 data 访问仍通过 host service 授权协议和`capability/guest`业务 client；缓存一致性无新增缓存源、失效通道、跨实例协调分支或孤立服务实例；开发工具跨平台影响限于 Go 实现的`linactl` wasmbuilder 和 Go 单元测试，未新增平台专属 shell 依赖；测试策略为 wasmbuilder 单元测试、动态插件 WASI artifact smoke、linactl 全包测试、静态扫描、OpenSpec 严格校验和格式检查，不新增 E2E。
- 5.4 验证方式：已运行`cd hack/tools/linactl && go test ./internal/wasmbuilder -run 'TestPluginDemoDynamicGeneratedDispatcherIsZeroReflection|TestPluginDemoDynamicRuntimeArtifactEmbedsReviewedAssets' -count=1`通过，覆盖生成 dispatcher 边界和官方动态插件 WASI artifact 构建 smoke；已运行`cd hack/tools/linactl && go test ./... -count=1`通过；已运行`rg -n "lina-core/pkg/pluginbridge|pkg/pluginbridge" hack/tools/linactl/internal/wasmbuilder apps/lina-plugins/linapro-demo-dynamic --glob '*.go' --glob '!*_test.go'`无生产匹配；已运行`rg -n "pluginbridge\\.(Runtime|Storage|HTTP|Data|Cache|Lock|Config|Notify|Cron|HostConfig|Manifest|Org|Tenant)\\(" hack/tools/linactl/internal/wasmbuilder apps/lina-plugins/linapro-demo-dynamic --glob '*.go' --glob '!*_test.go'`无生产匹配；已运行`rg -n "lina-core/pkg/plugin/capability/guest|capabilityguest" hack/tools/linactl/internal/wasmbuilder apps/lina-plugins/linapro-demo-dynamic --glob '*.go' --glob '!*_test.go'`确认匹配仅在动态样例业务 service 中；已运行`openspec validate harden-plugin-capability-boundaries --strict`通过；已运行本段修改文件的`git diff --check`通过。
- 6.1 数据权限确认：`user.List`先通过`tenantcap.ScopeService.ApplyUserTenantScope`和`datascope.Service.ApplyUserScope`把租户成员关系与角色数据范围注入`sys_user`查询模型，再执行`Count`、分页和投影装配；`role`授权用户列表通过共享`datascope.Service.ApplyUserScope`过滤候选用户；`notify`发布通知收件人通过`tenantcap.ScopeService.ApplyUserTenantScope`在数据库侧约束有效租户成员；插件`datahost`在授权资源模型上调用`applyResourceDataScope`后再查询或写入目标表。
- 6.2 契约边界确认：普通`orgcap.Service`和`tenantcap.Service`只暴露状态、DTO、值对象、只读投影和批量查询；`*gdb.Model`、`*ghttp.Request`、写入、启动一致性和数据范围注入仅留在 provider-facing 或宿主内部`ScopeService`、`RequestResolver`、`UserMembershipService`等窄接口中。动态 data capability 的 guest 面通过`DataQueryPlan`、分页、字段投影和 host-service request/response 结构传递数据，不返回 DAO、DO、Entity、`*gdb.Model`或 raw SQL。
- 6.3 高频路径确认：用户列表对组织、租户和角色信息使用`ListUserDeptAssignments(ctx, userIds)`、`ListUserTenantProjections(ctx, userIds)`、`WhereIn(userIds)`和批量角色查询完成当前页装配；角色授权候选使用`WhereIn`、`Count`和`Limit`完成数据库侧分页过滤；通知收件人先用数据库查询取有效用户集合再批量写入 delivery；插件`datahost.ExecuteList`先做授权计划、字段投影、`Count`、`Page`和`Order`后返回结构化记录，未引入前端瀑布式补查。
- 6.4 provider 状态确认：`orgcap`和`tenantcap`的`Available`/`Status`仍通过 capability registry 的 active provider 与`ProviderRuntime.IsProviderEnabled`判断 provider 可用性；`internal/service/plugin/internal/integration`继续以共享`enabledSnapshot`、`RefreshEnabledSnapshot`、`runtime revision`和 catalog runtime upgrade state 作为权威运行时状态，没有新增第二套 provider active 标记或绕过启动期共享实例的本地缓存。
- 6.x 规则加载：已读取`openspec`、`documentation`、`architecture`、`plugin`、`backend-go`、`data-permission`、`cache-consistency`和`testing`规则，并使用`goframe-v2`技能；本段只更新 OpenSpec 任务记录，不修改生产 Go 代码、HTTP API、SQL、前端、开发工具、插件清单、语言包或运行时用户可见文案。
- 6.x 影响判断：`i18n`资源无影响；数据权限影响为确认既有宿主内部窄接口仍在数据库查询阶段过滤，不扩大普通 capability 暴露面；缓存一致性无新增缓存源、失效通道、跨实例协调分支或第二套 provider active 状态；开发工具跨平台无影响；测试策略为静态治理检索、关键代码审查、OpenSpec 严格校验和格式检查，不新增 E2E。
- 6.x 验证方式：已静态检索`ScopeService`、`ApplyUserTenantScope`、`ApplyUserDeptScope`、`ListUserTenantProjections`、`ListUserDeptAssignments`、`datahost`分页投影和 provider enabled snapshot 路径；已运行`rg -n "pluginbridge\\.(Runtime|Storage|HTTP|Data|Cache|Lock|Config|Notify|Cron|HostConfig|Manifest|Org|Tenant)\\(|ProviderEnv\\.Services|HostServicesForPlugin|pluginhost\\.HostServices" apps/lina-core apps/lina-plugins hack/tools/linactl --glob '*.go' --glob '!*_test.go'`确认无生产匹配；已运行`rg -n "\\*gdb\\.Model|\\*ghttp\\.Request|internal/dao|internal/model/(do|entity)|\\bdao\\.|\\bRaw\\(|raw sql|Raw SQL" apps/lina-core/pkg/plugin/capability/guest apps/lina-core/pkg/plugin/capability/data apps/lina-core/pkg/plugin/pluginbridge --glob '*.go' --glob '!*_test.go'`确认普通动态 guest 与 bridge 生产面无底层模型或 raw SQL 暴露。
- 7.1 实现摘要：新增跨平台 Go 治理扫描，并将扫描纳入`linactl test.scripts`仓库工具 smoke；扫描使用 Go AST 和标准库文件遍历，不依赖 Shell、`rg`或平台专属路径语义。扫描覆盖`apps/lina-core/pkg/plugin/**`反向导入宿主`internal/service/plugin/**`、`apps/lina-plugins/**`导入宿主`lina-core/internal/**`、`apps/lina-core/**`/`apps/lina-plugins/**`/`hack/tools/linactl/**`生产 Go 代码导入旧`pkg/pluginbridge`/`pkg/pluginhost`/`pkg/pluginservice`等历史包、引用旧`pluginhost.HostServices`/`HostServicesForPlugin`/`HostServices()`、`ProviderEnv.Services`和`pluginbridge`根包业务能力 client/type alias。
- 7.1 测试覆盖：新增`internal/repository`单元测试，使用临时仓库构造负例验证公共插件包反向导入、官方插件导入宿主内部包、旧`pluginbridge.Runtime()`、旧`pluginhost.HostServices`、旧`HostServices()`、旧`ProviderEnv.Services`和插件引用宿主内部`orgcap.ScopeService`/`tenantcap.RequestResolver`会被阻断；另增加正例确保动态插件`plugin.yaml`语义中的`HostServices`授权快照字段不会被误判为旧访问器。
- 7.1 规则加载：已读取`openspec`、`documentation`、`architecture`、`plugin`、`backend-go`、`dev-tooling`、`testing`、`data-permission`、`cache-consistency`和`i18n`规则，并使用`goframe-v2`与`karpathy-guidelines`技能；本段修改`hack/tools/linactl`开发工具 Go 代码、仓库治理测试和 OpenSpec 任务记录，不修改 HTTP API、SQL、前端、插件清单、语言包或运行时用户可见文案。
- 7.1 影响判断：`i18n`资源无影响；数据权限无新增数据操作面，治理扫描用于防止插件绕过公共能力契约直接访问宿主内部数据权限或 DAO/模型；缓存一致性无新增缓存源、失效通道、跨实例协调分支或孤立服务实例；开发工具跨平台影响为新增 Go 实现的`test.scripts`内部治理检查，默认开发路径未新增平台专属脚本；测试策略为治理扫描单元测试、工具 smoke 回归、格式检查和任务级审查，不新增 E2E。
- 7.1 验证方式：已运行`cd hack/tools/linactl && go test ./internal/repository -count=1`通过；已运行`cd hack/tools/linactl && go test . ./internal/repository -count=1`通过；已运行`cd hack/tools/linactl && go test . -run 'TestCommandRegistry|TestValidateRepositoryTooling|TestValidateLinactlCommandFiles' -count=1`通过；已运行本段修改文件的`git diff --check`通过。
- 7.2 执行摘要：已运行公共插件契约和动态 guest SDK 全包测试，覆盖`pkg/plugin/capability`、`capability/guest`、`pluginbridge`协议 facade、bridge 子组件和`pluginhost`源码插件贡献契约；本段未修改生产 Go 代码，仅更新任务执行记录。
- 7.2 规则加载：已读取`openspec`、`documentation`、`architecture`、`plugin`、`backend-go`、`api-contract`、`data-permission`、`cache-consistency`、`testing`、`i18n`和`dev-tooling`规则，并使用`goframe-v2`与`karpathy-guidelines`技能。
- 7.2 影响判断：`i18n`资源无影响；数据权限无新增数据操作面，本段验证普通 capability、动态 guest 和 bridge 边界不暴露宿主内部数据库模型或写入接口；缓存一致性无新增缓存源、失效通道、跨实例协调分支或孤立服务实例；开发工具跨平台无新增实现影响；测试策略为后端 Go 编译门禁和公共插件契约单元测试，不新增 E2E。
- 7.2 验证方式：已运行`cd apps/lina-core && go test ./pkg/plugin/... -count=1`通过，覆盖公共插件契约和动态 guest SDK。
- 7.3 执行摘要：已运行宿主插件适配与运行时测试，覆盖`pluginhostservices`能力目录装配、`internal/service/plugin`运行时治理、catalog、datahost、dependency、frontend、integration、lifecycle、openapi、runtime、sourceupgrade、testutil和wasm子包；本段未修改生产 Go 代码，仅更新任务执行记录。
- 7.3 规则加载：已读取`openspec`、`architecture`、`plugin`、`backend-go`、`data-permission`、`cache-consistency`、`dev-tooling`、`testing`和`i18n`规则，并使用`goframe-v2`与`karpathy-guidelines`技能。
- 7.3 影响判断：`i18n`资源无影响；数据权限无新增数据操作面，本段验证宿主插件适配和运行时包仍通过窄接口、授权快照和数据库侧治理路径工作；缓存一致性无新增缓存源、失效通道、跨实例协调分支或孤立服务实例；开发工具跨平台无新增实现影响；测试策略为后端 Go 编译门禁和宿主插件运行时单元测试，不新增 E2E。
- 7.3 验证方式：首次运行`cd apps/lina-core && go test ./internal/service/pluginhostservices ./internal/service/plugin/... -count=1`时`pluginhostservices`和各`plugin/internal/*`子包通过，但`internal/service/plugin`包报告失败且输出未稳定呈现断言；随后单独运行`cd apps/lina-core && go test ./internal/service/plugin -count=1`通过，再次运行完整 7.3 命令通过，最终以复跑通过结果作为完成证据。
- 7.1 重新完成摘要：按 FB-3 约束，不恢复`hack/tools/linactl/internal/repository`插件边界扫描实现，也不重新接入`command_test.scripts.go`；新增`apps/lina-core/pkg/plugin/capability/capability_boundary_governance_test.go`，以 Go AST 和标准库目录遍历实现跨平台治理扫描，避免 Shell、`rg`或平台专属路径语义。
- 7.1 重新扫描范围：扫描覆盖`apps/lina-core/pkg/plugin/**`不得导入`lina-core/internal/service/plugin/**`，`apps/lina-plugins/**`不得导入`lina-core/internal/**`，以及`apps/lina-core/**`、`apps/lina-plugins/**`、`hack/tools/linactl/**`生产 Go 代码不得导入旧`pkg/pluginbridge`、`pkg/pluginhost`、`pkg/pluginservice`等历史包，且不得重新引用旧`ProviderEnv.Services`、`HostServicesForPlugin`、`HostServices()`或`pluginbridge.Runtime()`/`Data()`等根包业务能力入口和类型别名。
- 7.1 重新规则加载：已读取`openspec`、`documentation`、`architecture`、`plugin`、`backend-go`、`dev-tooling`、`testing`、`data-permission`、`cache-consistency`和`i18n`规则，并使用`goframe-v2`技能；本段新增 Go 测试和更新 OpenSpec 任务记录，不修改 HTTP API、SQL、前端、插件清单、语言包或运行时用户可见文案。
- 7.1 重新影响判断：`i18n`资源无影响；数据权限无新增数据操作面，治理测试用于阻止插件绕过公共能力契约直接访问宿主内部数据权限、DAO 或内部服务；缓存一致性无新增缓存源、失效通道、跨实例协调分支或孤立服务实例；开发工具跨平台影响为新增 Go 测试型治理扫描，未修改`linactl`命令、`test.scripts`、构建器或平台脚本；测试策略为治理扫描单元测试、包级 Go 编译门禁、OpenSpec 严格校验和格式检查，不新增 E2E。
- 7.1 重新验证方式：已运行`cd apps/lina-core && go test ./pkg/plugin/capability -run TestRepositoryPluginCapabilityBoundaries -count=1`通过，覆盖新增治理扫描；待本段收尾运行`cd apps/lina-core && go test ./pkg/plugin/capability -count=1`、`openspec validate harden-plugin-capability-boundaries --strict`和相关`git diff --check`作为本任务完成门禁。
- 7.4 执行摘要：已运行覆盖宿主启动装配、HTTP 运行时构造、路由绑定、插件能力目录注入和源码插件静态嵌入绑定的宿主编译门禁；本段未修改生产 Go 代码，仅更新任务执行记录。
- 7.4 规则加载：已读取`openspec`、`documentation`、`architecture`、`plugin`、`backend-go`、`testing`、`data-permission`、`cache-consistency`和`i18n`规则，并使用`goframe-v2`技能；本段未修改 HTTP API、SQL、前端、开发工具、插件清单、语言包或运行时用户可见文案。
- 7.4 影响判断：`i18n`资源无影响；数据权限无新增数据操作面，本段仅验证启动装配仍能编译；缓存一致性无新增缓存源、失效通道、跨实例协调分支或孤立服务实例；开发工具跨平台无新增实现影响；测试策略为宿主启动装配 Go 编译门禁、OpenSpec 严格校验和格式检查，不新增 E2E。
- 7.4 验证方式：已运行`cd apps/lina-core && go test ./internal/cmd -count=1`通过。
- 7.5 执行摘要：已在当前工作区运行 9 个官方源码插件后端编译 smoke，并运行官方动态样例 WASI artifact 构建 smoke；`linapro-demo-dynamic`为动态插件，不纳入源码插件后端 smoke 范围。
- 7.5 规则加载：已读取`openspec`、`documentation`、`architecture`、`plugin`、`backend-go`、`dev-tooling`、`testing`、`data-permission`、`cache-consistency`、`i18n`和`api-contract`规则，并使用`goframe-v2`技能；本段未修改 HTTP API、SQL、前端、插件清单、语言包或运行时用户可见文案。
- 7.5 影响判断：`i18n`资源无影响；数据权限无新增数据操作面，本段仅验证插件后端和动态 WASI artifact 在当前公共契约下可编译构建；缓存一致性无新增缓存源、失效通道、跨实例协调分支或孤立服务实例；开发工具跨平台影响沿用 Go 实现的`linactl wasm`和`wasmbuilder`，未新增平台专属脚本；测试策略为源码插件 Go 编译 smoke、动态插件 WASI 构建 smoke、OpenSpec 严格校验和格式检查，不新增 E2E。
- 7.5 验证方式：已使用`GOWORK=/Users/john/Workspace/github/linaproai/linapro/temp/go.work.plugins`分别运行`linapro-content-notice`、`linapro-demo-source`、`linapro-monitor-loginlog`、`linapro-monitor-online`、`linapro-monitor-operlog`、`linapro-monitor-server`、`linapro-ops-demo-guard`、`linapro-org-core`和`linapro-tenant-core`目录下的`go test ./backend -count=1`通过；已运行并在`wasmbuilder`格式化后复跑`cd hack/tools/linactl && go run . wasm p=linapro-demo-dynamic out=temp/output`通过，并生成`temp/output/linapro-demo-dynamic.wasm`。
- 7.6 执行摘要：本变更修改了`hack/tools/linactl/internal/wasmbuilder`，因此已运行`linactl`全包测试覆盖命令层、构建器、官方插件 workspace 准备和动态 WASM artifact 打包路径；本段仅更新 OpenSpec 任务记录。
- 7.6 规则加载：已读取`openspec`、`documentation`、`architecture`、`plugin`、`backend-go`、`dev-tooling`、`testing`、`data-permission`、`cache-consistency`和`i18n`规则，并使用`goframe-v2`技能；本段未修改 HTTP API、SQL、前端、插件清单、语言包或运行时用户可见文案。
- 7.6 跨平台影响：`wasmbuilder`和`linactl wasm`仍为 Go 标准库与 Go 工具链实现，默认开发路径未新增 Shell、POSIX 路径语义、平台专属脚本或单平台系统命令依赖。
- 7.6 影响判断：`i18n`资源无影响；数据权限无新增数据操作面；缓存一致性无新增缓存源、失效通道、跨实例协调分支或孤立服务实例；开发工具跨平台影响已通过 Go 全包测试覆盖；测试策略为`linactl`全包单元测试、OpenSpec 严格校验和格式检查，不新增 E2E。
- 7.6 验证方式：已运行并在`wasmbuilder`格式化后复跑`cd hack/tools/linactl && go test ./... -count=1`通过。
- 7.7 执行摘要：已在当前任务记录更新后运行 OpenSpec 严格校验和相关文件格式检查；本段只更新任务执行记录，不修改生产 Go 代码、HTTP API、SQL、前端、开发工具实现、插件清单、语言包或运行时用户可见文案。
- 7.7 规则加载：已读取`openspec`、`documentation`、`architecture`、`plugin`、`backend-go`、`dev-tooling`、`testing`、`data-permission`、`cache-consistency`、`i18n`和`api-contract`规则，并使用`goframe-v2`技能。
- 7.7 影响判断：`i18n`资源无影响；数据权限无新增数据操作面；缓存一致性无新增缓存源、失效通道、跨实例协调分支或孤立服务实例；开发工具跨平台无新增实现影响；测试策略为 OpenSpec 严格校验、格式检查、任务级审查和已完成的 Go 编译/构建 smoke，不新增 E2E。
- 7.7 验证方式：已运行`openspec validate harden-plugin-capability-boundaries --strict`通过；已运行`git diff --check -- openspec/changes/harden-plugin-capability-boundaries/tasks.md hack/tools/linactl/internal/wasmbuilder apps/lina-core/pkg/plugin/capability/capability_boundary_governance_test.go`通过；已运行`gofmt -l hack/tools/linactl/internal/wasmbuilder/*.go apps/lina-core/pkg/plugin/capability/capability_boundary_governance_test.go`无输出；已确认`temp/output/linapro-demo-dynamic.wasm`非空。
- 8.1 执行摘要：已完成本变更最终`i18n`影响记录。本 8.x 收尾步骤未新增或修改运行时用户可见文案、菜单、路由、按钮、表单、表格、提示信息、错误消息、API 文档源文本、插件清单字段、语言包或翻译缓存逻辑；前序实现如涉及相关文件，以对应任务执行记录、验证命令和审查结论为准，本任务不以 8.1 扩大范围。
- 8.1 规则加载：已读取`openspec`、`documentation`、`architecture`、`plugin`、`i18n`、`testing`、`data-permission`、`cache-consistency`、`backend-go`、`api-contract`和`dev-tooling`规则；本段只更新 OpenSpec 任务执行记录。
- 8.1 影响判断：`i18n`资源无影响，宿主和插件均不需要新增`manifest/i18n`、前端运行时语言包、`apidoc i18n JSON`或错误码翻译；也未修改翻译缓存、语言注册或多语言配置。
- 8.1 验证方式：使用 OpenSpec 严格校验、任务记录审查和格式检查作为治理验证；本段不运行`i18n.check`，原因是未修改运行时文案、API 文档源文本、语言包或插件`manifest/i18n`资源。
- 8.2 执行摘要：已完成本变更最终数据权限影响记录。本变更的核心数据权限影响是收窄公共暴露面：普通插件消费面不再暴露`*gdb.Model`、`*ghttp.Request`、DAO、DO、Entity、raw SQL、写入接口、数据范围注入或宿主内部治理接口；宿主内部仍通过`orgcap.ScopeService`、`tenantcap.ScopeService`、成员关系和写前校验窄接口在数据库查询阶段或操作前执行数据权限治理。
- 8.2 规则加载：已读取`openspec`、`architecture`、`plugin`、`data-permission`、`backend-go`、`api-contract`、`testing`、`cache-consistency`和`i18n`规则；本段只更新 OpenSpec 任务执行记录。
- 8.2 影响判断：本段无新增数据操作接口、HTTP API、SQL、导出、下载、聚合统计、批量写入或前端候选项；变更后的数据权限边界以宿主内部窄接口和动态插件`hostServices`授权快照为准，读取类路径继续要求数据库侧过滤，写操作和执行动作继续要求操作前可见性校验。
- 8.2 验证方式：以已完成的 6.1、6.2、6.3、7.1、7.2、7.3、7.4、7.5 和 7.7 验证记录为依据，覆盖公共接口无数据库模型暴露、宿主插件运行时编译、官方插件不依赖宿主内部包、动态 data service 通过结构化授权访问和 OpenSpec 严格校验；本段不新增运行时测试，因为只补充治理记录。
- 8.3 执行摘要：已完成本变更最终缓存一致性影响记录。本变更不新增缓存源、缓存键、缓存失效通道、预热流程、跨实例协调分支、共享修订号、事件广播或第二套 provider active 状态；缓存敏感能力继续由启动期共享实例注入并复用宿主既有插件 enabled snapshot、runtime revision、`i18n`、`pluginstate`、cache、tenant 和 org 服务实例。
- 8.3 规则加载：已读取`openspec`、`architecture`、`plugin`、`cache-consistency`、`backend-go`、`testing`、`data-permission`和`i18n`规则；本段只更新 OpenSpec 任务执行记录。
- 8.3 影响判断：缓存一致性无新增实现影响；权威数据源、失效触发点、跨实例同步机制、最大陈旧窗口和故障降级策略均沿用现有缓存敏感服务契约。审查重点是防止插件调用路径、中间件和 host service 临时构造孤立服务图，已在 2.x、6.4、7.3 和 7.4 的装配与编译验证中覆盖。
- 8.3 验证方式：以已完成的启动装配测试、插件宿主服务测试、公共插件契约测试、`linactl`构建器测试、动态 WASI 构建 smoke 和 OpenSpec 严格校验作为治理证据；本段不新增缓存单元测试，因为没有新增缓存或失效逻辑。
- 8.4 执行摘要：已完成本变更最终测试策略记录。本次迭代属于破坏性 Go 公共契约、宿主装配、插件 SDK、官方插件 provider 和治理边界重构，默认不改变用户可见 HTTP API、前端页面、数据库 schema、运行时文案或端到端工作流，因此不新增 E2E。
- 8.4 规则加载：已读取`openspec`、`documentation`、`architecture`、`plugin`、`backend-go`、`testing`、`data-permission`、`cache-consistency`、`i18n`、`api-contract`和`dev-tooling`规则；本段只更新 OpenSpec 任务执行记录。
- 8.4 测试策略：后端和插件行为通过定向 Go 单元测试、公共插件契约测试、宿主插件运行时测试、宿主启动装配编译门禁、官方源码插件后端编译 smoke、动态插件 WASI 构建 smoke、`linactl`全包测试、静态检索、格式检查和`openspec validate harden-plugin-capability-boundaries --strict`覆盖；前端和 E2E 不新增，原因是本变更不改变用户可观察行为或端到端业务流程。
- 8.4 验证方式：本段补充治理记录后已重新运行`openspec validate harden-plugin-capability-boundaries --strict`通过；已运行`git diff --check -- openspec/changes/harden-plugin-capability-boundaries/tasks.md`通过，并以通过结果作为最终完成门禁。

## Feedback

- [x] **FB-1**: 需求澄清为本迭代按破坏性公共契约收敛执行，旧兼容面直接删除，普通插件消费面只保留只读 DTO 化能力，`pluginbridge`根包只保留协议层，本迭代默认不改变用户可见行为，并将旧入口残留扫描作为完成门禁。
- [x] **FB-2**: 去掉`hack/tools/linactl/command_governance.plugin-boundary.go`以及独立`governance.plugin-boundary`命令入口，保留内部治理扫描供`test.scripts`使用。
- [x] **FB-3**: 去掉`hack/tools/linactl/internal/repository`和`command_test.scripts.go`中本次迭代新增的插件边界扫描实现与`test.scripts`接入。
- [x] **FB-4**: `Status`和`Available`必须验证 provider factory 构造成功，不能只检查已启用插件快照。
- [x] **FB-5**: 删除普通插件消费面中语义重复的`Enabled`方法，统一以`Available`表达能力可用性。
- [x] **FB-6**: 用户部门过滤不得通过组织能力返回高基数用户 ID 列表，应改为宿主内部数据库侧过滤契约。
- [x] **FB-7**: 普通`orgcap.Service`不应继续暴露工作台投影方法，组织工作台投影应拆到宿主内部窄接口。
- [x] **FB-8**: `tenantcap.RuntimeService`不应聚合无生产调用的 fallback 能力，删除或下沉`FallbackService`以收窄组合接口。
- [x] **FB-9**: 删除插件 manifest 和动态 artifact 解析中对旧`capabilities`配置项或 custom section 的定点判断，避免在无兼容负担项目中沉淀历史字段知识。
- [x] **FB-10**: 源码插件 registrar 和 hook payload 返回的是`pluginhost.Services`服务目录，应统一使用`Services()`命名，避免与动态插件能力授权集合混淆。
- [x] **FB-11**: 删除当前迭代中为了兼容历史版本保留的单元测试、旧方法定义、旧 facade、旧 manifest/artifact 解析和旧路径容忍逻辑。
- [x] **FB-12**: 统一动态插件消费宿主基础能力的`guest.Directory`入口，恢复受治理 data facade 的目录入口，收敛 network 入口命名并修正规范残留。

## Feedback 执行记录

- FB-1 原因分析：既有提案和设计已经表达“无历史兼容负担”和删除旧接口方向，但尚未把本轮确认的六项边界固化为可审查的规范要求，尤其是“不保留兼容转发层”“缺少读能力时只能新增 DTO 化批量只读方法”“默认不改变用户可见行为”和“旧入口残留扫描为硬门禁”。
- FB-1 影响分析：本段仅更新`harden-plugin-capability-boundaries`的 OpenSpec 提案、设计、增量规范和任务记录，不修改生产 Go 代码、插件源码、HTTP API、前端页面、SQL、插件清单、运行时文案或语言包资源。
- FB-1 规则加载：已读取`AGENTS.md`、`.agents/rules/openspec.md`、`.agents/rules/documentation.md`、`.agents/rules/architecture.md`、`.agents/rules/plugin.md`、`.agents/rules/backend-go.md`、`.agents/rules/data-permission.md`、`.agents/rules/cache-consistency.md`、`.agents/rules/testing.md`、`.agents/rules/i18n.md`和`.agents/rules/dev-tooling.md`。
- FB-1 影响判断：`i18n`资源无影响；数据权限无新增数据操作面，文档只强化数据库侧过滤和内部窄接口边界；缓存一致性无新增缓存源或失效通道；开发工具跨平台暂未修改工具实现；测试策略为 OpenSpec 严格校验、定向格式检查和审查结论，本段不新增单元测试或 E2E。
- FB-1 验证方式：已运行`openspec validate harden-plugin-capability-boundaries --strict`通过；已运行`git diff --check -- openspec/changes/harden-plugin-capability-boundaries/proposal.md openspec/changes/harden-plugin-capability-boundaries/design.md openspec/changes/harden-plugin-capability-boundaries/specs/core-host-boundary-governance/spec.md openspec/changes/harden-plugin-capability-boundaries/specs/plugin-host-service-extension/spec.md openspec/changes/harden-plugin-capability-boundaries/specs/pluginbridge-subcomponent-architecture/spec.md openspec/changes/harden-plugin-capability-boundaries/tasks.md`通过。
- FB-2 原因分析：7.1 的核心需求是新增或更新跨平台治理扫描作为完成门禁；独立`governance.plugin-boundary`命令属于额外公开命令入口，不是完成门禁必需。保留`repository.ValidatePluginBoundaries`并由`test.scripts`调用即可满足治理扫描目标，同时减少`linactl`公开命令面。
- FB-2 实现摘要：已删除`hack/tools/linactl/command_governance.plugin-boundary.go`，并从`commandRegistry()`移除`governance.plugin-boundary`注册；`command_test.scripts.go`继续调用`repository.ValidatePluginBoundaries`，治理扫描仍纳入仓库工具 smoke。
- FB-2 规则加载：已读取`AGENTS.md`、`.agents/rules/openspec.md`、`.agents/rules/documentation.md`、`.agents/rules/dev-tooling.md`、`.agents/rules/testing.md`、`.agents/rules/plugin.md`和`goframe-v2`技能；本段未修改 HTTP API、SQL、前端、插件资源、运行时用户可见文案或语言包。
- FB-2 影响判断：`i18n`资源无影响；数据权限无新增数据操作面；缓存一致性无新增缓存源、失效通道或跨实例协调分支；开发工具跨平台影响为移除一个额外`linactl`命令文件和注册项，不新增平台专属脚本；测试策略为 linactl 单元测试、内部治理扫描测试、OpenSpec 严格校验和格式检查，不新增 E2E。
- FB-2 验证方式：已运行`cd hack/tools/linactl && go test . -run 'TestCommandRegistryDoesNotExposePluginBoundaryGovernance|TestCommandRegistry|TestValidateRepositoryTooling|TestValidateLinactlCommandFiles' -count=1`通过；已运行`cd hack/tools/linactl && go test ./internal/repository -count=1`通过；已运行`cd hack/tools/linactl && go test ./... -count=1`通过；已运行`openspec validate harden-plugin-capability-boundaries --strict`通过；已运行`rg -n "governance\\.plugin-boundary|runGovernancePluginBoundary|command_governance\\.plugin-boundary|go run \\. governance\\.plugin-boundary" hack/tools/linactl openspec/changes/harden-plugin-capability-boundaries/tasks.md --glob '*.go' --glob '*.md'`确认生产代码无残留，仅任务记录和防回归测试保留；已运行本段修改文件的`git diff --check`通过。
- FB-3 原因分析：用户明确要求去掉`hack/tools/linactl/internal/repository`和`hack/tools/linactl/command_test.scripts.go`中本次迭代新增内容；对应新增点为插件边界扫描实现、扫描单元测试，以及`test.scripts`中对`repository.ValidatePluginBoundaries`的调用。该反馈取代 FB-2 中“保留内部治理扫描供`test.scripts`使用”的处理结论，因此 7.1 扫描任务已退回未完成。
- FB-3 实现摘要：已删除未纳入历史版本的`hack/tools/linactl/internal/repository/repository_plugin_boundary.go`和`repository_plugin_boundary_test.go`，并从`hack/tools/linactl/command_test.scripts.go`移除`repository.ValidatePluginBoundaries(a.root)`调用；保留`repository.go`既有`ValidateTooling`和`ValidateLinactlCommandFiles`，不影响原有`test.scripts`仓库工具 smoke。
- FB-3 规则加载：已读取`AGENTS.md`、`.agents/rules/openspec.md`、`.agents/rules/documentation.md`、`.agents/rules/dev-tooling.md`、`.agents/rules/testing.md`、`.agents/rules/backend-go.md`、`.agents/rules/architecture.md`、`.agents/rules/plugin.md`、`.agents/rules/data-permission.md`、`.agents/rules/cache-consistency.md`、`.agents/rules/i18n.md`，并使用`lina-feedback`、`goframe-v2`和`karpathy-guidelines`技能；本段未修改 HTTP API、SQL、前端、插件资源、运行时用户可见文案或语言包。
- FB-3 影响判断：`i18n`资源无影响；数据权限无新增数据操作面，删除的是开发工具治理扫描实现；缓存一致性无新增缓存源、失效通道或跨实例协调分支；开发工具跨平台影响为移除一个 Go 实现的`test.scripts`内部扫描接入，不新增平台专属脚本或 Shell 依赖；测试策略为 linactl 单元测试、OpenSpec 严格校验、静态残留检索和格式检查，不新增 E2E。
- FB-3 验证方式：已运行`cd hack/tools/linactl && go test . ./internal/repository -count=1`通过；已运行`openspec validate harden-plugin-capability-boundaries --strict`通过；已运行`rg -n "ValidatePluginBoundaries|ScanPluginBoundaries|PluginBoundaryFinding|PluginBoundaryError|repository_plugin_boundary|repository\\.ValidatePluginBoundaries" hack/tools/linactl openspec/changes/harden-plugin-capability-boundaries/tasks.md --glob '*.go' --glob '*.md'`确认`hack/tools/linactl`代码无残留，命中仅为任务历史记录；已运行`git diff --check -- hack/tools/linactl/command_test.scripts.go hack/tools/linactl/internal/repository openspec/changes/harden-plugin-capability-boundaries/tasks.md`通过。
- FB-4 原因分析：`orgcap.Status`、`tenantcap.Status`和`Available`原先只根据 provider 插件 enabled snapshot 判断可用性；如果 provider factory 缺少`TenantFilter`、`BizCtx`等构造依赖，状态会显示可用，但首次真实调用才失败，形成能力目录和运行路径语义不一致。
- FB-4 实现摘要：已在 shared capability registry 中新增`StatusWithProvider`并让`orgcap`、`tenantcap`的`Status`/`Available`调用它；该路径会实际尝试构造 active provider，构造失败时返回`Available=false`、清空`ActiveProvider`，并使用稳定 reason `provider_error`。运行期 provider 调用改为使用`ActiveProviderWithError`，不再把 provider 构造失败吞成“能力禁用”。同时为`tenantcap.EnsureTenantVisible`补充`bizCtxSvc`空依赖防护，provider 已激活但无法证明用户上下文时按拒绝处理，避免 panic 或安全降级。
- FB-4 测试覆盖：新增`pkg/plugin/capability/internal/capabilityregistry`单元测试，覆盖 factory 报错时`StatusWithProvider`降为 unavailable、`ActiveProviderWithError`返回构造错误、多个 enabled provider 返回 conflict，以及状态验证后 provider 实例缓存不重复构造。
- FB-4 规则加载：已读取`AGENTS.md`、`.agents/rules/openspec.md`、`.agents/rules/documentation.md`、`.agents/rules/architecture.md`、`.agents/rules/plugin.md`、`.agents/rules/backend-go.md`、`.agents/rules/data-permission.md`、`.agents/rules/cache-consistency.md`、`.agents/rules/testing.md`、`.agents/rules/i18n.md`、`.agents/rules/api-contract.md`和`.agents/rules/database.md`，并使用`lina-feedback`、`goframe-v2`和`karpathy-guidelines`技能；本段未修改 HTTP API、SQL、前端、开发工具、插件清单、语言包或运行时用户可见文案。
- FB-4 影响判断：`i18n`资源无影响；数据权限影响为 provider 构造失败不再降级为禁用后放行，安全路径返回错误或受控拒绝；缓存一致性无新增缓存源、失效通道或第二套 provider active 状态，仍复用插件 enabled snapshot 和已有 provider 实例缓存；开发工具跨平台无影响；测试策略为后端单元测试、定向编译门禁、静态残留检索和 OpenSpec 严格校验，不新增 E2E。
- FB-5 原因分析：`Enabled(ctx)`和`Available(ctx)`在普通`orgcap.Service`、`tenantcap.Service`消费面上都表达“当前是否有可用 provider”，与插件生命周期层的`IsEnabled/IsProviderEnabled`不同；同时保留两个方法会迫使调用方猜测差异，增加接口复杂度。
- FB-5 实现摘要：已从普通`orgcap.Service`、`tenantcap.Service`及宿主窄接口中删除 capability 级`Enabled(ctx)`，统一使用`Available(ctx)`表达“能力可用且 provider 可构造”。宿主调用方和测试替身已迁移到`Available`；插件生命周期、配置、多语言和状态枚举中的`IsEnabled`/`Enabled`保留原领域语义，不纳入本次删除范围。
- FB-5 测试覆盖：通过编译门禁覆盖所有实现接口的生产代码和测试替身；同时新增能力边界反射测试，防止相关公共组合接口重新暴露已删除的宽方法。
- FB-5 规则加载：已读取`openspec`、`documentation`、`architecture`、`plugin`、`backend-go`、`testing`、`data-permission`、`cache-consistency`和`i18n`规则，并使用`goframe-v2`技能；本段未修改 HTTP API、SQL、前端、开发工具、插件清单或语言包。
- FB-5 影响判断：`i18n`资源无影响；数据权限无新增数据操作面，语义收敛只改变 Go 接口消费面；缓存一致性无新增缓存源或失效路径；开发工具跨平台无影响；测试策略为 Go 编译门禁、反射治理测试和静态检索，不新增 E2E。
- FB-6 原因分析：用户列表部门筛选原实现路径通过组织能力返回部门下所有用户 ID 或未分配部门用户 ID，再对`sys_user`做`WhereIn/WhereNotIn`；该方式会在大部门或大租户下产生高基数 ID 列表，既增加内存和 SQL 参数压力，也把“用户集合枚举”暴露成跨模块契约。
- FB-6 实现摘要：已从普通组织能力和 provider 接口删除`GetUserIDsByDept`、`GetAllAssignedUserIDs`，改为在宿主内部`orgcap.ScopeService`提供`ApplyUserDeptFilter`和`ApplyUserDeptUnassignedFilter`数据库侧过滤契约；用户列表部门筛选改为直接让组织 provider 注入查询条件。`linapro-org-core` provider adapter 使用`EXISTS`/`NOT EXISTS`相关子查询过滤用户部门关系，不再物化用户 ID 列表。
- FB-6 测试覆盖：更新用户数据权限和批量删除相关测试替身到数据库侧 filter 契约；现有用户列表测试通过编译约束旧高基数方法不能再被调用，provider adapter 通过官方插件后端编译 smoke 覆盖方法签名。
- FB-6 规则加载：已读取`openspec`、`architecture`、`plugin`、`backend-go`、`data-permission`、`database`、`testing`、`cache-consistency`和`i18n`规则，并使用`goframe-v2`技能；本段未修改 HTTP API、前端、开发工具、插件清单、语言包或 SQL 迁移文件。
- FB-6 影响判断：`i18n`资源无影响；数据权限影响为读取类用户列表继续在数据库查询阶段注入部门过滤，避免先查全量再内存过滤；缓存一致性无新增缓存源或失效路径；数据库影响为查询路径改为关联子查询，未新增 schema 或索引；开发工具跨平台无影响；测试策略为定向 Go 测试、官方插件 provider 编译 smoke 和静态残留检索，不新增 E2E。
- FB-7 原因分析：`UserDeptTree`和`ListPostOptions`是宿主内建用户管理工作台使用的树形/下拉投影，并非普通插件消费组织能力时应依赖的稳定领域原语；继续放在`orgcap.Service`会把工作台页面适配形状混入公共插件消费面。
- FB-7 实现摘要：新增`orgcap.ProjectionService`作为宿主内部用户管理投影窄接口，并从普通`orgcap.Service`移除`UserDeptTree`和`ListPostOptions`；`orgcap.RuntimeService`继续组合`ProjectionService`，启动装配复用同一个 runtime 实例但在`httpRuntime`中按`orgcap.ProjectionService`字段传给用户控制器。用户控制器`NewV1`改为只依赖投影窄接口，普通`capability.Services.Org()`仍返回不含工作台投影的`orgcap.Service`。
- FB-7 测试覆盖：新增反射治理测试确认`orgcap.Service`不再暴露`UserDeptTree`/`ListPostOptions`；`orgcap`fallback 测试将投影方法验证移动到宿主内部 runtime 能力；启动装配和用户控制器包编译测试覆盖新增窄接口传递。
- FB-7 规则加载：已读取`openspec`、`documentation`、`architecture`、`plugin`、`backend-go`、`api-contract`、`testing`、`data-permission`、`cache-consistency`和`i18n`规则，并使用`goframe-v2`技能；本段未修改 API DTO、路由、SQL、前端、开发工具、插件清单、语言包或运行时用户可见文案。
- FB-7 影响判断：`i18n`资源无影响；数据权限无新增数据操作面，变更只收窄 Go 接口和控制器依赖；缓存一致性无新增缓存源或失效路径；API 契约无变更，既有用户部门树和岗位选项接口保持不变；开发工具跨平台无影响；测试策略为反射治理测试、控制器和启动装配编译门禁，不新增 E2E。
- FB-8 原因分析：`tenantcap.RuntimeService`聚合的`FallbackService`没有生产调用，只在测试 fake 中为了满足宽接口存在；继续放在 runtime 组合接口中会扩大所有需要租户 runtime 的调用方和测试替身职责。
- FB-8 实现摘要：已从`tenantcap.RuntimeService`移除`FallbackService`，删除`FallbackService`接口、`ReadWithPlatformFallback`实现、`FallbackScanner`类型和相关测试替身方法；保留的`RuntimeService`只组合普通消费、请求解析、数据库 scope、用户成员关系、自动开通和启动一致性等仍有生产调用的宿主内部能力。
- FB-8 测试覆盖：新增反射治理测试确认`tenantcap.RuntimeService`不再暴露`ReadWithPlatformFallback`；受影响的认证、pluginhostservices、租户能力和启动装配包编译测试覆盖 fake 与组合接口同步。
- FB-8 规则加载：已读取`openspec`、`architecture`、`plugin`、`backend-go`、`testing`、`data-permission`、`cache-consistency`和`i18n`规则，并使用`goframe-v2`技能；本段未修改 HTTP API、SQL、前端、开发工具、插件清单、语言包或运行时用户可见文案。
- FB-8 影响判断：`i18n`资源无影响；数据权限无新增数据操作面，删除的是无生产调用 fallback 读接口；缓存一致性无新增缓存源或失效路径；开发工具跨平台无影响；测试策略为反射治理测试、定向 Go 编译门禁和静态残留检索，不新增 E2E。
- FB-4~FB-8 验证方式：已运行`cd apps/lina-core && go test ./pkg/plugin/capability ./pkg/plugin/capability/internal/capabilityregistry ./pkg/plugin/capability/orgcap ./pkg/plugin/capability/tenantcap -count=1`通过；已运行`cd apps/lina-core && go test ./internal/controller/user ./internal/cmd ./internal/service/user ./internal/service/datascope ./internal/service/auth ./internal/service/middleware ./internal/service/menu ./internal/service/plugin ./internal/service/notify ./internal/service/role ./internal/service/pluginhostservices ./internal/service/plugin/internal/wasm ./internal/service/plugin/internal/integration ./internal/service/plugin/internal/datahost -count=1`通过；已运行`cd apps/lina-core && go test ./pkg/plugin/capability/... -count=1`通过；已运行`cd apps/lina-plugins/linapro-org-core && GOWORK=/Users/john/Workspace/github/linaproai/linapro/temp/go.work.plugins go test ./backend ./backend/internal/provider/orgcapadapter -count=1`通过；已运行`rg -n "FallbackService|ReadWithPlatformFallback|FallbackScanner|ApplyPlatformFallbackWhere|GetUserIDsByDept|GetAllAssignedUserIDs" apps/lina-core apps/lina-plugins -g'*.go'`确认仅防回归测试中保留旧方法名字符串；已运行`gofmt -l`确认本段修改 Go 文件无格式残留。
- FB-9 原因分析：`apps/lina-core/internal/service/plugin/internal/catalog/manifest.go`中对顶层`capabilities`和`dependencies.capabilities`的判断，是此前移除独立 framework capability manifest 配置块后留下的防御式校验，用于避免 YAML 宽松解码静默忽略旧字段；在当前项目无历史兼容负担的前提下，长期生产代码不应继续记住旧配置项名称。同类审查发现动态 artifact embedded manifest 解析也保留了`capabilities`/`dependencies.capabilities`特判，runtime artifact 还保留了旧`lina.plugin.backend.capabilities`custom section 常量和拒绝逻辑。
- FB-9 实现摘要：已将源码插件 YAML manifest 和嵌入式 source manifest 的预校验收敛为当前`dependencies.plugins[*]`schema 校验，只拒绝依赖项内非`id`/`version`字段；已将动态 artifact embedded manifest 预校验做同样收敛；已删除 runtime artifact 对旧 backend capabilities custom section 的拒绝逻辑、`pluginbridge`公开 facade 中的旧 section 常量和底层 artifact 常量；已同步删除 catalog/runtime 中固定旧`capabilities`行为的测试用例，保留`dependencies.plugins[*].required/install`当前 schema 约束测试。
- FB-9 其它文件审查：已静态检索`apps/lina-core`、`apps/lina-plugins`和`hack/tools/linactl`中`WasmSectionBackendCapabilities`、`rejectDeprecatedRuntimeArtifactCapabilities`、`rejectUnsupportedManifestDependencyFields`、`rejectUnsupportedRuntimeManifestDependencyFields`、`dependencies.capabilities`、`field capabilities is not supported`、`lina.plugin.backend.capabilities`和`hostServices migration`等旧字段/迁移判断，目标生产路径和构建器均无残留；剩余`capabilities`命名均为从当前`hostServices`派生的运行时能力集合或通用能力注释，不是插件配置项名称判断。
- FB-9 规则加载：已读取`AGENTS.md`、`.agents/rules/openspec.md`、`.agents/rules/documentation.md`、`.agents/rules/architecture.md`、`.agents/rules/plugin.md`、`.agents/rules/backend-go.md`、`.agents/rules/testing.md`、`.agents/rules/i18n.md`、`.agents/rules/data-permission.md`和`.agents/rules/cache-consistency.md`，并使用`lina-feedback`和`goframe-v2`技能；本段未修改 HTTP API、SQL、前端、开发工具实现、插件实际清单、语言包或运行时用户可见文案。
- FB-9 影响判断：`i18n`资源无影响，未新增或修改运行时文案、API 文档源文本、错误码、插件清单实际文件或语言包；数据权限无新增数据操作面，变更只收敛插件 manifest/artifact 解析中的旧字段知识；缓存一致性无新增缓存源、失效通道、跨实例协调或运行时快照变更；开发工具跨平台无新增脚本、命令或平台专属入口；测试策略为后端 Go 包级测试、公共`pluginbridge`包测试、静态残留检索、OpenSpec 严格校验和格式检查，不新增 E2E。
- FB-9 验证方式：已运行`cd apps/lina-core && go test ./internal/service/plugin/internal/catalog -run 'TestLoadManifestFromYAMLRejectsUnsupportedDependencyPolicyFields|TestValidatePluginManifestNormalizesDependencyDefaults|TestValidatePluginManifestRejectsInvalidDependencies' -count=1`通过；已运行`cd apps/lina-core && go test ./internal/service/plugin/internal/runtime -run 'TestParseRuntimeArtifactRejectsUnsupportedDependencyPolicyFields|TestParseRuntimeArtifactLoadsRoutesAndBridgeSpec|TestParseRuntimeArtifactIgnoresLegacyCronContractsSection' -count=1`通过；已运行`cd apps/lina-core && go test ./pkg/plugin/pluginbridge/... -count=1`通过；已运行`cd apps/lina-core && go test ./internal/service/plugin/internal/catalog ./internal/service/plugin/internal/runtime -count=1`通过；已运行`openspec validate harden-plugin-capability-boundaries --strict`通过；已运行`git diff --check -- apps/lina-core/internal/service/plugin/internal/catalog/manifest.go apps/lina-core/internal/service/plugin/internal/catalog/embedded.go apps/lina-core/internal/service/plugin/internal/catalog/catalog_test.go apps/lina-core/internal/service/plugin/internal/runtime/artifact.go apps/lina-core/internal/service/plugin/internal/runtime/artifact_test.go apps/lina-core/pkg/plugin/pluginbridge/pluginbridge.go apps/lina-core/pkg/plugin/pluginbridge/internal/artifact/artifact.go openspec/changes/harden-plugin-capability-boundaries/tasks.md`通过；已运行`gofmt -l`确认本段修改 Go 文件无格式残留；已运行旧字段静态检索确认无残留。
- FB-10 原因分析：源码插件 registrar、cron registrar、managed cron collector 和 hook payload 暴露的是`pluginhost.Services`运行期服务目录，而不是动态插件`hostServices`派生出的能力授权集合；继续使用`Capabilities()`会让调用方把“可调用服务目录”和“授权能力集合”混在一起。
- FB-10 实现摘要：已将`pluginhost.HTTPRegistrar`、`CronRegistrar`、`HookPayload`和`managedCronCollector`的公开访问器统一为`Services()`，将 hook payload 构造器改为`NewHookPayloadWithServices`，并同步更新宿主集成路径、测试替身和 9 个官方源码插件入口调用点；动态插件`CapabilitiesFromHostServices`、`HostCapabilities`等真实授权能力集合命名保持不变。
- FB-10 其它组件审查：已扫描`apps/lina-core/pkg/plugin/pluginhost`、`internal/service/plugin/internal/integration`、官方源码插件和`pluginbridge`相关能力集合命名。需要统一的类似问题仅限返回`pluginhost.Services`的源码插件服务目录入口；`pluginbridge/internal/hostservice`中的`AllCapabilities`和`CapabilitiesFromHostServices`语义为授权能力集合，不属于本次改名范围。
- FB-10 规则加载：已读取`AGENTS.md`、`.agents/rules/openspec.md`、`.agents/rules/documentation.md`、`.agents/rules/architecture.md`、`.agents/rules/plugin.md`、`.agents/rules/backend-go.md`、`.agents/rules/testing.md`、`.agents/rules/i18n.md`、`.agents/rules/data-permission.md`和`.agents/rules/cache-consistency.md`，并使用`lina-feedback`、`goframe-v2`和`karpathy-guidelines`技能；本段未修改 HTTP API、SQL、前端、开发工具实现、插件清单、语言包或运行时用户可见文案。
- FB-10 影响判断：`i18n`资源无影响；数据权限无新增数据操作面，源码插件仍只能通过`pluginhost.Services`中的受治理服务访问宿主能力；缓存一致性无新增缓存源、失效通道、跨实例协调或运行时快照变更，服务目录继续由启动期共享 capability services scoped 后注入；开发工具跨平台无新增脚本、命令或平台专属入口；测试策略为后端 Go 包级测试、官方源码插件后端 smoke、治理扫描、静态残留检索、OpenSpec 严格校验和格式检查，不新增 E2E。
- FB-10 验证方式：已运行`cd apps/lina-core && go test ./pkg/plugin/pluginhost ./pkg/plugin/capability -count=1`通过；已运行`cd apps/lina-core && go test ./internal/service/plugin/internal/integration -run TestSourcePluginCallbacksUsePluginScopedServices -count=1`通过；已运行`cd apps/lina-core && go test ./internal/service/plugin/internal/integration -count=1`通过；已运行`cd apps/lina-core && go test ./internal/service/pluginhostservices ./internal/service/plugin ./internal/cmd -count=1`通过；已使用`GOWORK=/Users/john/Workspace/github/linaproai/linapro/temp/go.work.plugins`分别运行 9 个官方源码插件`go test ./backend -count=1`通过；已运行`rg -n "pluginhost\\.HostServices|HostServicesForPlugin|ScopedHostServicesFactory|\\.HostServices\\(|NewHookPayloadWithCapabilities|registrar\\.Capabilities\\(|payload\\.Capabilities\\(|\\.Capabilities\\(|SourceCapabilities|sourceCapabilitiesForPlugin" apps/lina-core/pkg/plugin/pluginhost apps/lina-core/internal/service/plugin apps/lina-core/internal/service/pluginhostservices apps/lina-plugins --glob '*.go'`确认生产路径无残留；已运行`openspec validate harden-plugin-capability-boundaries --strict`通过；已运行`git diff --check`和`gofmt -l`确认本段修改无格式残留。
- FB-11 原因分析：当前迭代已经按无历史兼容负担收敛公共插件契约，但动态 guest dispatcher、`linactl` WASM 构建器和部分测试 fixture 仍残留历史容忍面：guest controller 仍支持 raw bridge envelope handler，生成 dispatcher 仍可用`InternalPath`回退命中 handler，生命周期构建逻辑允许 requestType 之外的旧路径可达性，动态样例测试仍断言旧`generatedEnvelopeController`，部分 route/cron/openapi/codec fixture 允许缺少`requestType`。
- FB-11 实现摘要：已删除 guest 反射 dispatcher 的 envelope handler 类型、`handlersByPath`索引和`InternalPath`回退分发；`InternalPath`仅保留为 route metadata。`hack/tools/linactl/internal/wasmbuilder`已删除 raw envelope lifecycle/callback handler 支持，生成代码只按`Route.RequestType`switch 分发；生命周期和 cron 等非公开 route callback 改为 typed DTO handler，并补充`backend/api`请求 DTO 包扫描，支持 lifecycle DTO alias 被生成 dispatcher 正确引用。测试 fixture 已统一补齐`RequestType`并改为 typed DTO/controller helper，删除旧`generatedEnvelopeController`断言。
- FB-11 规则加载：已读取`AGENTS.md`、`.agents/rules/openspec.md`、`.agents/rules/documentation.md`、`.agents/rules/architecture.md`、`.agents/rules/plugin.md`、`.agents/rules/backend-go.md`、`.agents/rules/dev-tooling.md`、`.agents/rules/testing.md`、`.agents/rules/data-permission.md`、`.agents/rules/cache-consistency.md`和`.agents/rules/i18n.md`，并使用`lina-feedback`、`goframe-v2`和`karpathy-guidelines`技能；本段未修改 HTTP API、SQL、前端、插件清单、语言包或运行时用户可见文案。
- FB-11 影响判断：`i18n`资源无影响；数据权限无新增数据操作面，变更仅收敛动态插件 bridge 分发契约和测试 fixture；缓存一致性无新增缓存源、失效通道、跨实例协调或运行时快照变更；开发工具跨平台影响为`linactl` Go WASM 构建器生成逻辑变更，未新增平台专属脚本、Shell 依赖或单平台入口；测试策略为后端 Go 单元/编译门禁、`linactl`全包测试、OpenSpec 严格校验、静态残留扫描和格式检查，不新增 E2E。
- FB-11 验证方式：已运行`cd hack/tools/linactl && go test ./internal/wasmbuilder -count=1`通过；已运行`cd hack/tools/linactl && go test ./... -count=1`通过；已运行`cd apps/lina-core && go test ./pkg/plugin/pluginbridge/guest ./pkg/plugin/pluginbridge/contract ./pkg/plugin/pluginbridge/internal/hostservice ./internal/service/plugin/internal/runtime ./internal/service/plugin/internal/wasm -count=1`通过；已运行`cd apps/lina-core && go test ./internal/service/plugin/internal/integration ./internal/service/plugin/internal/datahost ./internal/service/plugin/internal/datahost/internal/host ./internal/service/plugin/internal/wasm ./internal/service/plugin/internal/runtime ./internal/service/plugin ./internal/cmd ./api/i18n ./internal/controller/i18n ./internal/service/i18n ./internal/service/apidoc -count=1`通过；已运行`cd apps/lina-core && go test ./pkg/plugin/pluginbridge/internal/codec ./internal/service/plugin/internal/openapi -count=1`通过；已运行`openspec validate harden-plugin-capability-boundaries --strict`通过；已运行`git diff --check -- openspec/changes/harden-plugin-capability-boundaries/tasks.md apps/lina-core/pkg/plugin/pluginbridge/guest/guest_router.go hack/tools/linactl/internal/wasmbuilder apps/lina-core/internal/service/plugin/internal/runtime/route_dispatch_test.go apps/lina-core/internal/service/plugin/internal/wasm/hostfn_service_cron_test.go apps/lina-core/internal/service/plugin/internal/integration/menu_test.go apps/lina-core/pkg/plugin/pluginbridge/internal/codec/codec_test.go apps/lina-core/internal/service/plugin/internal/openapi/openapi_test.go`通过；已运行静态残留扫描确认剩余命中仅为当前 bridge envelope 协议、治理扫描测试字符串和非历史兼容 fallback 文案；已运行`gofmt -l`确认相关 Go 文件无格式残留。
- FB-12 原因分析：删除旧兼容`DataHostService`时也删除了`guest.Directory.Data()`目录入口，但受治理的`pkg/plugin/capability/data` facade 仍然存在，导致动态插件数据库能力只能通过`plugindata.Open()`直接获取，和 runtime、storage、config 等能力统一从`guest.Directory`获取的目标不一致。同类审查还发现动态插件 manifest 与 bridge 协议使用`network`服务名，但 guest 目录暴露`HTTP()`入口，存在 manifest 服务名和代码入口命名不一致的问题。
- FB-12 实现摘要：已恢复`guest.Directory.Data()`，返回受治理的`capability/data.DB` facade；`capability/data`继续拥有结构化查询 DSL 和 WASI host service 调用实现，不恢复旧`DataHostService`兼容接口。动态样例改为通过`guestServices.Data()`获取数据能力，并把受治理网络入口从`HTTP()`收敛为`Network()`，样例通过`guestServices.Network()`访问 outbound network host service。规范残留同步从旧`capability.Directory`改为源码插件`capability.Services`和动态插件`guest.Directory`，并明确 data 入口必须经`guest.Directory.Data()`统一取得。
- FB-12 其它地方审查：已对动态样例`plugin.yaml`中的`hostServices`、`capability/guest.Directory`方法、`pluginbridge`host service 标识和动态样例业务调用进行静态检索。除已修复的 data 直接`Open()`和 network/HTTP 命名映射外，runtime、storage、cron、config、hostConfig 均已通过`guestServices.<Service>()`进入统一目录；manifest、cache、lock、notify、org、tenant 当前样例未消费但已在`guest.Directory`中保留统一入口；源码插件`plugin.HTTP()`是路由注册 facade，不属于动态 host service 消费入口。
- FB-12 规则加载：已读取`AGENTS.md`、`.agents/rules/openspec.md`、`.agents/rules/documentation.md`、`.agents/rules/architecture.md`、`.agents/rules/plugin.md`、`.agents/rules/backend-go.md`、`.agents/rules/dev-tooling.md`、`.agents/rules/testing.md`、`.agents/rules/data-permission.md`、`.agents/rules/cache-consistency.md`、`.agents/rules/i18n.md`和`.agents/rules/api-contract.md`，并使用`lina-feedback`、`lina-review`、`goframe-v2`和`karpathy-guidelines`技能；本段未修改 HTTP API 路由/DTO、SQL、前端、插件清单、语言包或运行时用户可见文案。
- FB-12 影响判断：`i18n`资源无影响，未修改运行时文案、API 文档源文本、插件清单实际文件或语言包；数据权限无新增数据操作语义，动态数据访问仍通过既有`capability/data` DSL、`hostServices`授权快照、用户上下文和宿主 datahost 治理执行；缓存一致性无新增缓存源、失效通道、跨实例协调或运行时快照变更；开发工具跨平台影响仅为补充`linactl` Go 测试中的防回归字符串，不新增命令、脚本或平台专属入口；测试策略为后端 Go 包级测试、动态插件编译测试、WASI 编译 smoke、`linactl`生成器测试、静态残留扫描、OpenSpec 严格校验和格式检查，不新增 E2E。
- FB-12 验证方式：已运行`cd apps/lina-core && go test ./pkg/plugin/capability/guest ./pkg/plugin/capability/data -count=1`通过；已运行`cd apps/lina-plugins/linapro-demo-dynamic && GOWORK=off go test ./... -count=1`通过；已运行`cd apps/lina-core && GOOS=wasip1 GOARCH=wasm go test -c ./pkg/plugin/capability/guest -o /tmp/plugin_capability_guest_data_network_entry.test`通过；已运行`cd hack/tools/linactl && go test ./internal/wasmbuilder -run TestPluginDemoDynamicGeneratedDispatcherIsZeroReflection -count=1`通过；已运行`cd apps/lina-core && go test ./pkg/plugin/capability -run TestRepositoryPluginCapabilityBoundaries -count=1`通过；已运行`openspec validate harden-plugin-capability-boundaries --strict`通过；已运行`git diff --check`覆盖本段修改文件通过；已运行`gofmt -l`确认本段修改 Go 文件无格式残留；已运行静态检索确认动态样例只通过`guestServices.Network()`和`guestServices.Data()`消费对应能力，`capability.Directory`规范正文残留已清理，`pluginbridge.HTTP(`仅剩生成器防回归测试字符串。
